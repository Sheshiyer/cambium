# Sample build tasks — Cambium hands (I2b) live demo

A tiny spec-kit `tasks.md` so `compose run <tenant> --stage build --execute` can drive the
**hands** organ (`skill-clusters/scripts/resolve-task.mjs`) for real — a no-spend proof that the
conductor executes an organ end-to-end. Each task resolves to a real dev cluster by keyword.

Variable-contract exercise: pair this task list with `examples/sample-variable-contract.json` so
`compose plan` shows how `brand_system`, `asset_plan`, `section_plan`, `interaction_plan`, and
`acceptance_checks` survive the genesis → taste → build → ops handoff.

- [ ] T001 Build the landing page hero section in React with Tailwind
- [ ] T002 Add a pricing API endpoint in Python with FastAPI
- [ ] T003 Write the CI deploy workflow for the Cloudflare Worker
