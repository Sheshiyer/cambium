# Figma Style Design Doc Components Screenshot

Create a Figma-style design documentation component board using the shared style spine.

Visible text, exact:
- "CAMBIUM DESIGN SYSTEM"
- "TOKENS"
- "GLYPHS"
- "CONTROLS"
- "STATES"
- "PANELS"

Scene:
- A polished design-system sheet, like a Figma component page exported as an image.
- Show organized component specimens: color tokens, type scale, glyph nodes, progress rings, rails, packet particles, panels, chips, toggles, sliders, segmented controls, status badges, node labels, empty/loading/error states.
- Use a clean grid with ruler-like spacing guides, but keep the Cambium dark cartographic brand.
- Include component state rows: idle, active, stale, warning, selected, reduced-motion.
- It should be useful for designers and engineers as a visual inventory, not a marketing graphic.
