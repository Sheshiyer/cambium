# Elements And Settings Screenshot

Create the elements and settings page screenshot using the shared style spine.

Visible text, exact:
- "ELEMENTS"
- "SETTINGS"
- "PALETTE"
- "MOTION"
- "GLYPHS"
- "REDUCED"

Scene:
- A product surface where the operator/designer tunes visual engine elements.
- Show a split layout: left sidebar of element categories, center live preview of a selected glyph/node, right settings inspector.
- Controls should be familiar but brand-native: segmented controls, toggles, sliders, swatches, icon buttons, and compact menus.
- Include settings for palette, glyph family, ring orbit, packet density, label density, and reduced motion.
- Use the Cambium palette swatches visibly, but keep the page dark and precise.
- Do not write code, CSS, or implementation notes in the UI.
