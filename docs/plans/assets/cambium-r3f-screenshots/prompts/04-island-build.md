# Build Island Screenshot

Create the Build island interior screenshot using the shared style spine.

Visible text, exact:
- "BUILD"
- "HANDS"
- "ARTIFACT"
- "GATES"
- "PASS"

Scene:
- A zoomed island/interior state for the Build/Hands organ.
- Use a triangular extruded glyph as the main island object, with modular rails branching into artifact slabs.
- Show build gates as small vertical locks or check rails, not typical checklist cards.
- The selected route should pass through "GATES" into "ARTIFACT".
- Include a faint 3D workbench plane with simple component silhouettes: page slab, worker line, asset packet, validation marker.
- Keep it R3F-renderable: triangles, slabs, rails, particles, rings.
