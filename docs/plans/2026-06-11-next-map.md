# Next map — after Thalia .1

> Where the flow goes from the released v0.2.1. Ordered by leverage; the quest log itself is the
> position tracker (`quine quests` / the miniapp — arc VII is the honest frontier).

## The ordered queue

| # | Quest | What it is | Size |
|---|---|---|---|
| 1 | **W2.5 · Taste-cortex UI pass** | cambium's taste organ briefs its OWN miniapp; the brief becomes a cortex memory; the UI climbs toward the reference renders (lush rings, living edge, mesh substrate) | this session |
| 2 | **W3 · Narrative mapper** | `bin/operator/narrative/`: real prose beats (not raw log lines) — shares the forge's log grammar; heartbeat-sourced pushes (`source:"heartbeat"`) | S |
| 3 | **W4 · Founder gate** | the ONE write: handoff approvals (`/ts-approve` parity) — Ed25519 initData on the Worker, action queue + paperclip poller, outcomes recorded via `quine write skills record` | M |
| 4 | ~~M3 · Multi-tenancy~~ ✅ **DONE 2026-06-11** — #20–23 + milestone closed, arc VII flipped (7/7 live on the miniapp) | — |
| 5 | **M5 · MultiCA wiring** (below) | the gateway joins the game | M |
| 6 | Housekeeping | M4 GitHub milestone backfill (was permission-blocked); `CF_API_TOKEN` re-mint (restores Vectorize REST; wrangler-OAuth lane works meanwhile) | XS |

## M5 · MultiCA wiring — the untouched organ

Today: the TG dispatcher health-checks MultiCA (`/healthz`) and that is the ENTIRE integration.
Local client config is ready at `~/.multica/config.json` (`server_url · app_url · workspace_id ·
token` — read in-process, house pattern). Architecture (vault CLAUDE.md): MultiCA = model gateway
+ persistent agent-session backend, workspace `Thoughtseedlabs`.

**Phase R — read (first, cheap):** a `multica` quine hypha —
- `status` → gateway reachability (same check the dispatcher makes, on the one surface)
- `read agents|sessions` → workspace agent/session summary (the org's live activity)
- Feeds: narrative beats gain `source:"multica"` entries ("Engineer session opened on heyzack-x"),
  and `/ts-status` parity lands in the miniapp later.

**Phase G — gateway (second):** cambium's model calls (npc chat · embeddings) gain a `multica`
provider branch beside NVIDIA/Kimi in `pickProvider` — centralized keys/billing/observability;
direct NIM stays the fallback. Zero call-site changes (the provider seam already exists).

**Phase Q — quest surface (with M3+):** org activity becomes quest evidence where it is REAL
(e.g. handoff counts from the gate, session activity per venture) — never decoration.

## Standing context

- UI fidelity self-assessment: ~15% → this pass targets the reference renders in
  `03-Resources/Design/Curios-Self-Quest-Miniapp/` (vault).
- All Telegram rules hold (Hermes sole channel; miniapp = surface, polling = house transport).
- Vault R2 stays sealed (backup-only). Quest data lane: Worker + KV only.
