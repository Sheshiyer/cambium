# Milestone v0.5 Context: Thoughtseed Labs Consolidation

## Goal

Consolidate Cambium's production Cloudflare authority in the
`thoughtseed-labs` Wrangler profile, reconcile exact `9d9d` source assets
through provenance-bound gates, and retire the legacy source only after
verified parity and a founder-approved rollback window.

## Authority decision

- `thoughtseed-labs` with `workers/quests/wrangler.labs.jsonc` is the sole
  production profile for `curious.thoughtseed.space`.
- `9d9d` with `workers/quests/wrangler.jsonc` is read-only source and
  rollback evidence.
- Labs D1 remains the sole operational task and receipt writer.
- Hermes remains Telegram transport and does not become task authority.
- ISA owns acceptance; GSD owns finite milestone execution.

## Finite horizon

1. **Phase 8 — Labs Authority and Profile Safety:** make the repository
   fail closed on profile selection and publish the stable resource map.
2. **Phase 9 — Source Inventory and Classification:** obtain authenticated
   source-key evidence and classify every exact delta without copying data.
3. **Phase 10 — Allowlisted Reconciliation and Retirement:** copy only
   separately approved keys, verify parity, observe the rollback window, and
   retire source assets only after a fresh founder gate.

## Current authorization

Phase 8 repository implementation is approved. No Cloudflare write, deploy,
traffic, DNS, Access, tunnel, D1, KV, R2, Vectorize, copy, deletion, or
retirement operation is authorized by this planning packet.

## Evidence boundary

Mutable object counts are observations, not a transfer manifest. Phase 9 must
produce exact key and digest evidence from authenticated read-only source
access before Phase 10 can propose an allowlist.
