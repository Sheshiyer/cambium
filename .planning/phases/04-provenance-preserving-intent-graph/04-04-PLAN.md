---
phase: 04-provenance-preserving-intent-graph
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - .planning/phases/04-provenance-preserving-intent-graph/04-04-PLAN.md
  - .planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - scripts/infinite-game-anchors.test.mjs
  - workers/quests/src/goal-graph/projection-contract.test.ts
  - workers/quests/src/goal-graph-intake.test.ts
  - workers/quests/src/handler.test.ts
  - workers/quests/src/goal-graph/projection-contract.ts
  - workers/quests/src/goal-graph-intake.ts
  - ISA.md
  - .project/HANDOFF.md
autonomous: true
gap_closure: true
requirements: []
gap_requirements:
  - GRAPH-04
must_haves:
  truths:
    - "GRAPH-04 / ISC-1280: valid and malformed cambium.intent-graph-projection.v1 values are rejected by the shared D1 authoritative-input guard."
    - "The production POST /v1/bridge/goal-graph-intake path invokes the shared guard before any D1 read or KV task/idempotency write; only the existing bounded rejection receipt may be persisted."
    - "Goal Graph envelope validation remains precise, unrelated authoritative commands remain accepted, and the Telegram parser has no competing projection discriminator."
    - "GRAPH-01, GRAPH-02, GRAPH-03, and GRAPH-05 remain regression-green; generated intent-graph semantics, root anchors, and committed readbacks do not change."
    - "ISC-1280 is visibly reopened while the gap is executable and may return to verify 5/5 only from committed shared-guard, route, and full-suite execution evidence."
    - "After the fourth SUMMARY, the orchestrator normalizes GSD's count-derived transient Complete state to a committed 4/4 verification hold before code review and the built-in verifier run."
    - "The gap-only executor leaves Phase 4 and GRAPH-04 open until execute-phase's built-in independent gsd-verifier replaces 04-VERIFICATION.md with `status: passed`; only that passed branch may invoke `phase.complete`."
    - "If the built-in verifier returns `gaps_found`, the orchestrator commits the replacement report plus an open Phase 4, unchecked GRAPH-04, verifying STATE, and `/gsd:plan-phase 4 --gaps` routing."
  artifacts:
    - path: workers/quests/src/goal-graph/projection-contract.ts
      provides: "Shared D1 discriminator and authoritative-input rejection for Goal Graph and Intent Graph projection families"
      exports:
        - isGoalGraphProjection
        - isIntentGraphProjection
        - isDerivedGraphProjection
        - validateAuthoritativeInput
    - path: workers/quests/src/goal-graph/projection-contract.test.ts
      provides: "Shared-guard valid, malformed, and unrelated-authority regression contract"
      contains: "cambium.intent-graph-projection.v1"
    - path: workers/quests/src/goal-graph-intake.ts
      provides: "Production Telegram intent boundary wired to the shared authoritative-input guard"
      contains: "validateAuthoritativeInput"
    - path: workers/quests/src/handler.test.ts
      provides: "Behavior-level HTTP proof of pre-D1/pre-task projection rejection"
      contains: "goal graph intake"
    - path: ISA.md
      provides: "Truthful reopened and repaired ISC-1280 acceptance evidence"
      contains: "ISC-1280"
    - path: .project/HANDOFF.md
      provides: "Bounded gap-closure receipt and built-in verification lifecycle"
      contains: "built-in gsd-verifier"
    - path: .planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md
      provides: "Executor-owned committed evidence consumed by the built-in phase verifier"
      contains: "GAP_BASE_SHA"
    - path: .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md
      provides: "Verifier-owned post-execution phase verdict"
      contains: "status: passed"
  key_links:
    - from: workers/quests/src/goal-graph-intake.ts
      to: workers/quests/src/goal-graph/projection-contract.ts
      via: "a source-level RED invariant and GREEN behavior proof require parseTelegramGoalGraphIntentBoundary to import and call validateAuthoritativeInput before stableJson/normalization, with no projectionLike definition or call"
      pattern: "validateAuthoritativeInput.*stableJson|projectionLike"
    - from: workers/quests/src/handler.ts
      to: workers/quests/src/goal-graph-intake.ts
      via: "intakeTelegramGoalGraphRoute calls the pure boundary before KV reconciliation and goalGraphStore reads"
      pattern: "parseTelegramGoalGraphIntentBoundary"
    - from: workers/quests/src/handler.test.ts
      to: POST /v1/bridge/goal-graph-intake
      via: "valid and malformed intent-projection route probes with mutation/read counters"
      pattern: "goal-graph-intake"
    - from: ISA.md
      to: workers/quests/src/goal-graph/projection-contract.test.ts
      via: "ISC-1280 names the shared D1 guard proof rather than compiler-local foldback alone"
      pattern: "projection-contract.test.ts"
    - from: .planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md
      to: .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md
      via: "execute-phase invokes the independent gsd-verifier only after the executor SUMMARY and metadata exist; the verifier must enforce the post-execution contract before returning passed"
      pattern: "status: passed"
    - from: .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md
      to: .planning/REQUIREMENTS.md
      via: "only execute-phase's passed verifier branch invokes phase.complete, which may mark GRAPH-04 complete; gaps_found leaves it unchecked"
      pattern: "GRAPH-04"
---

<objective>
Close the verified D1 foldback gap without weakening the published Phase 4 authority claim.

Purpose: Make the shared D1 authoritative-input boundary reject valid and malformed Intent Graph projections, prove the production Goal Graph intake consumes that boundary before any authority read or proposal mutation, and restore truthful Phase 4 acceptance.
Output: One RED gap contract, one GREEN shared-guard/intake integration, and one committed ISA/handoff repair ready for execute-phase's built-in independent phase verifier.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@.project/HANDOFF.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md
@.planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md
@.planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md
@.planning/phases/04-provenance-preserving-intent-graph/04-03-SUMMARY.md
@ISA.md
@workers/quests/src/goal-graph/projection-contract.ts
@workers/quests/src/goal-graph/projection-contract.test.ts
@workers/quests/src/goal-graph-intake.ts
@workers/quests/src/goal-graph-intake.test.ts
@workers/quests/src/handler.ts
@workers/quests/src/handler.test.ts
@scripts/infinite-game-anchors.test.mjs
@scripts/intent-graph.mjs
@scripts/intent-graph.test.mjs
@scripts/generate-intent-graph.test.mjs

<interfaces>
Existing authority seam:
- `validateProjectionEnvelope` is the precise validator for `cambium.goal-graph-projection.v1`; it must not be broadened into an Intent Graph schema validator.
- `validateAuthoritativeInput` is the shared fresh-authority guard. It currently recognizes only Goal Graph projection markers and is the gap source.
- `parseTelegramGoalGraphIntentBoundary` is invoked by `intakeTelegramGoalGraphRoute` before idempotency reconciliation, `goalGraphStore.readHead`, `goalGraphStore.readNodes`, compilation, or task persistence.
- The existing intake rejection contract may persist one bounded `cambium.goal-graph-intake-rejection.v1` receipt; it may not echo raw projection bodies.
- The compiler-local `projectionShaped` check remains a separate defense for read-projection compilation, not evidence that the D1 authority lane is guarded.

Required rejection fixtures:
- Valid-shaped Intent Graph projection: exact schema, `projectionAuthority: read_only`, lowercase SHA-256 `sourceSetDigest` and `graphDigest`, bounded `nodes`, and bounded `edges`.
- Malformed Intent Graph projection: exact schema marker with missing or invalid envelope fields.
- Unrelated authoritative input: a normal Goal Graph command-shaped record with no recognized derived-projection marker.

Immutable range baseline:
- `GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25` is the exact verified gap head and never floats during this plan.
- Before the planning packet is committed, the planner must record that `git rev-parse HEAD` equals `GAP_BASE_SHA`. Before Task 1 changes any execution file, the executor must prove that every commit after `GAP_BASE_SHA` is planning-only and every changed path is one of `.planning/phases/04-provenance-preserving-intent-graph/04-04-PLAN.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `.planning/REQUIREMENTS.md`; this planning commit is the sole permitted reason execution HEAD differs from `GAP_BASE_SHA`.
- Every later pre/post comparison uses either `git diff "$GAP_BASE_SHA"` to include the current worktree or `git diff "$GAP_BASE_SHA...HEAD"` for committed evidence; never use a relative ancestor, merge-base guess, or moving remote.

Lifecycle contract during gap execution:
- `ISA.md` moves to `phase: execute`, `progress: 4/5`; ISC-1277, ISC-1278, ISC-1279, and ISC-1281 remain checked, while only ISC-1280 is unchecked.
- `scripts/infinite-game-anchors.test.mjs` accepts that exact gap state and rejects every other partial Phase 4 combination.
- Committed execution evidence may return ISA to `phase: verify`, `progress: 5/5`; that records ISC evidence only. Because `roadmap.update-plan-progress` derives a transient Complete label when the fourth SUMMARY appears, the execute-phase orchestrator must immediately apply and commit the verification hold below before code review or verification.
- `04-VERIFICATION.md` is immutable input throughout Tasks 1–3 and executor-owned `04-04-SUMMARY.md`/metadata creation: the executor may read and cite it but cannot edit, stage, commit, supersede, or replace it.
- After Tasks 1–3, `04-04-SUMMARY.md`, and executor/orchestrator plan-progress metadata are complete, execute-phase's built-in independent `gsd-verifier` is authorized to replace `04-VERIFICATION.md`. Only `status: passed` may flow to `phase.complete`; `gaps_found` preserves the open phase and routes back to `/gsd:plan-phase 4 --gaps`. A later `/gsd:verify-work 4` remains an optional manual re-verification path, not the normal required gap-cycle step.
- Standard `requirements: []` is intentional: `execute-plan` runs `requirements.mark-complete` before independent verification, so this gap plan must not prematurely check GRAPH-04. `gap_requirements: [GRAPH-04]` is read-only traceability for planning/verifier consumption; shipped Plans 04-01 through 04-03 retain the original phase-wide GRAPH-01 through GRAPH-05 mapping.
</interfaces>
</context>

<scope_exception>
This single gap plan intentionally couples five test/control paths (`scripts/infinite-game-anchors.test.mjs`, `projection-contract.test.ts`, `goal-graph-intake.test.ts`, `handler.test.ts`, and the executable control prompt `04-04-PLAN.md`), two production authority-boundary paths (`projection-contract.ts`, `goal-graph-intake.ts`), ISA/HANDOFF evidence, and four GSD planning artifacts (`04-04-SUMMARY.md`, STATE, ROADMAP, REQUIREMENTS). The gap is one end-to-end authority invariant: discriminator → pure intake → HTTP pre-D1 behavior → acceptance evidence → one independent verifier lifecycle. Splitting it into separate plans would either permit production wiring without the committed RED route invariant or cause multiple verifier/requirements completion lifecycles for one requirement. The broad declared write set is therefore a correctness boundary, not hidden scope; Task commits remain narrowly staged and all runtime/external mutations stay forbidden.
</scope_exception>

<execution_preflight>
Set `GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25`. The planning orchestrator records `test "$(git rev-parse HEAD)" = "$GAP_BASE_SHA"` before committing this packet. At execution start, require `git merge-base --is-ancestor "$GAP_BASE_SHA" HEAD`, require at least one planning commit after the base, and reject any `GAP_BASE_SHA...HEAD` path other than the four planning packet paths named above. This reconciles the immutable execution baseline with the required planning commit without treating the planning commit as runtime work. Abort if either invariant fails.

Planning evidence captured before Task 1: `git rev-parse HEAD` returned `1b9267457173b9e58ec54bd8d25f0d9dba2dfe25`; the only planning changes were the new `04-04-PLAN.md` plus `STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md`; no 04-01 through 04-03 plan or summary changed.
</execution_preflight>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reopen ISC-1280 and commit the RED shared-boundary contract</name>
  <files>scripts/infinite-game-anchors.test.mjs, workers/quests/src/goal-graph/projection-contract.test.ts, workers/quests/src/goal-graph-intake.test.ts, workers/quests/src/handler.test.ts, ISA.md</files>
  <read_first>
    - .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md — preserve the exact `gaps_found` diagnosis and close the D1 key-link, not merely the compiler-local test.
    - ISA.md — retain the exact active Goal and all historical checkboxes; reopen only ISC-1280 and replace its misleading local-only evidence at closure.
    - scripts/infinite-game-anchors.test.mjs — extend the existing coherent-state predicate with exactly the Phase 4 execute 4/5 gap state; do not accept arbitrary partial progress.
    - workers/quests/src/goal-graph/projection-contract.ts and projection-contract.test.ts — bind RED directly to `validateAuthoritativeInput`; preserve the precise Goal Graph envelope result and unrelated-input acceptance.
    - workers/quests/src/goal-graph-intake.ts and goal-graph-intake.test.ts — prove both projection families enter the shared guard and keep the current `projection_input` parser result.
    - workers/quests/src/handler.ts lines containing `intakeTelegramGoalGraphRoute` and `POST /v1/bridge/goal-graph-intake` — locate the production pre-KV/pre-D1 seam without moving authentication or receipt behavior.
    - workers/quests/src/handler.test.ts Goal Graph intake harness/tests — reuse the in-memory D1/KV harness and explicit read/mutation assertions.
    - scripts/intent-graph.test.mjs projection-foldback test — preserve it as compiler-local defense, but do not use it as the new D1 proof.
  </read_first>
  <behavior>
    - A valid-shaped `cambium.intent-graph-projection.v1` object makes `validateAuthoritativeInput` return `accepted: false`.
    - A malformed object carrying the same exact Intent Graph schema marker also returns `accepted: false`.
    - A valid and malformed Goal Graph projection continue to be rejected, and valid Goal Graph envelopes continue to use the precise existing validator result.
    - An unrelated authoritative command remains accepted with its original value.
    - The pure Telegram intake returns `projection_input` for both valid and malformed Intent Graph projections.
    - A named source-wiring invariant proves `goal-graph-intake.ts` imports `validateAuthoritativeInput`, calls it inside `parseTelegramGoalGraphIntentBoundary` before `stableJson(input)` and normalization, and contains no `projectionLike` definition or call.
    - The authenticated HTTP route returns the existing bounded rejection shape for both fixtures before any D1 read, task write, or idempotency write; one redacted rejection receipt is allowed per request.
    - Normal Telegram intake, idempotent replay, approval/CAS, Goal Graph projection rejection, and compiler-local foldback tests remain unchanged and green after GREEN.
  </behavior>
  <action>
First set `GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25` and execute the preflight above; abort rather than rebasing, amending, or changing the baseline. Then run and record the direct D1 probe from `04-VERIFICATION.md`, proving both Intent Graph fixtures currently return `accepted:true`. Change ISA only to the exact execute 4/5 gap state in interfaces and annotate ISC-1280 as reopened by `04-VERIFICATION.md`; do not check it or claim repaired evidence. Extend `isCoherentIsaPhaseState` and its table tests to accept only that exact checkbox combination.

Add RED assertions to `projection-contract.test.ts` for valid-shaped and malformed Intent Graph projections plus the existing unrelated command. Add matching boundary assertions to `goal-graph-intake.test.ts`, including a named source-wiring test that reads `goal-graph-intake.ts` and requires: an import of `validateAuthoritativeInput` from `./goal-graph/projection-contract.ts`; an exact `validateAuthoritativeInput(input)` call within `parseTelegramGoalGraphIntentBoundary` positioned before `stableJson(input)` and before `normalizeInput(input)`; and zero `function projectionLike` definitions or `projectionLike(` calls. This invariant must fail on the current source even though the local `projectionLike` parser already rejects both fixtures. Add behavior-level route tests in `handler.test.ts` that authenticate through the existing bridge/Hermes lane, instrument `readHead` and `readNodes`, snapshot KV, submit both fixtures, and prove: response `accepted:false`/`code:projection_input`; D1 read count is zero; no `goal-graph-intake-task:` or `goal-graph-intake-idem:` key appears; D1 head/nodes remain byte-equivalent; any new KV key is only a bounded `goal-graph-intake-rejection:` receipt with no raw node, edge, digest, or payload echo. Do not modify production source in this task. Run RED and prove failure includes both the shared-guard result and the source-wiring invariant, rather than passing on the pre-existing local parser; reject syntax, module-resolution, authentication, or fixture failures. Commit the lifecycle reopen plus all three immutable RED boundaries atomically.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25; git merge-base --is-ancestor "$GAP_BASE_SHA" HEAD; node --test scripts/infinite-game-anchors.test.mjs; out=$(mktemp); trap '\''rm -f "$out"'\'' EXIT; if node --experimental-strip-types --test --test-name-pattern="intent projection|derived projection|shared guard wiring|goal graph intake.*projection" workers/quests/src/goal-graph/projection-contract.test.ts workers/quests/src/goal-graph-intake.test.ts workers/quests/src/handler.test.ts &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; rg -q "not ok" "$out"; rg -q "accepted|validateAuthoritativeInput|projectionLike|shared guard wiring" "$out"; if rg -q "SyntaxError|ERR_MODULE_NOT_FOUND|ENOENT|not authorized|fixture intent must parse" "$out"; then cat "$out"; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - ISA is exactly execute 4/5 with only ISC-1280 reopened, and the anchor lifecycle test accepts no other partial Phase 4 state.
    - All three RED test surfaces contain explicit valid Intent Graph, malformed Intent Graph, and unrelated-authority assertions at their appropriate layer.
    - RED fails a named wiring invariant unless the intake imports/calls the shared guard before stableJson/normalization and removes every projectionLike definition/call; fixture behavior alone cannot satisfy the test.
    - Route RED asserts zero D1 reads, zero task/idempotency writes, unchanged D1 data, bounded rejection-only KV mutation, and no projection-body echo.
    - RED exits non-zero on a named shared-guard or route assertion and contains no syntax/module/authentication/fixture failure.
    - No production source, generated graph, root anchor, documentation contract, package, lockfile, migration, deployment, or connected repository changes in this commit.
  </acceptance_criteria>
  <commit>Stage only scripts/infinite-game-anchors.test.mjs, workers/quests/src/goal-graph/projection-contract.test.ts, workers/quests/src/goal-graph-intake.test.ts, workers/quests/src/handler.test.ts, and ISA.md; commit `test(04-04): expose D1 intent projection foldback gap`.</commit>
  <done>The acceptance source is truthful and one committed RED chain reproduces the gap from shared guard through the production HTTP route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared derived-projection guard and wire intake</name>
  <files>workers/quests/src/goal-graph/projection-contract.ts, workers/quests/src/goal-graph-intake.ts</files>
  <read_first>
    - The committed Task 1 tests — satisfy every assertion without weakening, deleting, renaming, or bypassing a fixture.
    - workers/quests/src/goal-graph/projection-contract.ts — retain the Worker/D1-independent pure module, compatibility aliases, Goal Graph validator, and existing reason behavior.
    - workers/quests/src/goal-graph-intake.ts — remove the independent `projectionLike` policy after the shared guard covers it; preserve total parsing, limits, forbidden keys, canonicalization, and compile behavior.
    - workers/quests/src/handler.ts `intakeTelegramGoalGraphRoute` — confirm the boundary call still precedes idempotency reconciliation and all `goalGraphStore` reads; no handler source edit is required unless this ordering is no longer true.
    - docs/architecture/contracts/intent-graph-v1.md and docs/architecture/goal-graph-operating-model.md — implement their existing D1 rejection claim exactly; do not weaken or rewrite the published contract.
  </read_first>
  <action>
In `projection-contract.ts`, add the exact Intent Graph schema constant and exported `isIntentGraphProjection` plus `isDerivedGraphProjection` family discriminator. Keep `isGoalGraphProjection` and `validateProjectionEnvelope` Goal Graph-specific so valid Goal Graph evidence still receives precise envelope validation. Recognize the exact Intent Graph schema and normalized malformed/future `intent-graph` plus `projection` schema markers without treating arbitrary records, generic `origin`, or unrelated command fields as projections. Extend the authoritative-input rejection reason union with an Intent Graph-specific non-authoritative result and make `validateAuthoritativeInput` reject both valid-shaped and malformed Intent Graph markers before its accepted return. Do not import the Phase 4 JavaScript compiler, Worker, D1, Telegram, clock, or provider modules and do not duplicate the full Intent Graph validator.

In `goal-graph-intake.ts`, import and call `validateAuthoritativeInput` at the start of `parseTelegramGoalGraphIntentBoundary`, before canonicalization, forbidden-key traversal, normalization, or compilation. Translate any shared derived-projection rejection to the established total `projection_input` result using bounded shared-guard errors. Remove the local `projectionLike` discriminator so the D1 guard is the single projection-family policy. Preserve every existing Telegram schema/default/bounds/canonical digest/idempotency behavior. Because `intakeTelegramGoalGraphRoute` already calls this boundary before KV reconciliation and D1 reads, keep its ordering unchanged; do not add a second handler-only discriminator.

Run the committed focused tests GREEN, then run all Goal Graph intake route tests to preserve accepted intent, Goal Graph rejection, replay collapse, approval/CAS, stale-head, and rejection-receipt behavior. Run the direct D1 probe and require valid/malformed rejection plus unrelated acceptance before committing both source files atomically.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25; node --experimental-strip-types --test workers/quests/src/goal-graph/projection-contract.test.ts workers/quests/src/goal-graph-intake.test.ts; node --experimental-strip-types --test --test-name-pattern="shared guard wiring" workers/quests/src/goal-graph-intake.test.ts; node --experimental-strip-types --test --test-name-pattern="goal graph intake|goal graph approval" workers/quests/src/handler.test.ts; node --experimental-strip-types -e '\''import("./workers/quests/src/goal-graph/projection-contract.ts").then(({validateAuthoritativeInput})=&gt;{const d=`sha256:${"a".repeat(64)}`; const fixtures=[{schema:"cambium.intent-graph-projection.v1",projectionAuthority:"read_only",sourceSetDigest:d,graphDigest:d,nodes:[],edges:[]},{schema:"cambium.intent-graph-projection.v1"}]; for(const value of fixtures){const result=validateAuthoritativeInput(value); if(result.accepted) throw new Error("intent projection accepted")}; const command={kind:"goal-command",tenant:"cambium",command:"add-node"}; const accepted=validateAuthoritativeInput(command); if(!accepted.accepted||accepted.value!==command) throw new Error("unrelated command rejected")})'\''; git diff --check "$GAP_BASE_SHA" -- workers/quests/src/goal-graph/projection-contract.ts workers/quests/src/goal-graph-intake.ts'</automated>
  </verify>
  <acceptance_criteria>
    - The shared guard rejects exact, malformed, and normalized Intent Graph projection markers while unrelated commands remain accepted.
    - Goal Graph envelope validation and existing Goal Graph projection reasons remain precise and regression-green.
    - `parseTelegramGoalGraphIntentBoundary` consumes the shared guard and no local competing projection-family discriminator remains.
    - The committed source-level invariant proves the import, early call ordering, and complete removal of the `projectionLike` definition/call; the old local-parser implementation cannot pass GREEN.
    - Route tests prove the shared guard executes before D1/KV task/idempotency work and preserve only the bounded rejection receipt.
    - Existing Telegram acceptance, Goal Graph projection rejection, idempotent replay, approval/CAS, stale-head, and compiler-local foldback tests remain green.
    - No D1 data, D1 schema, migration, provider, deployment, Telegram send, generated graph, root anchor, or connected-repository mutation occurs.
  </acceptance_criteria>
  <commit>Stage only workers/quests/src/goal-graph/projection-contract.ts and workers/quests/src/goal-graph-intake.ts; commit `fix(04-04): enforce D1 intent projection boundary`.</commit>
  <done>The shared D1 guard is the production projection-family policy and the authoritative intake rejects foldback before reading or staging operational state.</done>
</task>

<task type="auto">
  <name>Task 3: Repair ISC-1280 evidence and publish the bounded verifier handoff</name>
  <files>ISA.md, .project/HANDOFF.md</files>
  <read_first>
    - ISA.md — preserve the approved Goal, Phase 4 iteration, ISC identifiers, and all non-ISC-1280 historical acceptance state.
    - .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md — cite its `gaps_found` diagnosis as immutable input while writing corrective ISA/HANDOFF evidence; do not edit, stage, commit, supersede, or replace the report during Tasks 1–3 or executor metadata/SUMMARY creation.
    - Task 1 and Task 2 commits — name exact committed SHAs and command results in acceptance evidence.
    - .project/HANDOFF.md newest Phase 4 checkpoint — append a corrective checkpoint; state that the older D1 claim was not executable at that head.
    - docs/architecture/intent-graph.v1.json and docs/architecture/intent-graph.md — confirm no generated semantic or digest change.
    - .planning/STATE.md — leave plan execution in verification state; execute-phase's built-in gsd-verifier, not this task, owns the independent verdict and next transition.
  </read_first>
  <action>
From committed Task 2 head, set the immutable `GAP_BASE_SHA`, then run the focused guard/intake/route tests, source-wiring invariant, combined Phase 4 anchor/compiler/generator tests, direct D1 probe, generator `--check` twice, full `npm test`, drift audit, rendered-doc check, exact allowed-path membership, delete/rename rejection, committed-added-line privacy scan, root-anchor preservation, generated-readback preservation, and forbidden mutation scans listed below. Stop on any failure or high-severity threat; do not check ISC-1280 from a partial or uncommitted result.

After all gates pass, restore ISA to `phase: verify`, `progress: 5/5`, check only ISC-1280, and rewrite ISC-1280's misleading compiler-local-only verification line with exact shared `projection-contract.test.ts`, pure intake, HTTP route, zero-D1-read/zero-task-idempotency-write, direct-probe, and full-gate evidence. Preserve the compiler-local foldback result as defense-in-depth, not the D1 proof. This ISA state means execution evidence is ready for audit; it does not close Phase 4 or GRAPH-04. Append a newest `.project/HANDOFF.md` checkpoint that identifies the original false-green verification, both atomic gap commits, bounded runtime/test/acceptance write set, preserved rejection receipt behavior, unchanged D1 data/schema and root/generated semantics, held external boundaries, and the normal next lifecycle step: execute-phase's built-in independent `gsd-verifier`. State that `/gsd:verify-work 4` is optional manual re-verification, not required in the normal gap cycle.

Commit ISA and handoff together only after rerunning focused tests, direct probe, generator checks, anchor lifecycle, render, and immutable-range checks. Task 3 gates the executor-owned committed source/evidence range; it does not pretend `04-04-SUMMARY.md` already exists. After all tasks commit, the executor must create and atomically commit `04-04-SUMMARY.md`; execute-plan then records plan 04-04 executed. Its count-derived Roadmap update may transiently label the phase Complete, so the execute-phase orchestrator must run `<orchestrator_verification_hold>` immediately afterward. The committed pre-verifier state must read four PLAN/SUMMARY pairs, Phase 4 awaiting verification, GRAPH-04 unchecked, and STATE in verification. Through that metadata boundary, `04-VERIFICATION.md` must remain byte-identical to the blob at `GAP_BASE_SHA`. Do not begin Phase 5: code review and the built-in independent verifier follow, and only the verifier's `status: passed` branch may call `phase.complete`.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25; git merge-base --is-ancestor "$GAP_BASE_SHA" HEAD; node --experimental-strip-types --test workers/quests/src/goal-graph/projection-contract.test.ts workers/quests/src/goal-graph-intake.test.ts; node --experimental-strip-types --test --test-name-pattern="goal graph intake|goal graph approval" workers/quests/src/handler.test.ts; node --test scripts/infinite-game-anchors.test.mjs scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs; node --experimental-strip-types -e '\''import("./workers/quests/src/goal-graph/projection-contract.ts").then(({validateAuthoritativeInput})=&gt;{const d=`sha256:${"a".repeat(64)}`; const fixtures=[{schema:"cambium.intent-graph-projection.v1",projectionAuthority:"read_only",sourceSetDigest:d,graphDigest:d,nodes:[],edges:[]},{schema:"cambium.intent-graph-projection.v1"}]; for(const value of fixtures) if(validateAuthoritativeInput(value).accepted) throw new Error("intent projection accepted"); if(!validateAuthoritativeInput({kind:"goal-command",tenant:"cambium",command:"add-node"}).accepted) throw new Error("unrelated command rejected")})'\''; node scripts/generate-intent-graph.mjs --check; node scripts/generate-intent-graph.mjs --check; npm test; npm run drift:audit; npm run render-docs:check; git diff --check "$GAP_BASE_SHA"; git diff --quiet "$GAP_BASE_SHA" -- .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md; allowed=".planning/phases/04-provenance-preserving-intent-graph/04-04-PLAN.md .planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md scripts/infinite-game-anchors.test.mjs workers/quests/src/goal-graph/projection-contract.test.ts workers/quests/src/goal-graph-intake.test.ts workers/quests/src/handler.test.ts workers/quests/src/goal-graph/projection-contract.ts workers/quests/src/goal-graph-intake.ts ISA.md .project/HANDOFF.md"; while IFS= read -r path; do case " $allowed " in *" $path "*) ;; *) echo "unexpected path: $path"; exit 1;; esac; done &lt; &lt;(git diff --name-only --diff-filter=ACM "$GAP_BASE_SHA"); test -z "$(git diff --name-only --diff-filter=DR "$GAP_BASE_SHA")"; git diff --quiet "$GAP_BASE_SHA" -- VISION.md MISSION.md docs/architecture/intent-graph.v1.json docs/architecture/intent-graph.md; user_path='\''/'\''"Users/"; volume_path='\''/'\''"Volumes/"; private_marker="BEGIN "'\''PRIVATE KEY'\''; added=$(git diff --unified=0 "$GAP_BASE_SHA" | sed -n '\''s/^+//p'\''); if printf "%s\\n" "$added" | rg -n "$user_path|$volume_path|$private_marker|AKIA[0-9A-Z]{16}|gh[pousr]_[A-Za-z0-9_]{20,}"; then exit 1; fi; if git diff --name-only "$GAP_BASE_SHA" | rg "^(migrations/|\\.temperance/|\\.github/workflows/|wrangler|package(-lock)?\\.json$|pnpm-lock\\.yaml$|yarn\\.lock$|apps/|.*(?:provider|deployment).*(?:json|toml|yaml|yml|ts|js|mjs)$)"; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - All focused unit, pure-intake, route, combined Phase 4, generator, full-suite, drift, render, diff, root/generated-preservation, and forbidden-mutation gates pass from committed GREEN source.
    - Direct D1 guard probe rejects valid and malformed Intent Graph projections and accepts the unrelated command.
    - ISA returns to verify 5/5 with only ISC-1280 newly re-checked and with shared D1 plus production-route evidence, not compiler-local evidence alone.
    - After executor metadata records four PLAN/SUMMARY pairs, the orchestrator commits the verification hold that reopens any count-derived transient completion, keeps GRAPH-04 unchecked, and puts STATE in verification.
    - `git diff --quiet "$GAP_BASE_SHA" -- .planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md` proves the committed gap report is byte-preserved through Tasks 1–3 and SUMMARY/metadata creation; only the subsequent verifier-owned step may replace it.
    - Handoff explicitly corrects the prior false-green claim, names both gap commits and the bounded write set, and names the built-in verifier as the normal next lifecycle step while retaining `/gsd:verify-work 4` as optional manual re-verification.
    - No D1 table/schema/data, provider/deploy/Telegram send, root anchor, generated graph semantic, package/lockfile, `.temperance`, or connected-repository mutation exists.
    - Phase 5 remains blocked until independent GSD re-verification reports `status: passed`.
  </acceptance_criteria>
  <commit>Stage only ISA.md and .project/HANDOFF.md; commit `docs(04-04): repair D1 foldback acceptance evidence`.</commit>
  <done>The gap implementation is committed, ISA/handoff evidence is truthful, and Phase 4 is ready—but not yet declared passed—for execute-phase's built-in independent verification.</done>
</task>

</tasks>

<orchestrator_verification_hold>
This is an execute-phase orchestrator step, not an executor task and not a second verifier. Run it immediately after `04-04-SUMMARY.md` and ordinary plan-progress metadata are committed, before the required code-review gate:

1. Read the committed Roadmap, State, and Requirements. Preserve all four Phase 4 plan checkmarks and the exact `4/4` count, but normalize the Phase 4 milestone checkbox to open, its phase `**Plans:**` line to `4/4 plans executed — awaiting verification`, and its progress-table status/date to `Awaiting verification` / `-`. This is the explicit exception to the count-derived `roadmap.update-plan-progress` result; no registered SDK command models this intermediate verification hold.
2. Require GRAPH-04 to remain unchecked in both the requirements list and traceability table. Standard `requirements: []` on 04-04 means no requirement completion is permitted at the plan metadata boundary.
3. Use `gsd-sdk query state.patch` for registered State fields, then verify STATE reads Phase 4, plan 4/4, status `verifying`, and identifies the built-in `gsd-verifier` as the current step. Apply the narrow Roadmap/Requirements normalization with the repository patch tool because the SDK exposes no reopen/awaiting-verification mutation; stage no other path.
4. Commit exactly `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md` as `docs(phase-04): enter verification hold`. Re-read the three committed files and require `04-VERIFICATION.md` still equals its `GAP_BASE_SHA` blob.
5. Run execute-phase's mandatory code-review gate. Its only additional allowed artifact is `04-REVIEW.md`; the built-in verifier must inspect its status and findings but code review remains advisory as defined by the upstream workflow.

If the built-in verifier later returns `gaps_found`, apply the same hold normalization, set STATE's next command to `/gsd:plan-phase 4 --gaps`, and commit exactly the verifier-owned `04-VERIFICATION.md` plus STATE/ROADMAP/REQUIREMENTS as `docs(phase-04): persist verification gaps`. If it returns `passed`, do not normalize again: invoke the upstream `phase.complete` handler, which exclusively owns final Phase 4 and GRAPH-04 completion.
</orchestrator_verification_hold>

<post_execution_verifier_contract>
The following contract is consumed by execute-phase's built-in independent `gsd-verifier` after Task 3, `04-04-SUMMARY.md`, the verification-hold commit, and the required `04-REVIEW.md` are committed. The verifier must not report `status: passed` unless every item succeeds:

1. Pin `GAP_BASE_SHA=1b9267457173b9e58ec54bd8d25f0d9dba2dfe25`; require `git merge-base --is-ancestor "$GAP_BASE_SHA" HEAD` and inspect the exact committed range `"$GAP_BASE_SHA"...HEAD`, never a floating remote or relative ancestor.
2. Require all four Phase 4 PLAN/SUMMARY pairs, require the Task 1 RED, Task 2 GREEN, Task 3 acceptance, and executor metadata/SUMMARY commits to be descendants of `GAP_BASE_SHA`, and cross-check the exact SHAs and command evidence recorded by `04-04-SUMMARY.md` against git history.
3. Before replacing the report, require `git status --porcelain --untracked-files=all` to be empty. During the verifier-owned report-write step, the only permitted dirty path is `.planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md`; after the verification/phase-completion commit, the worktree must again be clean. No unexpected untracked path is permitted at any point.
4. The complete allowed changed-path set from `GAP_BASE_SHA` is exactly: `.planning/phases/04-provenance-preserving-intent-graph/04-04-PLAN.md`, `.planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md`, `.planning/phases/04-provenance-preserving-intent-graph/04-REVIEW.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `scripts/infinite-game-anchors.test.mjs`, `workers/quests/src/goal-graph/projection-contract.test.ts`, `workers/quests/src/goal-graph-intake.test.ts`, `workers/quests/src/handler.test.ts`, `workers/quests/src/goal-graph/projection-contract.ts`, `workers/quests/src/goal-graph-intake.ts`, `ISA.md`, `.project/HANDOFF.md`, plus `.planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md` only when the verifier owns its replacement. Reject every other tracked or untracked path and every delete or rename. Read `04-REVIEW.md`; unresolved findings must be represented truthfully in the verdict.
5. Re-run the committed added-line privacy/local-path/secret scan across `GAP_BASE_SHA...HEAD`; reject machine-local home or mounted-volume prefixes, private-key markers, AWS/GitHub token patterns, credentials, prompt/response bodies, or native session identifiers. Assemble every forbidden marker at runtime so the control text cannot match its own scan. Require `VISION.md`, `MISSION.md`, `docs/architecture/intent-graph.v1.json`, and `docs/architecture/intent-graph.md` to be byte-identical to `GAP_BASE_SHA`.
6. Before the verifier writes a replacement, require the current `04-VERIFICATION.md` blob to equal `"$GAP_BASE_SHA":.planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md`. This protects the diagnosed input through the whole executor lifecycle while still authorizing the verifier-owned replacement now.
7. Immediately before the verdict, require four Phase 4 summaries, the committed `04-REVIEW.md`, Roadmap plan progress `4/4` with Phase 4 awaiting verification, GRAPH-04 unchecked in REQUIREMENTS, and STATE in verification. The verification-hold commit must follow the SUMMARY/progress commit. `requirements: []` on 04-04 must have caused execute-plan to skip `requirements.mark-complete`; `gap_requirements: [GRAPH-04]` is traceability only.
8. Re-run the shared-guard, source-wiring, pure-intake, HTTP zero-read/zero-task-idempotency-mutation, Phase 4 regression, generator, full-suite, drift, render, and immutable-range gates from Task 3. Verify all must-haves and `gap_requirements` against actual code and committed evidence, not SUMMARY claims alone.
9. Only after 1–8 pass may the verifier replace `04-VERIFICATION.md` with a report whose frontmatter reads `status: passed`. Execute-phase may then invoke `phase.complete`, which owns the final Phase 4 checkbox, GRAPH-04 completion, Roadmap completion, STATE advancement, and next command. If any item fails, write `status: gaps_found`; the orchestrator must persist that report with the open-phase verification hold and STATE routing to `/gsd:plan-phase 4 --gaps` before returning.
</post_execution_verifier_contract>

<threat_model>
## Trust Boundaries

ASVS baseline: Level 1. Any unresolved high-severity threat blocks Task 3 closure and the built-in phase verifier's passed verdict.

| Boundary | Description |
|----------|-------------|
| Derived projection → shared authority guard | Untrusted valid, malformed, and future-shaped graph projections must never become fresh D1 commands. |
| Shared guard → Telegram intake parser | One classifier result must survive adapter translation without a competing allow path. |
| HTTP intake → KV/D1 | Projection rejection must occur before D1 reads, proposal task/idempotency writes, or later Gate/CAS operations. |
| Rejected body → bounded receipt | A rejection may preserve a bounded receipt but never raw nodes, edges, digests, source bodies, credentials, or identity material. |
| Verification evidence → ISA/GSD completion | Local compiler tests and executor summaries cannot substitute for shared-guard, production-route, immutable-range, and built-in independent verification evidence. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-04-19 | high | Spoofing | Intent projection schema markers | mitigate | Exact plus normalized projection-family discriminator; valid/malformed tests; unrelated command negative control. |
| T-04-20 | high | Tampering | D1 authority lane | mitigate | Shared guard executes before parser compilation, KV task/idempotency writes, D1 reads, or Gate/CAS. |
| T-04-21 | medium | Repudiation | Rejection/acceptance evidence | mitigate | Atomic RED/GREEN commits, route mutation counters, bounded receipt, direct probe, pinned-range SUMMARY, and built-in independent verifier. |
| T-04-22 | high | Information disclosure | Rejection receipt/response | mitigate | Preserve current error bounding and assert no raw projection node, edge, digest, payload, credential, or source body is echoed. |
| T-04-23 | medium | Denial of service | Malformed projection input | mitigate | Pure total discriminator, bounded errors, no retrying exception path, no D1 read for rejected input. |
| T-04-24 | high | Elevation of privilege | Derived projection foldback | mitigate | Reject every recognized derived graph projection before fresh-authority acceptance; block closure on any accepted probe. |
| T-04-25 | high | Tampering | False acceptance state | mitigate | Reopen only ISC-1280, keep `requirements: []`, preserve the original gap report through executor metadata, and allow only the built-in verifier's passed branch to invoke `phase.complete`. |
| T-04-SC | low | Tampering | npm supply chain | accept | No package install, manifest change, or lockfile change; existing Node/TypeScript stack only. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Inspectable deterministic intent graph whose references preserve authority through verified learning | 1–3 | COVERED | Existing graph remains unchanged; the broken D1 authority link is made executable and re-verified. |
| REQ | GRAPH-04 | Read-only close/renew semantics cannot enter or mutate the D1 authority lane | 1–3 + built-in verifier | COVERED | Primary gap ownership via `gap_requirements`; standard `requirements` stays empty until a passed verifier lets `phase.complete` close it. |
| REQ | GRAPH-01 | Stable graph semantics | 3 | COVERED | Regression-preserved by combined Phase 4 tests and unchanged generated readbacks; not reimplemented. |
| REQ | GRAPH-02 | Complete provenance and freshness | 3 | COVERED | Regression-preserved by double generator checks and unchanged digests/readbacks; not reimplemented. |
| REQ | GRAPH-03 | Overlay references do not become authority | 3 | COVERED | Regression-preserved by combined tests and unchanged anchors/readbacks; not reimplemented. |
| REQ | GRAPH-05 | Explicit gates/freshness/stops/blocked truth | 3 | COVERED | Regression-preserved by combined tests and existing approval/CAS route suite; not reimplemented. |
| RESEARCH | — | No Phase 4 RESEARCH.md or DISCOVERY.md exists | — | EXCLUDED | Existing summaries and the independent verification report supply the implementation and gap evidence. |
| CONTEXT | — | No Phase 4 CONTEXT.md exists | — | EXCLUDED | Locked authority decisions are carried by ISA, prior plans/summaries, Roadmap, and verification. |
| VERIFICATION | Gap 1 | Shared D1 guard accepts valid/malformed Intent Graph projections and lacks production wiring | 1–3 + built-in verifier | COVERED | Exact failure becomes RED, GREEN, route proof, acceptance repair, pinned-range post-execution verification, and passed-only phase completion. |
</source_audit>

<non_goals>
- No D1 data, table, migration, schema, tenant, allowlist, or stored Goal Graph mutation.
- No provider call, deployment, Cloudflare traffic change, production Telegram send, or Hermes activation.
- No `VISION.md` or `MISSION.md` change and no root doctrine authority change.
- No generated Intent Graph node, edge, selector, digest, JSON, Markdown, or semantic change.
- No new planner, graph writer, projection authority, package, dependency, or lockfile.
- No connected-repository, vault, registry, native session, credential, `.temperance`, or external-state mutation.
- No weakening of the published D1 rejection claim without explicit owner approval.
</non_goals>

<verification>
1. Task 1 commits one semantic RED chain and the exact ISA execute 4/5 gap state.
2. Task 2 makes the shared unit, pure intake, behavior-level route, and direct D1 probes GREEN without changing D1 data/schema or handler ordering.
3. Task 3 passes the complete focused and immutable-range repository gates and restores only ISC-1280 as execution evidence while Phase 4 and GRAPH-04 remain open.
4. The executor writes and commits `04-04-SUMMARY.md`; workflow metadata records 4/4 plans, Phase 4 awaiting verification, GRAPH-04 unchecked, and STATE in verification without changing the original gap report.
5. Execute-phase's built-in independent gsd-verifier enforces `<post_execution_verifier_contract>` and may replace the report only after the executor lifecycle is complete. `passed` routes to `phase.complete`; `gaps_found` routes back to `/gsd:plan-phase 4 --gaps`.
</verification>

<success_criteria>
- Valid and malformed Intent Graph projections are rejected by the shared D1 authoritative-input guard.
- The production Goal Graph intake route proves that rejection before any D1 read or task/idempotency mutation, preserving only the bounded rejection receipt.
- Unrelated commands, normal Telegram intents, Goal Graph projections, replay, approval/CAS, and all five Phase 4 requirement behaviors remain regression-green.
- ISA and handoff truthfully repair ISC-1280 execution evidence without changing root doctrine or generated graph semantics; Roadmap and GRAPH-04 remain open through executor metadata.
- The built-in GSD verifier passes the pinned post-execution contract before `phase.complete` or Phase 5 planning begins.
</success_criteria>

<output>
Create and atomically commit `.planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md` with `GAP_BASE_SHA`, RED/GREEN/acceptance commit SHAs, direct probe outputs, focused/full command counts, zero-D1-read and zero-task/idempotency mutation evidence, permitted rejection receipt behavior, unchanged graph/source-set digests, exact bounded write set, threat disposition, and built-in verifier handoff. The next command after this planning turn is `/gsd:execute-phase 4 --gaps-only`. After execution, follow STATE: a passed built-in verifier advances through `phase.complete`; `gaps_found` routes to `/gsd:plan-phase 4 --gaps`; `/gsd:verify-work 4` is optional manual re-verification.
</output>
