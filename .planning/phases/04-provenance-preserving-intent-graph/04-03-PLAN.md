---
phase: 04-provenance-preserving-intent-graph
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - docs/architecture/README.md
  - docs/README.md
  - docs/architecture/loops-to-graphs.md
  - docs/architecture/goal-graph-operating-model.md
  - ISA.md
  - .project/HANDOFF.md
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
  - GRAPH-04
  - GRAPH-05
must_haves:
  truths:
    - "GRAPH-01 / ISC-1277: architecture discovery links the exact generated JSON, Markdown readback, contract, source model, and check command without a hand-maintained competing graph."
    - "GRAPH-02 / ISC-1278: final committed-head checks prove every projected selector digest is current and both generated files match one source model."
    - "GRAPH-03 / ISC-1279: documentation distinguishes canonical anchors, reference-only overlays, ISA/GSD authorities, the intent projection, and the D1 operational writer."
    - "GRAPH-04 / ISC-1280: documentation and final evidence prove close/produce/renew edges are observations for explicit review and never anchor or Mission mutation."
    - "GRAPH-05 / ISC-1281: final ISA evidence closes only after focused, generator, full-suite, drift, rendered-doc, privacy, and committed-range gates prove blocked work remains non-complete."
  artifacts:
    - path: docs/architecture/README.md
      provides: "Primary architecture discovery for the contract and both generated readbacks"
      contains: "intent-graph.v1.json"
    - path: docs/README.md
      provides: "Top-level documentation discovery for the read-only intent graph"
      contains: "Provenance-preserving intent graph"
    - path: docs/architecture/loops-to-graphs.md
      provides: "Explicit distinction between the cross-authority read projection and the D1 Goal Graph"
      contains: "Intent Graph"
    - path: docs/architecture/goal-graph-operating-model.md
      provides: "D1 sole-writer contract cross-linked to the separate intent projection"
      contains: "sole operational writer"
    - path: ISA.md
      provides: "Checked ISC-1277..1281 and exact Phase 4 command evidence"
      contains: "progress: 5/5"
    - path: .project/HANDOFF.md
      provides: "Bounded reviewed pickup with exact write set, evidence, held boundaries, and next GSD command"
      contains: "/gsd:verify-work 4"
  key_links:
    - from: docs/architecture/README.md
      to: docs/architecture/intent-graph.v1.json
      via: "machine-readable projection discovery"
      pattern: "intent-graph\.v1\.json"
    - from: docs/architecture/README.md
      to: docs/architecture/intent-graph.md
      via: "human readback discovery"
      pattern: "intent-graph\.md"
    - from: docs/architecture/goal-graph-operating-model.md
      to: docs/architecture/contracts/intent-graph-v1.md
      via: "explicit separate-contract reference and foldback ban"
      pattern: "intent-graph-v1\.md"
    - from: ISA.md
      to: scripts/intent-graph.test.mjs
      via: "ISC-1277..1281 focused evidence"
      pattern: "intent-graph\.test\.mjs"
    - from: ISA.md
      to: scripts/generate-intent-graph.mjs
      via: "current generated projection check evidence"
      pattern: "generate-intent-graph\.mjs --check"
---

<objective>
Make the Phase 4 projection discoverable, prove its distinction from operational and planning authorities, and close acceptance from committed evidence.

Purpose: Finish Phase 4 with an operator-visible contract and a bounded handoff, while preventing generated status, documentation, or graph language from becoming another writer.
Output: One documentation-boundary commit and one final ISA/handoff evidence commit after all release gates pass.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@.project/HANDOFF.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md
@.planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md
@ISA.md
@VISION.md
@MISSION.md
@scripts/intent-graph.mjs
@scripts/intent-graph-sources.mjs
@scripts/generate-intent-graph.mjs
@scripts/intent-graph.test.mjs
@scripts/generate-intent-graph.test.mjs
@docs/architecture/contracts/intent-graph-v1.md
@docs/architecture/intent-graph.v1.json
@docs/architecture/intent-graph.md
@docs/architecture/README.md
@docs/README.md
@docs/architecture/loops-to-graphs.md
@docs/architecture/goal-graph-operating-model.md

<interfaces>
Required distinction:
- VISION.md and MISSION.md own doctrine.
- ISA owns approved goals and acceptance.
- GSD under .planning owns finite planning state.
- D1 Goal Graph owns operational graph writes, revisions, approvals, leases, and receipts.
- cambium.intent-graph-projection.v1 is a deterministic cross-authority read projection only.

Coherent ISA Phase 4 states accepted by focused tests:
- execution state: phase execute, progress 0/5, ISC-1277..1281 unchecked.
- verified state: phase verify, progress 5/5, ISC-1277..1281 checked with focused and generated-manifest evidence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish graph discovery and the D1 authority distinction</name>
  <files>docs/architecture/README.md, docs/README.md, docs/architecture/loops-to-graphs.md, docs/architecture/goal-graph-operating-model.md</files>
  <read_first>
    - docs/architecture/intent-graph.v1.json and intent-graph.md — link exact committed artifacts; do not retype their graph tables.
    - docs/architecture/contracts/intent-graph-v1.md — reuse exact schema and authority vocabulary.
    - docs/architecture/README.md — preserve existing architecture navigation and add one bounded discovery group.
    - docs/README.md — preserve canonical anchor precedence and add the projection after doctrine/authority sources.
    - docs/architecture/loops-to-graphs.md — preserve the five-layer diagnosis and current Goal Graph ownership table.
    - docs/architecture/goal-graph-operating-model.md — preserve D1, approval, lease, receipt, and Cortex rules exactly.
    - VISION.md, MISSION.md, ISA.md, and .planning/STATE.md — keep source authority names and precedence exact.
    - workers/quests/src/goal-graph/projection-contract.ts — confirm the existing D1 read-envelope/foldback seam remains unchanged.
  </read_first>
  <action>
Add one compact Provenance-Preserving Intent Graph section to docs/architecture/README.md linking, in order, the machine JSON, human readback, v1 contract, source declaration module, generator, and exact node scripts/generate-intent-graph.mjs --check command. Add one top-level docs/README.md discovery row after the canonical Vision/Mission and ISA/GSD authority rows; label it generated, read-only, and non-authoritative.

In docs/architecture/loops-to-graphs.md, add Intent Graph as a cross-authority read projection distinct from the existing D1 Goal Graph row. State that it observes doctrine, finite planning, evidence, and learning by reference/digest; it does not select agents, create operational nodes, write status, approve work, or replace D1/ISA/GSD. In docs/architecture/goal-graph-operating-model.md, add a direct link to the intent-graph contract and state the converse distinction: D1 remains sole operational writer; neither valid nor malformed intent projections can enter its authoritative command lane; evidence/learning can inform a separately approved intent only through the existing Gate/CAS path.

Do not copy generated tables, anchor prose, current node counts, digests, or state into these hand-maintained docs. Do not edit Worker/runtime types, routes, UI, D1/KV/schema, package files, deployment, providers, .temperance, or connected repositories. Run focused tests, generator --check, rendered-doc check, and a documentation scan that proves the four files contain direct links but no copied root Just Cause or Current pursuit paragraph.
  </action>
  <acceptance_criteria>
    - Both indexes expose the machine readback, human readback, contract, source model, and --check command with generated/read-only labeling.
    - loops-to-graphs.md names Intent Graph and D1 Goal Graph separately and retains D1 as operational writer.
    - goal-graph-operating-model.md links the intent contract and rejects intent projection foldback into the D1 command lane.
    - None of the four files copies a complete VISION.md or MISSION.md paragraph, graph table, node count, graph digest, or current status.
    - Focused graph tests, generator --check, and rendered-doc check pass before commit.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; npm run render-docs:check &amp;&amp; rg -q "intent-graph\.v1\.json" docs/architecture/README.md docs/README.md &amp;&amp; rg -q "intent-graph-v1\.md" docs/architecture/README.md docs/architecture/goal-graph-operating-model.md &amp;&amp; rg -q "D1 Goal Graph" docs/architecture/loops-to-graphs.md docs/architecture/goal-graph-operating-model.md</automated>
  </verify>
  <commit>Stage only docs/architecture/README.md, docs/README.md, docs/architecture/loops-to-graphs.md, and docs/architecture/goal-graph-operating-model.md and commit docs(04-03): publish intent graph authority map.</commit>
  <done>Operators can discover both readbacks and cannot confuse the intent projection with doctrine, planning, acceptance, or D1 operational authority.</done>
</task>

<task type="auto">
  <name>Task 2: Close Phase 4 acceptance from full committed evidence</name>
  <files>ISA.md, .project/HANDOFF.md</files>
  <read_first>
    - ISA.md — preserve the coordinator-owned Phase 4 decision, exact active goal, historical criteria/checkboxes, Test Strategy, and Feature; change only the active Phase 4 state/evidence after gates.
    - .planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md and 04-02-SUMMARY.md — use actual commit SHAs, graph digests, counts, and command evidence.
    - docs/architecture/intent-graph.v1.json and intent-graph.md — confirm parity and current source/graph digests.
    - scripts/intent-graph.test.mjs and generate-intent-graph.test.mjs — map exact named tests to ISC-1277..1281.
    - .project/HANDOFF.md — append one bounded newest checkpoint without rewriting prior evidence.
    - .planning/STATE.md — record the exact next GSD command in handoff; do not edit GSD state from this task.
  </read_first>
  <action>
From the committed Task 1 head, run the focused graph tests, node scripts/generate-intent-graph.mjs --check twice, node --test scripts/infinite-game-anchors.test.mjs, npm test, npm run drift:audit, npm run render-docs:check, git diff --check, the generated JSON/Markdown parity probe, an added-line sensitive-material scan, and a forbidden-path commit-range check. The check range starts at git merge-base HEAD origin/main and must exclude Worker/runtime source, D1/KV migrations or schemas, Telegram, UI, packages/lockfiles, .temperance, deployment/provider configuration, and connected repositories.

Only after every pre-closure gate exits zero, edit ISA.md: retain the exact task/Goal and Phase 4 iteration; change phase to verify and progress to 5/5; set the real updated timestamp; check only ISC-1277 through ISC-1281; and append one evidence line per criterion naming its exact focused test behavior, node scripts/generate-intent-graph.mjs --check, graph/source-set digest readback where applicable, and the full gate result. Do not alter any earlier ISC checkbox or rewrite the coordinator's refined Phase 4 decision.

Append a dated .project/HANDOFF.md checkpoint naming the bounded Phase 4 write set, both generated artifact paths and current digests, exact focused/full gate results, D1/ISA/GSD/root-anchor authority boundaries, absence of deployment/provider/runtime/external mutations, and next command /gsd:verify-work 4. Then rerun focused graph tests, generator --check twice, deterministic ISA readback for the coherent verified state, rendered-doc check, diff check, sensitive scan, and forbidden-path range check. Commit ISA.md and .project/HANDOFF.md together only after the post-evidence gates pass. Run the post-commit verification below against committed HEAD; an uncommitted diff is not completion evidence.
  </action>
  <pre_closure_verify>
    1. `node --test scripts/infinite-game-anchors.test.mjs scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; npm test &amp;&amp; npm run drift:audit &amp;&amp; npm run render-docs:check &amp;&amp; git diff --check`.
    2. Normalized copied-anchor paragraph rejection: `node -e 'const fs=require("node:fs"); const norm=s=&gt;s.replace(/\r\n?/g,"\n").replace(/\s+/g," ").trim(); const paras=["VISION.md","MISSION.md"].flatMap(f=&gt;fs.readFileSync(f,"utf8").split(/\n\s*\n/).map(norm).filter(p=&gt;p.length&gt;=80&amp;&amp;!p.startsWith("#"))); const targets=["docs/architecture/intent-graph.v1.json","docs/architecture/intent-graph.md","docs/architecture/contracts/intent-graph-v1.md","docs/architecture/README.md","docs/README.md","docs/architecture/loops-to-graphs.md","docs/architecture/goal-graph-operating-model.md"]; for(const f of targets){const body=norm(fs.readFileSync(f,"utf8")); for(const p of paras){if(body.includes(p)){console.error(`${f} copies canonical anchor paragraph: ${p.slice(0,80)}`);process.exit(1)}}}'`.
    3. Added-line-only sensitive scan: `bash -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); added=$(mktemp); trap '\''rm -f "$added"'\'' EXIT; git diff --unified=0 "$base" -- ISA.md .project/HANDOFF.md scripts/infinite-game-anchors.test.mjs scripts/intent-graph.test.mjs scripts/intent-graph.mjs scripts/generate-intent-graph.test.mjs scripts/intent-graph-sources.mjs scripts/generate-intent-graph.mjs docs/architecture/contracts/intent-graph-v1.md docs/architecture/intent-graph.v1.json docs/architecture/intent-graph.md docs/architecture/README.md docs/README.md docs/architecture/loops-to-graphs.md docs/architecture/goal-graph-operating-model.md .planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md .planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md .planning/phases/04-provenance-preserving-intent-graph/04-03-SUMMARY.md | sed -n "s/^+//p" &gt;"$added"; if rg -n "(/U[s]ers/|/V[o]lumes/|/pr[i]vate/tmp/|AKIA[0-9A-Z]{16}|gh[pousr]_[A-Za-z0-9]{20,}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|(?:api[_-]?key|access[_-]?token|client[_-]?secret|password)[[:space:]]*[:=][[:space:]]*[^[:space:]]+|native[_-]?session|prompt[_-]?body|response[_-]?body)" "$added"; then exit 1; fi'`.
    4. Exact allowed-path membership: `node -e 'const cp=require("node:child_process"); const base=cp.execFileSync("git",["merge-base","HEAD","origin/main"],{encoding:"utf8"}).trim(); const actual=cp.execFileSync("git",["diff","--name-only",base],{encoding:"utf8"}).trim().split("\n").filter(Boolean); const allowed=new Set([".planning/ROADMAP.md",".planning/STATE.md",".planning/phases/04-provenance-preserving-intent-graph/04-01-PLAN.md",".planning/phases/04-provenance-preserving-intent-graph/04-02-PLAN.md",".planning/phases/04-provenance-preserving-intent-graph/04-03-PLAN.md",".planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md",".planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md",".planning/phases/04-provenance-preserving-intent-graph/04-03-SUMMARY.md","ISA.md",".project/HANDOFF.md","scripts/infinite-game-anchors.test.mjs","scripts/intent-graph.test.mjs","scripts/intent-graph.mjs","scripts/generate-intent-graph.test.mjs","scripts/intent-graph-sources.mjs","scripts/generate-intent-graph.mjs","docs/architecture/contracts/intent-graph-v1.md","docs/architecture/intent-graph.v1.json","docs/architecture/intent-graph.md","docs/architecture/README.md","docs/README.md","docs/architecture/loops-to-graphs.md","docs/architecture/goal-graph-operating-model.md"]); const unexpected=actual.filter(p=&gt;!allowed.has(p)); if(unexpected.length){console.error(unexpected.join("\n"));process.exit(1)}'`.
    5. Deletion/rename rejection: `bash -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); test -z "$(git diff --name-status --diff-filter=DR "$base")"'`.
    6. Forbidden-category rejection: `bash -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); if git diff --name-only "$base" | rg "^(workers/|apps/|bin/|shared/|migrations/|\.github/workflows/|\.temperance/|wrangler|package(-lock)?\.json$|pnpm-lock\.yaml$|yarn\.lock$|.*(?:cloudflare|telegram|provider|deployment).*(?:json|toml|yaml|yml|ts|js|mjs)$)"; then exit 1; fi'`.
  </pre_closure_verify>
  <acceptance_criteria>
    - All pre-closure focused, generator, anchor, full-suite, drift, rendered-doc, parity, diff, privacy, and forbidden-path gates pass from committed Task 1 head.
    - ISA ends phase verify and progress 5/5 with exactly ISC-1277..1281 newly checked; every earlier checkbox is unchanged.
    - Each Phase 4 ISC names exact test/check evidence and no criterion is checked from prose or generated status alone.
    - Handoff records exact bounded artifacts/digests, held authorities, zero external/runtime mutation, and /gsd:verify-work 4.
    - Post-evidence and post-commit focused, generator, ISA, docs, privacy, and range checks pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; node -e 'const fs=require("node:fs"); const s=fs.readFileSync("ISA.md","utf8"); const fm=(s.match(/^---\n([\s\S]*?)\n---/)||[])[1]||""; const checks=[/^phase: verify$/m.test(fm),/^progress: 5\/5$/m.test(fm),/^iteration: 2026-08-18-infinite-game-doctrine-and-intent-graph$/m.test(fm),...Array.from({length:5},(_,i)=&gt;new RegExp(`- \\[x\\] ISC-${1277+i}:`).test(s)),/ISC-1277\.\.1281[\s\S]*intent graph/i.test(s),/generate-intent-graph\.mjs --check/.test(s)]; if(checks.some(v=&gt;!v)) process.exit(1)' &amp;&amp; npm run render-docs:check &amp;&amp; git diff --check</automated>
  </verify>
  <commit>Stage only ISA.md and .project/HANDOFF.md and commit docs(04-03): close intent graph acceptance evidence.</commit>
  <post_commit_verify>
    1. `node --test scripts/infinite-game-anchors.test.mjs scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; npm test &amp;&amp; npm run drift:audit &amp;&amp; npm run render-docs:check &amp;&amp; git diff --check` passes from committed HEAD.
    2. The deterministic ISA readback in verify passes and exactly ISC-1277..1281 are the newly checked range relative to origin/main.
    3. npm test, npm run drift:audit, npm run render-docs:check, and git diff --check all pass.
    4. Parse docs/architecture/intent-graph.v1.json; prove Markdown contains identical schema, graphDigest, sourceSetDigest, all node IDs, all edge IDs, and no node has state.completion satisfied while state.blockedReason is non-empty.
    5. Normalized copied-anchor paragraph rejection at committed HEAD: `node -e 'const fs=require("node:fs"); const norm=s=&gt;s.replace(/\r\n?/g,"\n").replace(/\s+/g," ").trim(); const paras=["VISION.md","MISSION.md"].flatMap(f=&gt;fs.readFileSync(f,"utf8").split(/\n\s*\n/).map(norm).filter(p=&gt;p.length&gt;=80&amp;&amp;!p.startsWith("#"))); const targets=["docs/architecture/intent-graph.v1.json","docs/architecture/intent-graph.md","docs/architecture/contracts/intent-graph-v1.md","docs/architecture/README.md","docs/README.md","docs/architecture/loops-to-graphs.md","docs/architecture/goal-graph-operating-model.md"]; for(const f of targets){const body=norm(fs.readFileSync(f,"utf8")); for(const p of paras){if(body.includes(p)){console.error(`${f} copies canonical anchor paragraph: ${p.slice(0,80)}`);process.exit(1)}}}'`.
    6. Added-line-only committed sensitive scan: `bash -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); added=$(mktemp); trap '\''rm -f "$added"'\'' EXIT; git diff --unified=0 "$base"..HEAD -- ISA.md .project/HANDOFF.md scripts/infinite-game-anchors.test.mjs scripts/intent-graph.test.mjs scripts/intent-graph.mjs scripts/generate-intent-graph.test.mjs scripts/intent-graph-sources.mjs scripts/generate-intent-graph.mjs docs/architecture/contracts/intent-graph-v1.md docs/architecture/intent-graph.v1.json docs/architecture/intent-graph.md docs/architecture/README.md docs/README.md docs/architecture/loops-to-graphs.md docs/architecture/goal-graph-operating-model.md .planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md .planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md .planning/phases/04-provenance-preserving-intent-graph/04-03-SUMMARY.md | sed -n "s/^+//p" &gt;"$added"; if rg -n "(/U[s]ers/|/V[o]lumes/|/pr[i]vate/tmp/|AKIA[0-9A-Z]{16}|gh[pousr]_[A-Za-z0-9]{20,}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|(?:api[_-]?key|access[_-]?token|client[_-]?secret|password)[[:space:]]*[:=][[:space:]]*[^[:space:]]+|native[_-]?session|prompt[_-]?body|response[_-]?body)" "$added"; then exit 1; fi'`.
    7. Exact committed allowed-path membership: `node -e 'const cp=require("node:child_process"); const base=cp.execFileSync("git",["merge-base","HEAD","origin/main"],{encoding:"utf8"}).trim(); const actual=cp.execFileSync("git",["diff","--name-only",`${base}..HEAD`],{encoding:"utf8"}).trim().split("\n").filter(Boolean); const allowed=new Set([".planning/ROADMAP.md",".planning/STATE.md",".planning/phases/04-provenance-preserving-intent-graph/04-01-PLAN.md",".planning/phases/04-provenance-preserving-intent-graph/04-02-PLAN.md",".planning/phases/04-provenance-preserving-intent-graph/04-03-PLAN.md",".planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md",".planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md",".planning/phases/04-provenance-preserving-intent-graph/04-03-SUMMARY.md","ISA.md",".project/HANDOFF.md","scripts/infinite-game-anchors.test.mjs","scripts/intent-graph.test.mjs","scripts/intent-graph.mjs","scripts/generate-intent-graph.test.mjs","scripts/intent-graph-sources.mjs","scripts/generate-intent-graph.mjs","docs/architecture/contracts/intent-graph-v1.md","docs/architecture/intent-graph.v1.json","docs/architecture/intent-graph.md","docs/architecture/README.md","docs/README.md","docs/architecture/loops-to-graphs.md","docs/architecture/goal-graph-operating-model.md"]); const unexpected=actual.filter(p=&gt;!allowed.has(p)); if(unexpected.length){console.error(unexpected.join("\n"));process.exit(1)}'`.
    8. Committed deletion/rename rejection: `bash -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); test -z "$(git diff --name-status --diff-filter=DR "$base"..HEAD)"'`.
    9. Committed forbidden-category rejection: `bash -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); if git diff --name-only "$base"..HEAD | rg "^(workers/|apps/|bin/|shared/|migrations/|\.github/workflows/|\.temperance/|wrangler|package(-lock)?\.json$|pnpm-lock\.yaml$|yarn\.lock$|.*(?:cloudflare|telegram|provider|deployment).*(?:json|toml|yaml|yml|ts|js|mjs)$)"; then exit 1; fi'`.
    10. `base=$(git merge-base HEAD origin/main); git log --format=%s "$base"..HEAD` contains the coherent-state/RED commit, compiler GREEN, generator RED, generator GREEN, authority-map docs, and ISA/handoff closure in dependency order.
  </post_commit_verify>
  <done>All five Phase 4 ISCs are checked from committed deterministic evidence and the bounded continuation is ready for independent GSD verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated projection → documentation | Discovery may link contract/readbacks but cannot copy status or confer authority. |
| Intent projection → D1 Goal Graph | No valid or malformed intent projection may enter the operational command lane. |
| Verification output → ISA completion | Only exact committed command evidence may change active acceptance state. |
| Local checkout → handoff/commit | Machine paths, secrets, prompt/session material, private vault data, and external mutations are forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Spoofing | Docs discovery authority labels | mitigate | Exact root/ISA/GSD/D1/projection role map and direct links. |
| T-04-14 | Tampering | Generated readbacks after docs/ISA edits | mitigate | --check twice before and after evidence, committed-head parity proof. |
| T-04-15 | Repudiation | ISA and handoff completion claim | mitigate | One line per ISC with command/digest evidence plus atomic closure commit. |
| T-04-16 | Information disclosure | ISA/handoff/docs additions | mitigate | Added-line sensitive scan and repository-relative path contract. |
| T-04-17 | Denial of service | Stale or contradictory operator readback | mitigate | Generator check, full suite, drift and rendered-doc gates before closure. |
| T-04-18 | Elevation of privilege | Documentation/generated graph becoming planner or writer | mitigate | Explicit non-authority labels, foldback ban, forbidden runtime/D1 range gate. |
| T-04-SC | Tampering | npm supply chain | accept | Existing lockfiles only; no install, manifest, or dependency changes. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Inspectable deterministic graph preserving authority through verified learning | 1–2 | COVERED | Discovery plus committed acceptance evidence. |
| REQ | GRAPH-01 | Stable graph and operator discovery | 1–2 | COVERED | Links, focused test, final parity. |
| REQ | GRAPH-02 | Provenance/digest freshness | 2 | COVERED | Double --check and committed readback. |
| REQ | GRAPH-03 | Overlay and authority distinction | 1–2 | COVERED | Architecture map and final tests. |
| REQ | GRAPH-04 | Read-only close/renew semantics | 1–2 | COVERED | D1 distinction plus exact evidence. |
| REQ | GRAPH-05 | Explicit gates/freshness/stops/blocked truth | 2 | COVERED | Final parity and blocked-not-complete probe. |
| RESEARCH | — | No RESEARCH.md exists; research disabled | — | EXCLUDED | Completed Plan 04-01/02 summaries provide implementation evidence. |
| CONTEXT | — | No CONTEXT.md exists | — | EXCLUDED | User authorized direct planning from canonical sources. |
</source_audit>

<verification>
Task 2 post_commit_verify is the phase-level gate. It proves all six implementation commits, current generated artifacts, exact ISA closure, bounded files, privacy, non-destructive behavior, and preserved runtime/external authority before independent verification.
</verification>

<success_criteria>
- Both generated readbacks and their contract are discoverable without copied graph truth.
- D1 operational graph, ISA acceptance, GSD planning, root doctrine, and the intent projection remain distinct.
- ISC-1277 through ISC-1281 close only from exact committed evidence.
- Handoff names /gsd:verify-work 4; no implementation, deployment, or connected-repository mutation is implied beyond this phase.
</success_criteria>

<output>
Create .planning/phases/04-provenance-preserving-intent-graph/04-03-SUMMARY.md with both Plan 04-03 commit SHAs, pre/post-closure command results, exact graph/source-set digests, ISA readback, bounded file list, privacy/forbidden-path proof, preserved D1/ISA/GSD/anchor boundaries, and next command $gsd-verify-work 4.
</output>
