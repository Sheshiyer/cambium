---
phase: 04-provenance-preserving-intent-graph
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/generate-intent-graph.test.mjs
  - scripts/intent-graph-sources.mjs
  - scripts/generate-intent-graph.mjs
  - docs/architecture/intent-graph.v1.json
  - docs/architecture/intent-graph.md
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
  - GRAPH-04
  - GRAPH-05
must_haves:
  truths:
    - "GRAPH-01 / ISC-1277: one declared repository source model compiles the complete Vision → Mission → goal → task → evidence → learning chain and identical inputs produce byte-identical JSON and Markdown."
    - "GRAPH-02 / ISC-1278: the generated projection records and rechecks exact canonical digests for every declared source; --check fails on stale output, missing sources, and digest drift."
    - "GRAPH-03 / ISC-1279: PROJECT.md and .planning/PROJECT.md appear only as overlay reference nodes linked to root anchors, never as copied Vision or Mission authorities."
    - "GRAPH-04 / ISC-1280: completed Phase 3 evidence closes its finite goal and produces a verified learning signal; that signal informs one uniquely selected reviewed ISA gate, and only the gate renews the next finite goal."
    - "GRAPH-05 / ISC-1281: generated JSON and Markdown expose identical gate, freshness, stop, blocked, and completion facts, with no blocked node rendered complete."
  artifacts:
    - path: scripts/generate-intent-graph.test.mjs
      provides: "End-to-end generator, stale-output, parity, and source-preservation tests"
      contains: "--check"
    - path: scripts/intent-graph-sources.mjs
      provides: "Single repository source model consumed by both generated readbacks"
      exports:
        - buildIntentGraphSources
    - path: scripts/generate-intent-graph.mjs
      provides: "Deterministic --write, --check, and --json CLI"
      contains: "intent-graph.v1.json"
    - path: docs/architecture/intent-graph.v1.json
      provides: "Machine-readable read-only Phase 4 projection"
      contains: "cambium.intent-graph-projection.v1"
    - path: docs/architecture/intent-graph.md
      provides: "Human-readable readback rendered from the same compiled object"
      contains: "Read-only projection"
  key_links:
    - from: scripts/intent-graph-sources.mjs
      to: VISION.md
      via: "vision node source and canonical digest"
      pattern: "VISION\.md"
    - from: scripts/intent-graph-sources.mjs
      to: MISSION.md
      via: "mission node source and canonical digest"
      pattern: "MISSION\.md"
    - from: scripts/intent-graph-sources.mjs
      to: .planning/phases/03-canonical-infinite-game-anchors/03-VERIFICATION.md
      via: "receipt-backed Phase 3 evidence node and closes edge"
      pattern: "03-VERIFICATION\.md"
    - from: scripts/intent-graph-sources.mjs
      to: ISA.md
      via: "approved-goal frontmatter task node plus uniquely selected reviewed Phase 4 gate; execution-state frontmatter is excluded"
      pattern: "frontmatter"
    - from: scripts/generate-intent-graph.mjs
      to: scripts/intent-graph.mjs
      via: "compile once, serialize JSON and render Markdown from that same object"
      pattern: "compileIntentGraph"
    - from: docs/architecture/intent-graph.md
      to: docs/architecture/intent-graph.v1.json
      via: "matching schema, graph digest, source-set digest, node IDs, and edge IDs"
      pattern: "graph digest"
---

<objective>
Compile the verified Phase 4 repository source model into byte-stable machine and human readbacks.

Purpose: Let an operator inspect the exact authority chain, provenance, lifecycle, transitions, gates, freshness, stops, and blockers without reading a copied doctrine body or trusting a hand-maintained diagram.
Output: A committed RED generator contract, one source declaration module, a deterministic CLI, and matching JSON/Markdown projections.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@.project/HANDOFF.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-canonical-infinite-game-anchors/03-01-PLAN.md
@.planning/phases/03-canonical-infinite-game-anchors/03-02-PLAN.md
@.planning/phases/03-canonical-infinite-game-anchors/03-VERIFICATION.md
@.planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md
@.planning/phases/04-provenance-preserving-intent-graph/04-01-PLAN.md
@.planning/phases/04-provenance-preserving-intent-graph/04-02-PLAN.md
@.planning/phases/04-provenance-preserving-intent-graph/04-03-PLAN.md
@ISA.md
@VISION.md
@MISSION.md
@scripts/intent-graph.mjs
@docs/architecture/contracts/intent-graph-v1.md
@docs/architecture/goal-graph-operating-model.md

<interfaces>
Actual graph branches:
- VISION.md directs MISSION.md.
- MISSION.md scopes the approved milestone goal selected only from ISA frontmatter task plus the completed Phase 3 and active Phase 4 goals selected from the exact current **Goal**: field spelling inside their exact .planning/ROADMAP.md Phase headings.
- The Phase 3 goal decomposes into 03-01-PLAN.md and 03-02-PLAN.md; the Phase 4 goal decomposes into 04-01-PLAN.md, 04-02-PLAN.md, and 04-03-PLAN.md.
- Phase 3 plan tasks point through proves edges to the exact Probe Execution subsection of 03-VERIFICATION.md; that evidence closes only the Phase 3 finite goal and produces a learning node selected from the exact Gaps Summary subsection.
- The Phase 3 learning node informs one gate selected by the exact unique prefix of the 2026-08-18 06:25 refined Phase 4 ISA decision. Learning has verification_evidence authority only; the ISA gate retains isa_acceptance authority and alone points through renews to the finite Phase 4 goal.
- PROJECT.md and .planning/PROJECT.md are overlay nodes with references edges to root VISION.md and MISSION.md only.
- .planning/STATE.md, ROADMAP plan checkbox/status fields, ISA phase/progress/updated fields, and both generated artifacts are excluded from source selection. They may be read by the execution workflow but never become intent nodes or digest inputs.

Generated output paths:
- docs/architecture/intent-graph.v1.json
- docs/architecture/intent-graph.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit the RED generator and readback-parity contract</name>
  <files>scripts/generate-intent-graph.test.mjs</files>
  <read_first>
    - .planning/phases/04-provenance-preserving-intent-graph/04-01-SUMMARY.md — use actual compiler exports and commit boundaries.
    - scripts/intent-graph.mjs and scripts/intent-graph.test.mjs — reuse only the committed public compiler/renderer contract.
    - VISION.md, MISSION.md, ISA.md, and .planning/ROADMAP.md — establish exact anchor, frontmatter task, reviewed-decision, and Phase Goal-field selectors; explicitly exclude execution tracking fields.
    - .planning/phases/03-canonical-infinite-game-anchors/03-01-PLAN.md, 03-02-PLAN.md, and 03-VERIFICATION.md — establish the completed finite-goal/task/evidence branch.
    - .planning/phases/03-canonical-infinite-game-anchors/03-VERIFICATION.md — select Probe Execution as evidence and Gaps Summary as verified Phase 3 learning; do not describe .planning/RETROSPECTIVE.md as Phase 3 because it is v0.3.
    - PROJECT.md and .planning/PROJECT.md — establish overlay-reference nodes without copying anchor content.
    - docs/architecture/goal-graph-operating-model.md — keep the D1 Goal Graph as a named sole writer boundary, not a generated intent-graph source of commands.
  </read_first>
  <behavior>
    - Actual repository compilation includes the exact node/edge branches listed in interfaces and every node has a fresh source digest.
    - Two writes from identical sources are byte-identical; --check succeeds only when both committed files exactly match current compilation.
    - Changed content selected by a declared node selector, stale generated JSON, stale Markdown, missing source, or inconsistent recorded digest makes --check exit non-zero with the affected repository-relative path and selector.
    - JSON and Markdown carry equal schema, graphDigest, sourceSetDigest, node IDs, edge IDs, and state facts.
    - Generated outputs contain references, selectors, lifecycle/state, and digests but contain no source body, root-anchor excerpt, machine-local absolute path, prompt/session body, credential, or native session identifier.
    - Unrelated ROADMAP plan checkbox/status changes and ISA phase/progress/updated changes leave selected goal IDs/digests and outputs unchanged; changing a selected Phase Goal field or ISA task makes --check stale.
    - Generated source-path/selector tuples contain no .planning/STATE.md, ROADMAP tracking field, ISA phase/progress/updated field, generated JSON, or generated Markdown entry.
    - Generation leaves VISION.md, MISSION.md, ISA.md, .planning/ROADMAP.md, and every declared source byte-identical.
  </behavior>
  <action>
Create scripts/generate-intent-graph.test.mjs using Node built-ins and subprocess execution. Guard not-yet-present modules and outputs so RED fails on a named generator/readback assertion, not module resolution or missing-file exceptions. Test the real repository source branch plus a temporary-root fixture accepted through --root and explicit output arguments. Prove --write idempotence, --check success, selected-source and output-drift failure, missing-source failure, JSON/Markdown parity, read-only source preservation, forbidden-content absence, and projection-foldback rejection. Simulate ROADMAP plan-checkbox/status and ISA phase/progress/updated mutations and prove identical selected goal IDs/digests; then mutate the exact Phase Goal field and ISA task and prove staleness. Assert every generated source tuple excludes STATE, tracking fields, and generated outputs. Capture all declared source bytes before and after both CLI modes. Run RED, stage only the test, and commit before Task 2.
  </action>
  <acceptance_criteria>
    - The test names actual-source, idempotence, stale-source/output, parity, privacy/path, and source-preservation cases.
    - RED exits non-zero for a named generator assertion and excludes SyntaxError, ERR_MODULE_NOT_FOUND, ENOENT, and no such file or directory.
    - Temporary-root tests write only beneath the temporary directory and clean it after each test.
    - git status --short -- scripts/generate-intent-graph.test.mjs prints exactly ?? scripts/generate-intent-graph.test.mjs before commit.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; out=$(mktemp); trap '\''rm -f "$out"'\'' EXIT; test "$(git status --short -- scripts/generate-intent-graph.test.mjs)" = "?? scripts/generate-intent-graph.test.mjs"; if node --test scripts/generate-intent-graph.test.mjs &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; rg -q "generator|readback|stale|parity|source" "$out"; if rg -q "SyntaxError|ERR_MODULE_NOT_FOUND|ENOENT|no such file or directory" "$out"; then cat "$out"; exit 1; fi'</automated>
  </verify>
  <commit>Stage only scripts/generate-intent-graph.test.mjs and commit test(04-02): add failing intent graph generator contract.</commit>
  <done>The committed RED boundary proves both outputs must derive from one source model and stay current.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate the canonical repository projection and matching readback</name>
  <files>scripts/intent-graph-sources.mjs, scripts/generate-intent-graph.mjs, docs/architecture/intent-graph.v1.json, docs/architecture/intent-graph.md</files>
  <read_first>
    - scripts/generate-intent-graph.test.mjs — satisfy every committed integration contract without weakening assertions.
    - scripts/intent-graph.mjs — call the public compiler and renderer; do not fork canonicalization, digest, validation, or Markdown logic.
    - .planning/ROADMAP.md — use only the exact bold Goal field within the exact Phase 3 or Phase 4 heading; never select plan checkboxes, Plans, Success Criteria, progress rows, or status prose.
    - ISA.md — use only frontmatter task for the approved goal and the uniquely prefixed 2026-08-18 06:25 refined Phase 4 decision for the gate; never select phase, progress, updated, criteria checkboxes, or historical Goal content.
    - .planning/phases/03-canonical-infinite-game-anchors/03-VERIFICATION.md — use exact Probe Execution evidence and Gaps Summary learning subsections; do not mislabel the v0.3 retrospective as Phase 3 learning.
    - PROJECT.md and .planning/PROJECT.md — declare overlay references with derived_reference authority only.
    - VISION.md, MISSION.md, and ISA.md — preserve canonical doctrine and acceptance boundaries; read only.
    - docs/architecture/contracts/intent-graph-v1.md — keep generated fields within the committed schema.
  </read_first>
  <action>
Create scripts/intent-graph-sources.mjs exporting buildIntentGraphSources(repositoryRoot). Declare the exact nodes and edges in interfaces with stable keys, repository-relative paths, closed selectors, the exact source-authority/lifecycle vocabularies, complete state objects, and only matrix-valid relations. Use exact Markdown-section selectors for Vision/Mission/overlays; exact Phase heading plus bold Goal-field selectors for Roadmap goals; exact XML task-name selectors for plan tasks; exact Probe Execution and Gaps Summary selectors in Phase 3 verification; ISA frontmatter task for the approved goal; and the uniquely prefixed reviewed Phase 4 ISA decision for the gate. Represent complete Phase 3 tasks/evidence/learning separately from pending Phase 4 tasks. Each Phase 3 task points through proves to evidence; evidence closes only the finite Phase 3 goal and produces verified learning; learning informs the ISA gate without acquiring ISA authority; and the gate alone renews the finite Phase 4 goal. Represent PROJECT.md and .planning/PROJECT.md as derived_reference overlays using references edges to exact root anchors; store no anchor prose. Give the reviewed ISA gate completion blocked, approval required, freshness fresh, a non-empty blockedReason, and approval_boundary stop condition until explicit next-wave execution approval is recorded outside this generated source model. Keep D1 Goal Graph in an authority-boundary registry as sole operational writer and never as an intent projection writer.

Hard-reject .planning/STATE.md, both generated output paths, any ROADMAP selector other than an exact Phase Goal field, and any ISA selector other than frontmatter task or the exact reviewed Phase 4 decision. This exclusion is a plan-level invariant, not an optional source-model convention.

Create scripts/generate-intent-graph.mjs with --root, --json-output, and --markdown-output path options plus mutually exclusive --write, --check, and --json modes. Defaults must resolve from the script location, never process.cwd alone. Compile the source model exactly once per invocation, serialize the validated object as two-space JSON plus one terminal LF, and pass that same object to renderIntentGraphMarkdown. --write must use contained temporary siblings plus atomic rename and refuse outputs outside the real repository root. --check must perform zero writes, byte-compare both expected outputs, report each stale/missing file, and return non-zero. --json writes only the compiled JSON to stdout and no status prose.

Run --write to create docs/architecture/intent-graph.v1.json and docs/architecture/intent-graph.md. Then run --check twice, the focused compiler/generator tests, selector-mutation simulations, an excluded-source assertion, and a parity probe that parses JSON and extracts the Markdown digest/ID tables. Confirm git diff names only the four Task 2 files and that VISION.md, MISSION.md, ISA.md, .planning/ROADMAP.md, and .planning/STATE.md have no Task 2 diff before committing.
  </action>
  <acceptance_criteria>
    - The source model contains the exact complete/pending branches, overlay references, verified evidence/learning chain, uniquely selected ISA gate, and matrix-valid transitions in interfaces.
    - Actual node sources use only Roadmap Phase Goal fields and ISA task/unique gate decision; excluded tracking and generated paths are absent.
    - Both outputs have identical schema, graph/source-set digests, complete sorted node/edge ID sets, and explicit state facts.
    - --check is read-only, passes twice on current outputs, and fails after any temporary source or output drift in tests.
    - JSON/Markdown include no doctrine body, absolute path, prompt/session content, secret-shaped material, or independent authority claim.
    - D1 Goal Graph is named only as sole operational writer and the intent graph remains projectionAuthority read_only.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/generate-intent-graph.mjs --check &amp;&amp; node scripts/generate-intent-graph.mjs --check &amp;&amp; node --test scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs &amp;&amp; node -e 'const fs=require("node:fs"); const j=JSON.parse(fs.readFileSync("docs/architecture/intent-graph.v1.json","utf8")); const m=fs.readFileSync("docs/architecture/intent-graph.md","utf8"); if(j.schema!=="cambium.intent-graph-projection.v1"||j.projectionAuthority!=="read_only") process.exit(1); for(const value of [j.graphDigest,j.sourceSetDigest,...j.nodes.map(n=&gt;n.id),...j.edges.map(e=&gt;e.id)]) if(!m.includes(value)) process.exit(1); const sources=j.nodes.map(n=&gt;`${n.source.path}#${n.source.selector}`); if(sources.some(s=&gt;s.startsWith(".planning/STATE.md#")||s.includes("intent-graph.v1.json")||s.includes("intent-graph.md")||/ROADMAP\.md#.*(Plans|checkbox|status)/i.test(s)||/ISA\.md#frontmatter\.(phase|progress|updated)/.test(s))) process.exit(1)' &amp;&amp; test -z "$(git diff --name-only -- VISION.md MISSION.md ISA.md .planning/ROADMAP.md .planning/STATE.md)"</automated>
  </verify>
  <commit>Stage only scripts/intent-graph-sources.mjs, scripts/generate-intent-graph.mjs, docs/architecture/intent-graph.v1.json, and docs/architecture/intent-graph.md and commit feat(04-02): generate provenance intent graph readbacks.</commit>
  <done>Machine and human projections are committed, deterministic, current, source-preserving, and derived from one source declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical source model → compiler | Curated selectors and lifecycle claims must resolve to current source bytes and closed authorities. |
| Compiler object → JSON/Markdown | Both outputs must serialize the same validated object without hand-maintained divergence. |
| Repository source → generated output | Source drift must stale outputs; generation must never mutate sources. |
| CLI options → filesystem | Root/output arguments are untrusted and must remain inside the real repository root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | Overlay and D1 authority registry | mitigate | Exact closed authority names, reference-only overlay edges, D1 sole-writer assertion. |
| T-04-08 | Tampering | Generated JSON/Markdown freshness | mitigate | Recompile-and-byte-compare --check, graph/source digests, two consecutive checks. |
| T-04-09 | Repudiation | Close/produce/inform/renew transitions | mitigate | Exact Phase 3 verification subsections, uniquely selected reviewed ISA gate, direction matrix, graph digest. |
| T-04-10 | Information disclosure | Generated paths/content | mitigate | Relative paths, no source bodies, sensitive-material tests and final added-line scan. |
| T-04-11 | Denial of service | CLI output destinations | mitigate | realpath containment, finite declared source set, atomic sibling rename. |
| T-04-12 | Elevation of privilege | Generated graph as writer/planner | mitigate | projectionAuthority read_only, authority registry, foldback rejection, no mutation APIs. |
| T-04-SC | Tampering | npm supply chain | accept | No dependency, manifest, or lockfile change; Node built-ins only. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Operator-inspectable deterministic graph | 1–2 | COVERED | Generated JSON and Markdown parity. |
| REQ | GRAPH-01 | Complete stable semantic chain | 1–2 | COVERED | Actual source branch plus idempotence tests. |
| REQ | GRAPH-02 | Provenance and stale detection | 1–2 | COVERED | Source digests and --check. |
| REQ | GRAPH-03 | Inherited overlays reference anchors | 1–2 | COVERED | Two reference-only overlay nodes. |
| REQ | GRAPH-04 | Evidence closes; verified learning informs reviewed gate; gate renews | 1–2 | COVERED | Exact Phase 3 verification subsections plus unique ISA decision. |
| REQ | GRAPH-05 | Gates, freshness, stops, blocked truth | 1–2 | COVERED | Explicit held-boundary gate and parity assertions. |
| RESEARCH | — | No RESEARCH.md exists; research disabled | — | EXCLUDED | Plan 04-01 compiler and canonical repository sources govern. |
| CONTEXT | — | No CONTEXT.md exists | — | EXCLUDED | User authorized direct planning from canonical sources. |
</source_audit>

<verification>
1. Four Phase 4 commits exist in order through Plan 04-02: two RED boundaries and two GREEN implementations.
2. node scripts/generate-intent-graph.mjs --check passes twice from committed HEAD.
3. node --test scripts/intent-graph.test.mjs scripts/generate-intent-graph.test.mjs passes.
4. node --test scripts/infinite-game-anchors.test.mjs passes unchanged.
5. git diff --check origin/main...HEAD over Plan 04-01/02 files passes; forbidden runtime, D1, Worker, package, deployment, provider, and connected-repository paths are absent.
</verification>

<success_criteria>
- Operators can inspect the complete source-backed graph in JSON or Markdown with equal truth.
- Stale or missing sources/outputs fail closed and identify exact repository-relative paths.
- Generated outputs cannot silently mutate anchors, ISA, GSD, D1, or any declared source.
- The blocked boundary remains blocked, never complete, in both readbacks.
</success_criteria>

<output>
Create .planning/phases/04-provenance-preserving-intent-graph/04-02-SUMMARY.md with both commit SHAs, actual graph/source-set digests, node/edge counts, exact source paths, parity and stale-output test evidence, preserved authority boundaries, and handoff to Plan 04-03. Do not claim Phase 4 complete.
</output>
