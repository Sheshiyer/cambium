---
phase: 03-canonical-infinite-game-anchors
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - ISA.md
  - docs/doctrine/README.md
  - docs/README.md
  - docs/LIFECYCLE.md
  - docs/architecture/cambium-operating-fabric.md
  - docs/architecture/contracts/mission-fabric-v1.md
autonomous: true
requirements:
  - ANCHOR-03
  - ANCHOR-04
must_haves:
  truths:
    - "D-04 / D-07 / D-08 / ANCHOR-04: every docs discovery/lifecycle surface links to both canonical anchors without copying doctrine or displacing retained history."
    - "D-05 / D-06 / ANCHOR-03: Repository Mission, FabricMission, and Mission scene have distinct names, scopes, sources, inheritance rules, and authority."
    - "D-03: Mission Fabric documentation cannot become a planner or writer; D1 Goal Graph retains operational state and Mission Fabric remains read-only."
    - "ISA closes only ISC-1275/1276 after full repository gates and deterministic post-evidence readback succeed."
  artifacts:
    - path: ISA.md
      provides: "Final Phase 3 progress, checked ISC-1273..1276, verify phase, and exact evidence readback"
      contains: "progress: 4/4"
    - path: docs/doctrine/README.md
      provides: "Doctrine catalog beginning with direct Vision and Mission references"
      contains: "Enduring Just Cause"
    - path: docs/README.md
      provides: "Documentation discovery entries for both canonical anchors"
      contains: "Current Repository Mission"
    - path: docs/LIFECYCLE.md
      provides: "Separate near-invariant Vision and renewable Repository Mission lifecycle rows"
      contains: "task or plan authority"
    - path: docs/architecture/cambium-operating-fabric.md
      provides: "Architecture-level Repository Mission / FabricMission / Mission scene terminology boundary"
      contains: "Repository Mission"
    - path: docs/architecture/contracts/mission-fabric-v1.md
      provides: "Frozen-contract terminology boundary linked to root MISSION.md"
      contains: "FabricMission"
  key_links:
    - from: docs/doctrine/README.md
      to: VISION.md
      via: "direct repository-relative link before supporting theory"
      pattern: "VISION\\.md"
    - from: docs/doctrine/README.md
      to: MISSION.md
      via: "direct repository-relative link before supporting theory"
      pattern: "MISSION\\.md"
    - from: docs/README.md
      to: VISION.md
      via: "Enduring Just Cause discovery row"
      pattern: "VISION\\.md"
    - from: docs/README.md
      to: MISSION.md
      via: "Current Repository Mission discovery row"
      pattern: "MISSION\\.md"
    - from: docs/LIFECYCLE.md
      to: VISION.md
      via: "near-invariant doctrine lifecycle row"
      pattern: "VISION\\.md"
    - from: docs/LIFECYCLE.md
      to: MISSION.md
      via: "renewable doctrine lifecycle row"
      pattern: "MISSION\\.md"
    - from: docs/architecture/cambium-operating-fabric.md
      to: MISSION.md
      via: "Repository Mission definition and no-inheritance rule"
      pattern: "MISSION\\.md"
    - from: docs/architecture/contracts/mission-fabric-v1.md
      to: MISSION.md
      via: "direct root doctrine reference without content or authority inheritance"
      pattern: "MISSION\\.md"
    - from: docs/architecture/cambium-operating-fabric.md
      to: docs/architecture/contracts/mission-fabric-v1.md
      via: "matching Repository Mission, FabricMission, and Mission scene vocabulary"
      pattern: "FabricMission"
---

<objective>
Complete reference-only documentation discovery and Mission terminology boundaries, then close the remaining Phase 3 ISA criteria from full verified evidence.

Purpose: Make the canonical anchors discoverable and semantically unambiguous without changing runtime shapes, creating a third authority, or relying on uncommitted diff state.
Output: One coherent six-file documentation/ISA commit plus final commit-range verification for the entire Phase 3 implementation.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@.project/HANDOFF.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-canonical-infinite-game-anchors/03-CONTEXT.md
@.planning/phases/03-canonical-infinite-game-anchors/03-RESEARCH.md
@.planning/phases/03-canonical-infinite-game-anchors/03-VALIDATION.md
@.planning/phases/03-canonical-infinite-game-anchors/03-01-SUMMARY.md
@ISA.md
@VISION.md
@MISSION.md
@docs/doctrine/README.md
@docs/README.md
@docs/LIFECYCLE.md
@docs/architecture/cambium-operating-fabric.md
@docs/architecture/contracts/mission-fabric-v1.md
@workers/quests/src/mission-fabric.ts
@scripts/infinite-game-anchors.test.mjs

<interfaces>
Exact approved ISA goal (must remain byte-for-byte unchanged from Plan 03-01):
Consolidate Cambium's doctrine into a provenance-preserving infinite-game architecture anchored by canonical VISION.md and renewable MISSION.md, with ISA and GSD as the only goal/planning authorities. Map vision → mission → finite goals → tasks → evidence → learning as a fractal graph, and expose Ralph next actions, skill-cluster and OmniRoute flows, gates, and stop conditions through Temperance.

Terminology contract:
- `Repository Mission` is the singular renewable doctrine horizon in root `MISSION.md`.
- `FabricMission` is an outcome-bounded D1 Goal Graph child record inside exactly one WorkObject.
- Mission Fabric only compiles and serves `FabricMission` read-only.
- `Mission scene` is the UI destination that renders the projection.
- No automatic content or authority inheritance exists among these meanings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link canonical anchors, close ISA evidence, and verify the committed range</name>
  <files>ISA.md, docs/doctrine/README.md, docs/README.md, docs/LIFECYCLE.md, docs/architecture/cambium-operating-fabric.md, docs/architecture/contracts/mission-fabric-v1.md</files>
  <read_first>
    - VISION.md, MISSION.md — canonical targets from Plan 03-01; link but never quote their bodies.
    - ISA.md — confirm `phase: execute`, `progress: 2/4`, checked ISC-1273/1274, unchecked ISC-1275/1276, exact goal, and historical labels from Plan 03-01.
    - docs/doctrine/README.md — preserve the additive root catalog and supporting theory.
    - docs/README.md — preserve current operating truth, historical/proof map, and hygiene status.
    - docs/LIFECYCLE.md — extend the single existing owner/meaning/instruction-authority table.
    - docs/architecture/cambium-operating-fabric.md — add terminology near OF-C1/OF-C3 without changing ontology shapes or operational rules.
    - docs/architecture/contracts/mission-fabric-v1.md — add matching terminology near Purpose while preserving the frozen public shape.
    - workers/quests/src/mission-fabric.ts — read-only source for exact `FabricMission`; never edit it.
    - .planning/phases/03-canonical-infinite-game-anchors/03-01-SUMMARY.md — use actual Plan 03-01 commit identities and evidence, never inferred values.
    - scripts/infinite-game-anchors.test.mjs — satisfy the remaining ANCHOR-03/04 assertions without weakening the Wave 0 contract.
  </read_first>
  <action>
In `docs/doctrine/README.md`, put direct `VISION.md` and `MISSION.md` links first in the root catalog with labels `Enduring Just Cause` and `Current Repository Mission`; make the doctrine stack start from those anchors and label `INFINITE-GAME.md` supporting theory. In `docs/README.md`, add the same two direct entries before `Infinite operator`. In `docs/LIFECYCLE.md`, add distinct linked rows for near-invariant Vision and renewable Repository Mission; state that both contain doctrine and neither supplies current task or plan authority. Preserve every existing row.

In both Mission Fabric documents, add the exact `<interfaces>` distinctions. State that D1 Goal Graph owns `FabricMission` operational state, Mission Fabric is read-only, Mission scene has no doctrine/write authority, and no `FabricMission` may inherit, replace, rewrite, or close Repository Mission. Add a direct repository-relative link to root `MISSION.md`. Preserve `FabricMission` and `MissionFabricProjectionV1` shapes and all runtime/route/UI behavior.

Before changing ISA, run the focused contract, full `npm test`, drift audit, and rendered-doc check while ISA remains at `progress: 2/4`. Only after all four pre-closure gates pass, mark only `ISC-1275` and `ISC-1276` checked in `ISA.md`; set active frontmatter `progress: 4/4`, `phase: verify`, and the actual `updated` timestamp. Append exact evidence entries for ISC-1275/1276 naming the focused test, full `npm test`, drift audit, rendered-doc check, and their real exit results. Preserve the exact approved task/Goal, Phase 3 iteration ID, Test Strategy, Feature, historical issue-331 labels, and every pre-existing ISC ID/checkbox.

Rerun the entire focused test after the ISA status/evidence edit, then run the deterministic ISA readback in `<verify>`. Only after both succeed, allow execute-plan's per-task commit step to stage exactly the six plan-owned files and create `docs(03-02): link anchors and close mission terminology`. After that single task commit exists, run every `<post_commit_verify>` command against `base=$(git merge-base HEAD origin/main)` through committed HEAD. Do not create an intermediate docs-only commit and do not use an uncommitted `git diff` as phase proof.
  </action>
  <acceptance_criteria>
    - Both indexes link to both anchors before supporting doctrine and contain no copied anchor paragraph.
    - Lifecycle has separate linked cadence/authority rows for Vision and Repository Mission.
    - Both Mission Fabric docs name Repository Mission, `FabricMission`, Mission scene, D1 Goal Graph, read-only projection, and the no-inheritance/no-rewrite boundary.
    - Focused test, full suite, drift audit, and rendered-doc check pass before ISA closure.
    - ISA ends at `phase: verify`, `progress: 4/4`; ISC-1273 through ISC-1276 are checked with exact command evidence.
    - Exact approved goal, iteration, Test Strategy, Feature, historical labels, and all pre-existing criteria remain unchanged.
    - Entire focused test and deterministic ISA readback pass after the final status/evidence edit and before commit.
    - One commit contains exactly ISA plus the five Plan 03-02 documentation files.
    - Final commit-range patch, forbidden-path, non-destructive, sensitive-material, and commit-boundary checks pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/infinite-game-anchors.test.mjs &amp;&amp; node -e 'const fs=require("node:fs"); const s=fs.readFileSync("ISA.md","utf8"); const exact="Consolidate Cambium\u0027s doctrine into a provenance-preserving infinite-game architecture anchored by canonical VISION.md and renewable MISSION.md, with ISA and GSD as the only goal/planning authorities. Map vision → mission → finite goals → tasks → evidence → learning as a fractal graph, and expose Ralph next actions, skill-cluster and OmniRoute flows, gates, and stop conditions through Temperance."; const fm=(s.match(/^---\n([\s\S]*?)\n---/)||[])[1]||""; const active=(s.split("\n## Goal\n")[1]||"").trimStart(); const verification=s.split("\n## Verification\n")[1]||""; const checks=[fm.includes(`task: "${exact}"`),/^phase: verify$/m.test(fm),/^progress: 4\/4$/m.test(fm),/^iteration: 2026-08-18-infinite-game-doctrine-and-intent-graph$/m.test(fm),active.startsWith(exact),/historical acceptance evidence/i.test(s),/- \[x\] ISC-1273:/.test(s),/- \[x\] ISC-1274:/.test(s),/- \[x\] ISC-1275:/.test(s),/- \[x\] ISC-1276:/.test(s),/ISC-1273\.\.1276[\s\S]*scripts\/infinite-game-anchors\.test\.mjs/.test(s),/CanonicalInfiniteGameAnchors[\s\S]*ISC-1273\.\.1276/.test(s),/ISC-1275:[\s\S]*node --test scripts\/infinite-game-anchors\.test\.mjs/.test(verification),/ISC-1276:[\s\S]*npm test[\s\S]*npm run drift:audit[\s\S]*npm run render-docs:check/.test(verification)]; if(checks.some(v=&gt;!v)) process.exit(1)'</automated>
  </verify>
  <commit>Stage only `ISA.md`, `docs/doctrine/README.md`, `docs/README.md`, `docs/LIFECYCLE.md`, `docs/architecture/cambium-operating-fabric.md`, and `docs/architecture/contracts/mission-fabric-v1.md`; commit `docs(03-02): link anchors and close mission terminology`.</commit>
  <post_commit_verify>
    1. `node --test scripts/infinite-game-anchors.test.mjs` and the deterministic ISA readback from `<verify>` pass against committed HEAD.
    2. `test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort | tr '\n' ' ')" = "ISA.md docs/LIFECYCLE.md docs/README.md docs/architecture/cambium-operating-fabric.md docs/architecture/contracts/mission-fabric-v1.md docs/doctrine/README.md "` proves the atomic Wave 2 commit owns exactly six files.
    3. `base=$(git merge-base HEAD origin/main) &amp;&amp; git diff --check "$base"..HEAD -- scripts/infinite-game-anchors.test.mjs ISA.md VISION.md MISSION.md INFINITE-GAME.md PROJECT.md docs/doctrine/README.md docs/README.md docs/LIFECYCLE.md docs/architecture/cambium-operating-fabric.md docs/architecture/contracts/mission-fabric-v1.md` passes.
    4. `base=$(git merge-base HEAD origin/main) &amp;&amp; test -z "$(git diff --name-only "$base"..HEAD -- workers/quests/src/mission-fabric.ts workers/quests/src/page ARCHITECTURE.md package.json package-lock.json pnpm-lock.yaml .temperance)"` passes.
    5. `base=$(git merge-base HEAD origin/main) &amp;&amp; test -z "$(git diff --diff-filter=DR --name-status "$base"..HEAD -- scripts/infinite-game-anchors.test.mjs ISA.md VISION.md MISSION.md INFINITE-GAME.md PROJECT.md docs/doctrine/README.md docs/README.md docs/LIFECYCLE.md docs/architecture/cambium-operating-fabric.md docs/architecture/contracts/mission-fabric-v1.md)"` passes.
    6. `bash -o pipefail -c 'set -euo pipefail; base=$(git merge-base HEAD origin/main); added=$(mktemp); trap '\''rm -f "$added"'\'' EXIT; git diff --unified=0 "$base"..HEAD -- scripts/infinite-game-anchors.test.mjs ISA.md VISION.md MISSION.md INFINITE-GAME.md PROJECT.md docs/doctrine/README.md docs/README.md docs/LIFECYCLE.md docs/architecture/cambium-operating-fabric.md docs/architecture/contracts/mission-fabric-v1.md | sed -n "s/^+//p" &gt;"$added"; if rg -n "(/Users/|/Volumes/|/private/tmp/|AKIA[0-9A-Z]{16}|gh[pousr]_[A-Za-z0-9]{20,}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|(?:api[_-]?key|access[_-]?token|client[_-]?secret|password)[[:space:]]*[:=][[:space:]]*[^[:space:]]+)" "$added"; then exit 1; fi'` passes.
    7. `base=$(git merge-base HEAD origin/main); git log --format=%s "$base"..HEAD | rg -qx 'test\(03-01\): add failing infinite-game anchor contract'; git log --format=%s "$base"..HEAD | rg -qx 'docs\(03-01\): bind ISA and canonical doctrine anchors'; git log --format=%s "$base"..HEAD | rg -qx 'docs\(03-02\): link anchors and close mission terminology'` passes.
  </post_commit_verify>
  <done>All four Phase 3 ISCs are checked from committed evidence, and the full implementation range preserves authority, runtime, history, privacy, and scope boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical anchors → docs indexes | Normative doctrine crosses only as direct repository-relative references and role labels. |
| Repository Mission → FabricMission | Shared language must not create shared content, lifecycle, or write authority. |
| Documentation → runtime | Architecture/contract prose may clarify names but cannot alter frozen types, D1 state, routes, or UI behavior. |
| Local checkout → commit | Machine paths, credentials, prompts/sessions, and private vault material are forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Docs indexes | mitigate | Direct singular anchor links plus semantic assertions; no excerpts. |
| T-03-02 | Elevation of privilege | Docs/ISA authority prose | mitigate | Explicit ISA/GSD role statements and no planner/writer claims. |
| T-03-03 | Tampering | Mission terminology/runtime boundary | mitigate | Matching terminology, D1 ownership, read-only projection, forbidden-path commit-range gate. |
| T-03-04 | Information disclosure | Added docs/ISA evidence | mitigate | Commit-range scan for machine paths, credentials, private-key headers, prompts/session material. |
| T-03-05 | Repudiation | ISA completion evidence | mitigate | Final status changes only after full gates, rerun, deterministic readback, and committed range proof. |
| T-03-SC | Tampering | npm supply chain | accept | No package installation or manifest/lockfile modification. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Task | Status |
|--------|----|-----------------------|------|--------|
| GOAL | — | Explicit Mission terminology and traceable canonical anchors | 1 | COVERED |
| REQ | ANCHOR-03 | Repository Mission / FabricMission distinction | 1 | COVERED |
| REQ | ANCHOR-04 | Reference-only indexes/lifecycle and supporting history | 1 | COVERED |
| RESEARCH | Docs write set | Doctrine index, docs index, lifecycle, architecture, contract | 1 | COVERED |
| RESEARCH | Full gates and committed-range proof | Full tests then ISA closure then range verification | 1 | COVERED |
| CONTEXT | D-03..D-06 | Authority, references, names, D1/read-only boundary | 1 | COVERED |
| CONTEXT | D-07..D-08 | Additive discoverability and retained history | 1 | COVERED |

Phase 4 graph/digests, Phase 5 Ralph/Temperance projection, Phase 6 inventory/relocation, Phase 7 broad enforcement, and connected-repository overlays remain explicit exclusions.
</source_audit>

<verification>
Task 1's `<post_commit_verify>` is the phase-level gate. It runs only after the three implementation commits exist and compares the merge-base with `origin/main` through committed HEAD. No completion claim may rely on an uncommitted diff.
</verification>

<success_criteria>
- Both plans and waves execute in dependency order: 03-01 then 03-02.
- Full tests precede final ISA closure; focused tests and deterministic ISA state readback repeat afterward and before commit.
- Final committed range contains only the bounded doctrine/ISA/documentation implementation and no forbidden runtime or later-phase change.
- ANCHOR-01 through ANCHOR-04 and ISC-1273 through ISC-1276 are proven without moving/deleting history or creating another authority.
</success_criteria>

<output>
Create `.planning/phases/03-canonical-infinite-game-anchors/03-02-SUMMARY.md` with the atomic Wave 2 commit SHA, pre-closure full-gate results, post-evidence focused/ISA readback results, final commit-range verification, exact six-file write set, preserved exclusions, and the next command `$gsd-verify-work 3`.
</output>
