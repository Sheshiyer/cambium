---
phase: 03-canonical-infinite-game-anchors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/infinite-game-anchors.test.mjs
  - ISA.md
  - VISION.md
  - MISSION.md
  - INFINITE-GAME.md
  - PROJECT.md
autonomous: true
requirements:
  - ANCHOR-01
  - ANCHOR-02
  - ANCHOR-04
must_haves:
  truths:
    - "D-01 / ANCHOR-01: root VISION.md is the singular near-invariant Just Cause, changes only through doctrine review, and declares no finite product endpoint."
    - "D-02 / ANCHOR-02: root MISSION.md is the singular renewable Repository Mission with an event-bounded v0.4 horizon, evidence, renewal triggers, and explicit retirement or replacement."
    - "D-03: ISA owns approved goals and acceptance, GSD owns finite planning state, and the two root anchors own doctrine without becoming planners."
    - "D-04 / D-07 / D-08 / ANCHOR-04: root supporting surfaces point to both anchors without copying doctrine, deleting history, or pulling graph/manifest work forward."
  artifacts:
    - path: scripts/infinite-game-anchors.test.mjs
      provides: "Wave 0 RED/GREEN semantic contract for ANCHOR-01 through ANCHOR-04"
      contains: "node:test"
    - path: ISA.md
      provides: "Exact active Phase 3 goal, stable ISC-1273..1276 contract, Test Strategy, Feature, and Wave 1 evidence"
      contains: "CanonicalInfiniteGameAnchors"
    - path: VISION.md
      provides: "Canonical near-invariant Cambium Vision"
      contains: "# Cambium Vision"
    - path: MISSION.md
      provides: "Canonical renewable Repository Mission"
      contains: "# Cambium Mission"
    - path: INFINITE-GAME.md
      provides: "Retained infinite-game theory scoped beneath both canonical anchors"
      contains: "supporting theory"
    - path: PROJECT.md
      provides: "Root pickup map linking both anchors and the ISA/GSD authority split"
      contains: "MISSION.md"
  key_links:
    - from: VISION.md
      to: ISA.md
      via: "explicit statement that ISA owns approved goals and acceptance"
      pattern: "ISA.*approved goals"
    - from: VISION.md
      to: .planning/STATE.md
      via: "explicit statement that GSD owns finite planning state"
      pattern: "GSD.*planning"
    - from: MISSION.md
      to: ISA.md
      via: "explicit approved-goal authority boundary"
      pattern: "ISA.*approved goals"
    - from: MISSION.md
      to: .planning/STATE.md
      via: "explicit finite-planning authority boundary"
      pattern: "GSD.*planning"
    - from: INFINITE-GAME.md
      to: VISION.md
      via: "prominent direct canonical-anchor reference"
      pattern: "VISION\\.md"
    - from: INFINITE-GAME.md
      to: MISSION.md
      via: "prominent direct canonical-anchor reference"
      pattern: "MISSION\\.md"
    - from: PROJECT.md
      to: VISION.md
      via: "root authority/pickup map row"
      pattern: "VISION\\.md"
    - from: PROJECT.md
      to: MISSION.md
      via: "root authority/pickup map row"
      pattern: "MISSION\\.md"
---

<objective>
Create and prove Cambium's canonical root Vision and renewable Repository Mission, with an exact active ISA binding and committed Wave 0 contract.

Purpose: Establish the singular doctrine and acceptance foundation that every later reference, graph, and generated projection must inherit without copying authority.
Output: One RED test commit and one coherent root-anchor/ISA commit, leaving Phase 3 at `progress: 2/4` for dependent Plan 03-02.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@.project/HANDOFF.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-canonical-infinite-game-anchors/03-CONTEXT.md
@.planning/phases/03-canonical-infinite-game-anchors/03-RESEARCH.md
@.planning/phases/03-canonical-infinite-game-anchors/03-VALIDATION.md
@ISA.md
@INFINITE-GAME.md
@scripts/release-contract.test.mjs
@package.json

<interfaces>
Exact approved ISA goal text:
Consolidate Cambium's doctrine into a provenance-preserving infinite-game architecture anchored by canonical VISION.md and renewable MISSION.md, with ISA and GSD as the only goal/planning authorities. Map vision → mission → finite goals → tasks → evidence → learning as a fractal graph, and expose Ralph next actions, skill-cluster and OmniRoute flows, gates, and stop conditions through Temperance.

Stable Phase 3 acceptance IDs:
- `ISC-1273` → ANCHOR-01 → `scripts/infinite-game-anchors.test.mjs`
- `ISC-1274` → ANCHOR-02 → `scripts/infinite-game-anchors.test.mjs`
- `ISC-1275` → ANCHOR-03 → `scripts/infinite-game-anchors.test.mjs`
- `ISC-1276` → ANCHOR-04 → `scripts/infinite-game-anchors.test.mjs`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit the RED infinite-game anchor contract</name>
  <files>scripts/infinite-game-anchors.test.mjs</files>
  <read_first>
    - package.json — confirms `scripts/*.test.mjs` already participates in `npm test`; do not change manifests.
    - scripts/release-contract.test.mjs — use the existing `node:test`, strict assertion, repository URL, and file-read conventions.
    - .planning/phases/03-canonical-infinite-game-anchors/03-CONTEXT.md — D-01 through D-08 are locked.
    - .planning/phases/03-canonical-infinite-game-anchors/03-RESEARCH.md — use stable semantic assertions and the exact write/non-write sets.
    - ISA.md — detect the stale issue-331 active binding without deleting its historical evidence.
    - INFINITE-GAME.md, PROJECT.md — detect missing canonical-anchor references.
    - docs/doctrine/README.md, docs/README.md, docs/LIFECYCLE.md — detect missing reference-only discovery/lifecycle rows for dependent Plan 03-02.
    - docs/architecture/cambium-operating-fabric.md, docs/architecture/contracts/mission-fabric-v1.md — detect missing Mission terminology boundaries for dependent Plan 03-02.
  </read_first>
  <behavior>
    - `canonical root anchors declare singular doctrine and authority` covers ISC-1273/ANCHOR-01 and ISC-1274/ANCHOR-02.
    - `ISA binds the approved v0.4 goal without erasing history` proves the exact goal, stable ISCs, Test Strategy, Feature, historical labels, and one of two coherent evidence states: `execute` + `2/4` + ISC-1273/1274 checked, or `verify` + `4/4` + ISC-1273..1276 checked.
    - `reference-only discovery surfaces point to canonical anchors` covers ISC-1276/ANCHOR-04.
    - `Repository Mission and FabricMission remain distinct` covers ISC-1275/ANCHOR-03.
  </behavior>
  <action>
Create `scripts/infinite-game-anchors.test.mjs` with Node built-ins only. Make missing files fail through bounded helper assertions rather than top-level reads. Assert stable headings, exact direct paths, authority phrases, active ISA readback, historical issue-331 labels, stable `ISC-1273..1276`, the Test Strategy/Feature binding, and frozen Mission Fabric terminology; do not snapshot complete prose or hash anchor bodies. The ISA test must accept only the two coherent state/checkbox pairs named in `<behavior>`, so it stays GREEN after Plan 03-01 and after Plan 03-02 while rejecting partial or false completion. Before any implementation, run the focused file and preserve expected RED output. The run must fail for at least one named Phase 3 semantic gap and must not contain `SyntaxError`, `ERR_MODULE_NOT_FOUND`, `ENOENT`, `no such file or directory`, or an uncaught top-level missing-file stack. Confirm the new test is untracked with `git status --short`, not `git diff`.
  </action>
  <acceptance_criteria>
    - The file imports `node:assert/strict`, `node:fs`, and `node:test` and resolves the repository through `new URL('..', import.meta.url)`.
    - Exactly four named semantic tests cover ANCHOR-01 through ANCHOR-04 and ISC-1273 through ISC-1276.
    - `git status --short -- scripts/infinite-game-anchors.test.mjs` prints `?? scripts/infinite-game-anchors.test.mjs` before commit.
    - The RED probe exits non-zero for a named semantic assertion and rejects syntax, module-resolution, and uncaught missing-file failures.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; out=$(mktemp); trap '\''rm -f "$out"'\'' EXIT; test "$(git status --short -- scripts/infinite-game-anchors.test.mjs)" = "?? scripts/infinite-game-anchors.test.mjs"; if node --test scripts/infinite-game-anchors.test.mjs &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; rg -q "VISION\\.md|MISSION\\.md|approved v0\\.4 goal|Repository Mission|FabricMission" "$out"; if rg -q "SyntaxError|ERR_MODULE_NOT_FOUND|ENOENT|no such file or directory" "$out"; then cat "$out"; exit 1; fi'</automated>
  </verify>
  <commit>Stage only `scripts/infinite-game-anchors.test.mjs` and commit `test(03-01): add failing infinite-game anchor contract`. Task 2 must start from this immutable RED boundary.</commit>
  <done>Wave 0 is committed with a semantically valid RED contract and no dependency, runtime, or manifest mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Bind active ISA acceptance and commit canonical root anchors</name>
  <files>ISA.md, VISION.md, MISSION.md, INFINITE-GAME.md, PROJECT.md</files>
  <read_first>
    - ISA.md — preserve every historical ID, checkbox, Goal paragraph, Decision, Changelog, and Verification entry.
    - INFINITE-GAME.md — source theory for Just Cause, near-invariant Vision, renewable Mission, finite goals, evidence, and learning.
    - PROJECT.md — extend existing pickup/authority maps additively.
    - .planning/phases/03-canonical-infinite-game-anchors/03-CONTEXT.md — implement D-01 through D-05, D-07, and D-08 exactly.
    - .planning/phases/03-canonical-infinite-game-anchors/03-RESEARCH.md — use the resolved exact goal, event-bounded v0.4 horizon, and documentation-only boundary.
    - scripts/infinite-game-anchors.test.mjs — make only the Wave 1 root/ISA test subset GREEN; do not weaken future Plan 03-02 assertions.
  </read_first>
  <action>
In `ISA.md`, replace the stale frontmatter `task` with the exact approved goal from `<interfaces>`; set `phase: execute`, `progress: 0/4`, `iteration: 2026-08-18-infinite-game-doctrine-and-intent-graph`, and the actual execution `updated` timestamp. Insert the exact same goal as the leading paragraph under `## Goal`. Label all retained issue-331 and earlier Vision/Goal/Criteria material explicitly historical without deleting, renumbering, rewriting, or toggling it. Append `ISC-1273` through `ISC-1276` as four unchecked active Phase 3 criteria, each naming `scripts/infinite-game-anchors.test.mjs` as its binary probe. Append one Test Strategy row for `ISC-1273..1276`, one `CanonicalInfiniteGameAnchors` Feature satisfying that range, and a `refined:` Decision recording the active/historical boundary.

Create `VISION.md` with `# Cambium Vision` and `## Just Cause`, `## Infinite-game commitments`, `## Non-goals`, `## Authority and change`, and `## Supporting references`. Implement D-01/D-03: continued meaningful play through coordinated finite games; revenue, growth, feature completion, and one product endpoint are not the finish line; only doctrine review changes Vision; ISA owns approved goals/acceptance and GSD owns planning.

Create `MISSION.md` with `# Cambium Mission` and `## Authority and lifecycle`, `## Current pursuit`, `## Horizon`, `## Evidence of progress`, `## Renewal triggers`, `## Retirement or replacement`, `## Inherited boundaries`, and `## Supporting references`. Implement D-02/D-05: name the singular `Repository Mission`; use reviewed verification and release completion of v0.4 as its event-bounded horizon; name checked requirements, summaries, verification, and reviewed handoff as progress evidence; define renewal and explicit replacement/retirement; state that ISA/GSD retain their authorities and no `FabricMission` inherits or rewrites repository doctrine.

Add a prominent note near the top of `INFINITE-GAME.md` linking both anchors and classifying the retained file as supporting theory. Add direct Vision/Mission rows to `PROJECT.md` before existing doctrine/planning maps and state the root-anchor/ISA/GSD role split. Link only; do not quote anchor bodies. Do not add an intent graph, digest, manifest, Ralph state, OmniRoute route, runtime edit, connected-repository overlay, move, or deletion.

Run the focused root/ISA tests. Only after they pass, mark `ISC-1273` and `ISC-1274` checked, set `progress: 2/4`, and append exact command evidence under `## Verification`; leave `ISC-1275` and `ISC-1276` unchecked. Rerun the same focused test and the deterministic ISA readback command in `<verify>` after the status/evidence edits and before committing.
  </action>
  <acceptance_criteria>
    - ISA frontmatter and leading Goal contain the exact approved text; active state ends `phase: execute`, `progress: 2/4`, with the Phase 3 iteration ID.
    - Issue-331 and earlier material remains present and explicitly historical; no pre-existing ISC ID or checkbox changes.
    - `ISC-1273` and `ISC-1274` are checked with command evidence; `ISC-1275` and `ISC-1276` remain unchecked.
    - Test Strategy and Features bind `ISC-1273..1276` to `scripts/infinite-game-anchors.test.mjs` and `CanonicalInfiniteGameAnchors`.
    - VISION.md, MISSION.md, INFINITE-GAME.md, and PROJECT.md satisfy their artifact/key-link contracts without copied doctrine.
    - Focused tests and deterministic ISA readback pass after the final status/evidence edits.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern='canonical root anchors|ISA binds' scripts/infinite-game-anchors.test.mjs &amp;&amp; node -e 'const fs=require("node:fs"); const s=fs.readFileSync("ISA.md","utf8"); const exact="Consolidate Cambium\u0027s doctrine into a provenance-preserving infinite-game architecture anchored by canonical VISION.md and renewable MISSION.md, with ISA and GSD as the only goal/planning authorities. Map vision → mission → finite goals → tasks → evidence → learning as a fractal graph, and expose Ralph next actions, skill-cluster and OmniRoute flows, gates, and stop conditions through Temperance."; const fm=(s.match(/^---\n([\s\S]*?)\n---/)||[])[1]||""; const active=(s.split("\n## Goal\n")[1]||"").trimStart(); const verification=s.split("\n## Verification\n")[1]||""; const checks=[fm.includes(`task: "${exact}"`),/^phase: execute$/m.test(fm),/^progress: 2\/4$/m.test(fm),/^iteration: 2026-08-18-infinite-game-doctrine-and-intent-graph$/m.test(fm),active.startsWith(exact),/historical acceptance evidence/i.test(s),/- \[x\] ISC-1273:/.test(s),/- \[x\] ISC-1274:/.test(s),/- \[ \] ISC-1275:/.test(s),/- \[ \] ISC-1276:/.test(s),/ISC-1273\.\.1276[\s\S]*scripts\/infinite-game-anchors\.test\.mjs/.test(s),/CanonicalInfiniteGameAnchors[\s\S]*ISC-1273\.\.1276/.test(s),/ISC-1273:[\s\S]*node --test scripts\/infinite-game-anchors\.test\.mjs/.test(verification),/ISC-1274:[\s\S]*node --test scripts\/infinite-game-anchors\.test\.mjs/.test(verification)]; if(checks.some(v=&gt;!v)) process.exit(1)'</automated>
  </verify>
  <commit>After both readbacks pass, stage only `ISA.md`, `VISION.md`, `MISSION.md`, `INFINITE-GAME.md`, and `PROJECT.md` and commit `docs(03-01): bind ISA and canonical doctrine anchors`.</commit>
  <done>The root anchor foundation is committed and ISA truthfully records 2/4 Phase 3 criteria, ready for dependent Plan 03-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root anchors → ISA/GSD | Doctrine must name but never acquire goal-setting, acceptance, or finite-planning authority. |
| Root anchors → supporting theory/indexes | Normative content may cross only as direct references and role labels. |
| Local checkout → committed artifacts | Machine-local paths, credentials, prompt/session material, and private vault content are forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Root Vision/Mission authority | mitigate | Singular paths, stable semantic tests, and direct-link-only supporting surfaces. |
| T-03-02 | Elevation of privilege | Anchor/ISA/GSD role split | mitigate | Explicit D-03 prose plus negative test assertions. |
| T-03-04 | Information disclosure | Added Markdown/test content | mitigate | Plan 03-02 final commit-range sensitive-material scan covers the full implementation write set. |
| T-03-05 | Repudiation | Doctrine lifecycle | mitigate | Vision doctrine review and Mission evidence/renewal/replacement conditions are explicit and tested. |
| T-03-SC | Tampering | npm supply chain | accept | No dependency or manifest change; Node built-ins own the test harness. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Task | Status |
|--------|----|-----------------------|------|--------|
| GOAL | — | Enduring Vision and renewable Mission with explicit authority | 1–2 | COVERED |
| REQ | ANCHOR-01 | Canonical root Vision | 1–2 | COVERED |
| REQ | ANCHOR-02 | Renewable root Mission | 1–2 | COVERED |
| REQ | ANCHOR-04 | Root supporting surfaces trace to anchors | 1–2 | COVERED |
| RESEARCH | Wave 0 | RED-first semantic contract | 1 | COVERED |
| RESEARCH | Root write set | ISA, anchors, INFINITE-GAME, PROJECT | 2 | COVERED |
| CONTEXT | D-01..D-03 | Anchor semantics and ISA/GSD authority | 1–2 | COVERED |
| CONTEXT | D-04, D-07, D-08 | Reference-only, additive, retained history | 1–2 | COVERED |

D-06 and the final ANCHOR-03/ANCHOR-04 documentation boundary are intentionally consumed by dependent Plan 03-02, not omitted.
</source_audit>

<verification>
After both task commits:

1. `node --test --test-name-pattern='canonical root anchors|ISA binds' scripts/infinite-game-anchors.test.mjs` passes.
2. Re-run Task 2's deterministic ISA readback against committed HEAD.
3. `base=$(git merge-base HEAD origin/main) &amp;&amp; git diff --check "$base"..HEAD -- scripts/infinite-game-anchors.test.mjs ISA.md VISION.md MISSION.md INFINITE-GAME.md PROJECT.md` passes.
4. `base=$(git merge-base HEAD origin/main) &amp;&amp; test -z "$(git diff --name-only "$base"..HEAD -- workers/quests/src/mission-fabric.ts workers/quests/src/page ARCHITECTURE.md package.json package-lock.json pnpm-lock.yaml .temperance)"` passes.
5. `base=$(git merge-base HEAD origin/main); git log --format=%s "$base"..HEAD | rg -qx 'test\(03-01\): add failing infinite-game anchor contract'; git log --format=%s "$base"..HEAD | rg -qx 'docs\(03-01\): bind ISA and canonical doctrine anchors'` passes.
</verification>

<success_criteria>
- RED and root-anchor commits exist as two explicit boundaries.
- Exact ISA active state is deterministic and historical issue-331 evidence remains unchanged.
- ISC-1273/1274 are proved; ISC-1275/1276 remain honestly open for Plan 03-02.
- No Phase 4–7, runtime, provider, external, connected-repository, move, or deletion work occurs.
</success_criteria>

<output>
Create `.planning/phases/03-canonical-infinite-game-anchors/03-01-SUMMARY.md` with RED evidence, both commit SHAs, deterministic ISA readback results, exact files changed, preserved historical boundary, and explicit handoff to Plan 03-02. Do not claim Phase 3 complete.
</output>
