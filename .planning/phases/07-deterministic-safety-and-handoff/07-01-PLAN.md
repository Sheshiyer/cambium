---
phase: 07-deterministic-safety-and-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ISA.md
  - scripts/infinite-game-anchors.test.mjs
  - scripts/deterministic-safety.test.mjs
  - scripts/deterministic-safety.mjs
  - scripts/intent-graph.mjs
  - scripts/temperance-flow.mjs
  - docs/architecture/contracts/deterministic-safety-v1.md
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04]
user_setup: []
must_haves:
  truths:
    - "D-01 / SAFE-01 / ISC-1290: one explicit committed revision yields the Phase 6 inventory path set; dirty worktree, staged bytes, untracked MEMORY/, and host Temperance state are ignored."
    - "D-02 / D-03 / SAFE-01: a normalized VISION.md or MISSION.md paragraph copied into any other corpus path fails closed; titles, filenames, and sha256 digest references are allowed."
    - "D-05 / D-06 / D-07 / SAFE-02 / ISC-1291: only the closed D-05 surface list is scanned for schema/role fields and self-claims; ISA.md and live .planning/STATE.md remain allowed claimants."
    - "D-09 / D-10 / D-11 / SAFE-03 / ISC-1292: generated projections that already declare source digests fail when a recorded selector digest does not match the committed blob, or when secrets, native session identifiers, prompt/response bodies, or machine-local absolute paths appear; Cloudflare account IDs, Worker Version UUIDs, and sha256 identities are not privacy hits."
    - "D-04 / D-08 / D-12: any hit throws with a repository-relative path, exits non-zero at the CLI boundary, and never rewrites the offending file or publishes a replacement projection."
  artifacts:
    - path: ISA.md
      provides: "Unchecked ISC-1290..1293 Phase 7 slice at plan 0/4 depending on DocumentationStewardship"
      contains: "ISC-1290"
    - path: scripts/deterministic-safety.test.mjs
      provides: "Named SAFE-01, SAFE-02, and SAFE-03 synthetic Git fixtures including dirty-tree and HEAD-pass probes"
    - path: scripts/deterministic-safety.mjs
      provides: "Pure SHA-bound compiler for SAFE-01..03"
      exports: [compileDeterministicSafety, validateDeterministicSafetyReceipt, DETERMINISTIC_SAFETY_SCHEMA]
    - path: docs/architecture/contracts/deterministic-safety-v1.md
      provides: "Committed inspection-only contract for npm run --silent safety:check -- --source-revision REV"
  key_links:
    - from: scripts/deterministic-safety.mjs
      to: scripts/documentation-inventory-sources.mjs
      via: "buildDocumentationInventorySources path-set equality before any body scan"
      pattern: "buildDocumentationInventorySources"
    - from: scripts/deterministic-safety.mjs
      to: "git show FULL_SHA:path"
      via: "one resolved ^{commit} SHA for every corpus and extra-surface blob read"
      pattern: "rev-parse"
    - from: ISA.md
      to: .planning/REQUIREMENTS.md
      via: "ISC-1290..1293 map one-to-one onto SAFE-01..04"
      pattern: "ISC-1290"
---

<objective>
Bind Phase 7 acceptance and implement the pure SHA-bound SAFE-01..03 compiler.

Purpose: make doctrine duplication, authority-drift self-claims, stale selector digests, and projection privacy leaks fail closed on one committed revision before any CLI is exposed.
Output: coherent unchecked ISC-1290..1293, named RED-then-GREEN compiler tests, a zero-write compiler, helper digest/selector exports, and the versioned safety contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@ISA.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-deterministic-safety-and-handoff/07-CONTEXT.md
@.planning/phases/07-deterministic-safety-and-handoff/07-RESEARCH.md
@.planning/phases/07-deterministic-safety-and-handoff/07-PATTERNS.md
@.planning/phases/07-deterministic-safety-and-handoff/07-VALIDATION.md
@scripts/documentation-inventory-sources.mjs
@scripts/documentation-inventory.mjs
@scripts/documentation-inventory.test.mjs
@scripts/intent-graph.mjs
@scripts/temperance-flow.mjs
@scripts/infinite-game-anchors.test.mjs
@docs/architecture/contracts/documentation-inventory-v1.md

<interfaces>
Schema is `cambium.deterministic-safety.v1`. Projection authority is exactly `read_only`. Gates are exactly `SAFE-01`, `SAFE-02`, and `SAFE-03`. Public command (wired in 07-02) is `npm run --silent safety:check -- --source-revision <REV>`. No committed JSON/Markdown safety report.

From scripts/documentation-inventory-sources.mjs:
- `buildDocumentationInventorySources({ repositoryRoot, sourceRevision })` requires `sourceRevision`, resolves once with `git rev-parse --verify REV^{commit}` under `/usr/bin/git --no-replace-objects --no-optional-locks`, validates a full lowercase 40-hex SHA, enumerates root `*.md` plus `docs/` plus `.planning/`, and returns `{ sourceRevision, corpusPaths, rootMemoryTracked, indexedProductBranchPackets, blobs }` where `blobs[].contentDigest` is raw-byte `sha256:` of `git show SHA:path`. It never reads worktree or index bytes.

From scripts/documentation-inventory.mjs (reuse closed-key / privacy shape only; do not feed bodies into `compileDocumentationInventory`):
- `DOCUMENTATION_INVENTORY_AUTHORITY = 'read_only'`
- Forbidden writer keys include `write`, `output`, `outputFile`, `queue`, `dispatch`, `command`, `promptBody`, `sessionId`.
- Unix roots in `PRIVATE_TEXT` are case-exact `/Users/` and `/Volumes/`.

From scripts/intent-graph.mjs (file-private today; this plan must export without changing algorithms):
- `canonicalText(value)` strips BOM, maps CR/CRLF to LF, and requires exactly one terminal LF.
- `digestText(value)` returns `sha256:` plus 64 lowercase hex of `canonicalText(value)`.
- `selectContent(raw, selector)` supports `whole-file`, `markdown.heading:`, `markdown.bold-field:H#F`, `frontmatter.F`, `markdown.list-item:`, `xml.task-name:`.

From scripts/temperance-flow.mjs (file-private today; this plan must export without changing algorithms):
- `selectContent(raw, selector)` supports the Temperance set including `text.line:` and does not implement `markdown.bold-field:`.
- Reviewed-handoff redaction before hashing: replace `` (`implementation_head` is `)[a-f0-9]{40}(`) `` with `$1<reviewed-implementation-head>$2` and generated `flowDigest` / `sourceSetDigest` lines with `<reviewed-generated-digest>`.
- `intentGraph.digest` is `digestText` of the Intent Graph JSON file bytes, not `graphDigest`.

From scripts/infinite-game-anchors.test.mjs:
- `PHASE6_CRITERIA = ['ISC-1286','ISC-1287','ISC-1288','ISC-1289']`
- `isCoherentIsaPhaseState(frontmatter, phase3Checks, phase4Checks, phase5Checks = {}, phase6Checks = {}, phase6AcceptanceHeading = null)` currently admits Phase 6 `plan 0/4` / execute prefix / `verify 4/4`. Extend with Phase 7 the same way Phase 6 was added on top of Phase 5.
- `phaseBaseSha()` today reads `06-01-SUMMARY.md`. Do not retarget it in this plan.

ISA currently has `phase: verify`, `progress: 4/4`, heading `### Completed Phase 6 acceptance`, and feature `DocumentationStewardship` satisfying ISC-1286..1289. Preserve completed Phase 3–6 headings and evidence.

Locked paragraph rule (RESEARCH A3, now a Wave 1 lock): after `canonicalText`, fold whitespace and punctuation, drop ATX-heading-only blocks, drop link-only lines, and drop blocks whose folded length is less than 80. Probe the real repository SHA in tests and require zero SAFE-01 hits on unmodified HEAD.

Locked claim grammar (RESEARCH A4, now a Wave 1 lock): schema/role fields are primary; prose fails only on self-claims or unattributed assertions. Allow denials, legends that define `gsd_planning`, and attributions to `ISA.md` / `.planning/STATE.md`. Unmodified HEAD must pass SAFE-02.

D-05 SAFE-02 surfaces (closed; do not scan historical `docs/plans/` or `docs/project-management/`):
- `docs/architecture/intent-graph.v1.json`, `docs/architecture/intent-graph.md`
- `docs/architecture/temperance-flow.v1.json`, `docs/architecture/temperance-flow.md`
- `.temperance/project.json` (named extra; not in the inventory corpus)
- Ralph: no committed ledger; a fixture `cambium.ralph-iteration.v1` object with `queue`, `dispatch`, or `selfCertified` fails
- Overlays: `PROJECT.md`, `README.md`, `docs/README.md`, `docs/doctrine/README.md`, `docs/LIFECYCLE.md`, `.planning/README.md`, `INFINITE-GAME.md`

Allowed claimants (D-07): `ISA.md`, `.planning/STATE.md`.

SAFE-03 generated projections that already declare source digests:
- Intent Graph JSON is the digest source of truth; Markdown byte parity remains complementary `generate-intent-graph.mjs --check` (not this SHA-bound gate).
- Temperance Flow JSON likewise; recompute with Temperance selectors plus reviewed-handoff redaction.
- Do not freshness-check ephemeral documentation-inventory stdout (D-10).

ISC IDs are locked as ISC-1290..1293. D-14 discuss-time `/gsd:plan-phase 7` is already consumed by this planning cycle; do not freeze it into ISA evidence. Shipped next command is owned by 07-03.

Do not install packages, edit lockfiles, invoke wrangler, CAS D1, ingest Vectorize, mint tenants, precreate `07-SUMMARY.md`, or edit `.planning/STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind coherent Phase 7 ISA acceptance</name>
  <files>ISA.md, scripts/infinite-game-anchors.test.mjs</files>
  <read_first>
    - ISA.md — preserve the approved goal, completed ISC-1273..1289 evidence, and Completed Phase 6 heading.
    - scripts/infinite-game-anchors.test.mjs — preserve every previously valid Phase 3–6 lifecycle assertion, then extend the matrix.
    - .planning/ROADMAP.md and .planning/REQUIREMENTS.md — map SAFE-01..SAFE-04 onto ISC-1290..ISC-1293.
    - 07-CONTEXT.md — implement D-01..D-16 without inventing a third goal or planning authority.
  </read_first>
  <behavior>
    - Phase 7 plan/execute 0/4 with all four ISC-1290..1293 criteria unchecked is valid only under heading Active Phase 7 acceptance after Phase 6 is complete.
    - Execute progress must equal a strict checked prefix; verify 4/4 requires all four checked under Completed Phase 7 acceptance.
    - Every previously valid Phase 3/4/5/6 lifecycle state remains valid when Phase 7 checks are omitted.
    - Feature DeterministicSafetyAndHandoff satisfies ISC-1290..1293 and depends_on DocumentationStewardship.
  </behavior>
  <action>
Extend the sentinel first. Add PHASE7_CRITERIA exactly ['ISC-1290','ISC-1291','ISC-1292','ISC-1293'] and optional phase7Checks plus phase7AcceptanceHeading parameters on isCoherentIsaPhaseState. When phase7 checks are omitted, return the existing Phase 6-coherent result. When they are present, require Phase 3–6 complete and admit only Active Phase 7 plan 0/4, execute 0/4, execute strict prefix, or Completed Phase 7 verify 4/4. Keep every prior Phase 3–6 assertion, including Completed Phase 6 verify 4/4.

Then add exactly one unchecked ISA slice. Insert heading Active Phase 7 acceptance with four unchecked criteria: ISC-1290 binds SAFE-01 doctrine-duplication fail-closed on the inventory corpus; ISC-1291 binds SAFE-02 authority-drift fail-closed on the D-05 surfaces; ISC-1292 binds SAFE-03 freshness and privacy fail-closed; ISC-1293 binds SAFE-04 reviewed handoff. Set frontmatter only to phase plan and progress 0/4. Preserve the approved goal and all prior criteria/evidence. Add Test Strategy row ISC-1290..1293 and Features row DeterministicSafetyAndHandoff | fail-closed commit-tree validation plus reviewed-held handoff without creating another authority | satisfies ISC-1290..1293 | depends_on DocumentationStewardship | parallelizable false. Update the live ISA coherence test to parse the unique Active Phase 7 heading and the four Phase 7 checkboxes.

Require the normal 07-01 summary to record the full pre-implementation phase_base_sha for the later T-07 privacy audit. Do not precreate the summary or alter normal GSD closeout.
  </action>
  <acceptance_criteria>
    - ISA.md contains exactly one heading matching ^### Active Phase 7 acceptance$ and no Completed Phase 7 heading.
    - ISA.md contains exactly one unchecked line per ISC-1290, ISC-1291, ISC-1292, and ISC-1293, and Completed Phase 6 remains checked.
    - ISA frontmatter has phase: plan and progress: 0/4.
    - ISA.md matches /DeterministicSafetyAndHandoff[^\n]*satisfies ISC-1290\.\.1293[^\n]*depends_on DocumentationStewardship/.
    - node --test scripts/infinite-game-anchors.test.mjs exits 0.
    - Previously valid Phase 6 Completed verify 4/4 assertions still pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/infinite-game-anchors.test.mjs</automated>
  </verify>
  <done>ISA has one coherent unchecked Phase 7 slice at plan 0/4, prior lifecycle states remain valid, and the summary contract records a full phase base without changing closeout behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing SAFE-01..03 synthetic-Git contracts (Wave 0)</name>
  <files>scripts/deterministic-safety.test.mjs</files>
  <read_first>
    - 07-VALIDATION.md — Wave 0 requires scripts/deterministic-safety.test.mjs named SAFE-01, SAFE-02, and SAFE-03.
    - scripts/documentation-inventory.test.mjs — copy createRepository, dirty/staged/MEMORY isolation, and replacement-ref denial.
    - scripts/intent-graph.test.mjs — copy the lazy-import requireSubject pattern so a missing compiler is a named assertion, not an unhandled module error.
    - 07-CONTEXT.md D-01..D-12 and 07-RESEARCH.md Pitfalls 1–4 plus A3/A4.
    - VISION.md and MISSION.md — use real paragraph bodies in fixtures so the fold rule is tested against doctrine, not toy strings.
  </read_first>
  <behavior>
    - Named tests include the substrings SAFE-01, SAFE-02, and SAFE-03 so node --test --test-name-pattern='SAFE-01' selects them.
    - Missing scripts/deterministic-safety.mjs fails as a named contract assertion, not as an uncaught module-not-found at import time.
    - Before the compiler exists, the focused suite exits non-zero.
    - Tests construct synthetic two-commit Git repositories with Node built-ins only and never write into the Cambium worktree corpus.
  </behavior>
  <action>
Create scripts/deterministic-safety.test.mjs using node:test and node:assert/strict only. Lazy-import ./deterministic-safety.mjs like intent-graph.test.mjs. Copy the inventory fixture helpers: temporary git init, two commits, writeFixtureFile, dirty worktree overlay, untracked MEMORY/private.md, and git replace denial. Name tests so --test-name-pattern='SAFE-01', 'SAFE-02', and 'SAFE-03' each select the corresponding requirement.

SAFE-01 cases: copy a folded-length>=80 VISION.md paragraph into a committed overlay and expect compileDeterministicSafety to throw a TypeError whose message contains SAFE-01 and the repository-relative overlay path; repeat for MISSION.md; allow titles, the filenames VISION.md / MISSION.md, and sha256: digest references; dirty-worktree and staged copies of doctrine must not change the SHA-bound result; untracked MEMORY/private.md must not appear in the scan set or diagnostics; independently enumerated root Markdown plus docs/ plus .planning/ paths at the SHA must equal buildDocumentationInventorySources(...).corpusPaths; replacement refs must not substitute another tree; an unmodified real-repository SHA (caller-supplied HEAD resolved to a full SHA of this checkout) must produce zero SAFE-01 hits; ATX headings, link-only lines, and folded blocks shorter than 80 must not match. After a throw, snapshot the fixture tree and Git index and assert byte/mode/mtime/index equality.

SAFE-02 cases: restrict scans to the D-05 list. Fail committed overlays or manifests with projectionAuthority other than read_only, role in {sole_operational_writer, planning authority, goal-setting}, active_planner other than isa / gsd / omitted, or sourceOfTruth claiming ISA/GSD. Fail the self-claim this file is the planning authority. Pass ISA.md and .planning/STATE.md containing the locked phrases. Pass a LIFECYCLE-shaped denial recency never grants release or planning authority. Pass .temperance/project.json with active_planner isa or with ownership.planning project and no active_planner. Fail active_planner ralph. Fail a committed cambium.ralph-iteration.v1 object that includes queue, dispatch, or selfCertified. A historical docs/plans/ file containing planning authority must not by itself fail when it is outside D-05. Unmodified HEAD must pass SAFE-02.

SAFE-03 cases: a generated intent-graph.v1.json whose recorded source.digest does not equal digestText(selectIntentGraphContent(git-show bytes, recorded selector)) throws with SAFE-03 and path#selector. A matching digest passes. Skip freshness when the projection is absent or declares no source digests (D-10), including inventory stdout. Privacy: generated projection bytes containing /Users/, /Volumes/, or promptBody= throw SAFE-03; Worker Version UUID 089181f6-ed60-4710-aab6-cd10855360e0, Cloudflare account-id-shaped 32-hex, and sha256: 64-hex must not throw. Split hostile fixture strings like T-06-22. Prove zero writes on hit.

Do not create scripts/deterministic-safety.mjs in this task. Run the focused suite and require a non-zero exit (RED). If the suite passes, the RED contract is wrong — do not proceed to Task 3 until it fails for missing or unimplemented compiler behavior.
  </action>
  <acceptance_criteria>
    - test -f scripts/deterministic-safety.test.mjs succeeds.
    - rg -n "SAFE-01|SAFE-02|SAFE-03" scripts/deterministic-safety.test.mjs reports at least one test name per gate.
    - scripts/deterministic-safety.mjs does not exist after this task.
    - node --test --test-name-pattern='SAFE-0' scripts/deterministic-safety.test.mjs exits non-zero.
    - The test file does not contain concatenated /Users/ or /Volumes/ literals; hostile strings are split.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; test -f scripts/deterministic-safety.test.mjs; test ! -f scripts/deterministic-safety.mjs; if node --test --test-name-pattern="SAFE-0" scripts/deterministic-safety.test.mjs; then echo "expected Wave 0 RED failure" >&2; exit 1; fi'</automated>
  </verify>
  <done>Named SAFE-01..03 tests exist, fail because the compiler is unimplemented, and encode dirty-tree, HEAD-pass, claim-grammar, freshness, and privacy contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the SHA-bound compiler and safety contract</name>
  <files>scripts/deterministic-safety.mjs, scripts/intent-graph.mjs, scripts/temperance-flow.mjs, docs/architecture/contracts/deterministic-safety-v1.md, scripts/deterministic-safety.test.mjs</files>
  <read_first>
    - scripts/deterministic-safety.test.mjs — implement exactly the Task 2 contracts; do not weaken HEAD-pass or dirty-tree assertions.
    - scripts/documentation-inventory-sources.mjs — consume buildDocumentationInventorySources; do not reimplement ls-tree.
    - scripts/intent-graph.mjs and scripts/temperance-flow.mjs — export helpers without changing digest or selector algorithms.
    - scripts/documentation-inventory.mjs — copy closed-key and fail-closed validate shape, not inventory vocabulary.
    - 07-PATTERNS.md compiler section and 07-RESEARCH.md Pattern 2 selector table.
  </read_first>
  <behavior>
    - compileDeterministicSafety requires repositoryRoot plus sourceRevision, resolves once to a full SHA, and reads only git show FULL_SHA:path bytes.
    - Path set equals inventory corpusPaths; untracked MEMORY/ is a rootMemoryTracked fact only.
    - Copied VISION/MISSION paragraphs fail SAFE-01; D-05 self-claims fail SAFE-02; stale selector digests and D-11 privacy tokens fail SAFE-03; unmodified HEAD passes all three.
    - Hits throw TypeError containing the gate id and repository-relative path, and never rewrite files.
    - Existing intent-graph and temperance-flow suites still pass after helper exports.
  </behavior>
  <action>
Export canonicalText, digestText, and selectContent as selectIntentGraphContent from scripts/intent-graph.mjs without changing their bodies. Export selectContent as selectTemperanceFlowContent and a redactReviewedHandoffForDigest helper that performs the two existing reviewed_handoff replacements from scripts/temperance-flow.mjs, again without changing algorithms. Do not switch generate-intent-graph or generate-temperance-flow onto git show; those remain worktree compilers.

Implement scripts/deterministic-safety.mjs as a pure module (no CLI, no writeFileSync, no mkdir, no network). Export DETERMINISTIC_SAFETY_SCHEMA = 'cambium.deterministic-safety.v1', DETERMINISTIC_SAFETY_AUTHORITY = 'read_only', DETERMINISTIC_SAFETY_GATES = frozen ['SAFE-01','SAFE-02','SAFE-03'], compileDeterministicSafety, and validateDeterministicSafetyReceipt. Input is a closed object { repositoryRoot, sourceRevision }. Resolve the revision through buildDocumentationInventorySources, then independently enumerate the same commit tree and require exact sorted corpusPaths equality. Re-read text blobs only via git show FULL_SHA:path using the same /usr/bin/git --no-replace-objects --no-optional-locks spawn as the inventory adapter. Never call compileDocumentationInventory with bodies.

SAFE-01: extract VISION.md and MISSION.md paragraphs using the locked fold (canonicalText, punctuation/whitespace fold, drop ATX-heading-only blocks, drop link-only lines, drop folded length less than 80). Match those paragraphs against every other corpus path. Allow titles, filenames, and sha256: digest references. Skip binary blobs. On match, throw TypeError including SAFE-01 and the relative path.

SAFE-02: scan only D-05 surfaces present at the SHA. Include .temperance/project.json as a named extra git-show read when that path exists at the SHA; do not add it to corpusPaths. Treat schema/role fields as primary: projectionAuthority other than read_only, role in the locked set, active_planner other than isa / gsd / omitted, sourceOfTruth claiming ISA/GSD, and Ralph writer fields queue / dispatch / selfCertified. For prose, fail self-claims and unattributed assertions of source of record, planning authority, or goal-setting; allow denials, gsd_planning legends, and attributions to ISA.md or .planning/STATE.md. Skip ISA.md and .planning/STATE.md as targets. Do not substring-scan docs/plans/ or docs/project-management/.

SAFE-03 freshness: if docs/architecture/intent-graph.v1.json exists at the SHA, parse it and recompute each declared source and overlay-anchor digest from git-show bytes using selectIntentGraphContent / digestText; overlay anchors are whole-file digestText. If docs/architecture/temperance-flow.v1.json exists, recompute references with selectTemperanceFlowContent, apply redactReviewedHandoffForDigest when kind is reviewed_handoff, and treat intentGraph.digest as digestText of the Intent Graph JSON blob. Print mismatches as path#selector. Do not freshness-check inventory stdout or projections that declare no source digests. Do not compare to Git SHA-1 or inventory digestBuffer unless the recorded selector is whole-file raw bytes (it is not for these projections).

SAFE-03 privacy: scan the generated projection bytes (the four Intent Graph / Temperance Flow paths when present). Reuse inventory PRIVATE_TEXT / T-06-22 shapes with case-exact Unix roots. Allow Cloudflare account-id-shaped 32-hex, Worker Version UUIDs, 64-hex sha256: / graph digests, and historical D1 Telegram refs only if they appear inside existing docs/evidence/ receipts — those receipts are not the privacy corpus. Split any hostile literals in this compiler the same way tests do.

validateDeterministicSafetyReceipt accepts a closed object with schema, projectionAuthority read_only, full sourceRevision, entryCount, empty hits, sourceSetDigest or equivalent path-set digest, and safetyDigest = sha256 of the canonical object without safetyDigest. Reject extra keys and writer fields (write, fix, output, outputFile, queue, dispatch, command, relocate, delete). compileDeterministicSafety returns that receipt on success and throws on any hit. It never prints stdout.

Document the same rules in docs/architecture/contracts/deterministic-safety-v1.md: inspection-only authority, required --source-revision, commit-tree isolation, inventory corpus plus named extra SAFE-02 surface, locked paragraph and claim grammars, selector table, D-11 allowlists, zero-write prohibition, no committed report, and public invocation npm run --silent safety:check -- --source-revision REV. State that the CLI lands in 07-02.

Adjust Task 2 tests only if a fixture setup bug is proven; do not relax HEAD-pass, dirty-tree, or claim-grammar assertions. After GREEN, rerun intent-graph and temperance-flow tests to prove helper exports are non-behavioral.
  </action>
  <acceptance_criteria>
    - node --test --test-name-pattern='SAFE-0' scripts/deterministic-safety.test.mjs exits 0.
    - rg -n "export function compileDeterministicSafety" scripts/deterministic-safety.mjs equals 1.
    - rg -n "selectIntentGraphContent|canonicalText|digestText" scripts/intent-graph.mjs matches export of those names.
    - rg -n "selectTemperanceFlowContent|redactReviewedHandoffForDigest" scripts/temperance-flow.mjs matches exports.
    - rg -n "cambium.deterministic-safety.v1" docs/architecture/contracts/deterministic-safety-v1.md matches at least once.
    - rg -n "safety:check" docs/architecture/contracts/deterministic-safety-v1.md matches at least once.
    - node --test scripts/intent-graph.test.mjs scripts/temperance-flow.test.mjs exits 0.
    - git diff --name-only -- package-lock.json pnpm-lock.yaml yarn.lock is empty.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; node --test --test-name-pattern="SAFE-0" scripts/deterministic-safety.test.mjs; node --test scripts/intent-graph.test.mjs scripts/temperance-flow.test.mjs scripts/infinite-game-anchors.test.mjs; test -z "$(git diff --name-only -- package-lock.json pnpm-lock.yaml yarn.lock)"'</automated>
  </verify>
  <done>The compiler proves commit-tree isolation, locked paragraph and claim grammars, selector-aware freshness, D-11 privacy allowlists, zero writes, and HEAD-pass on the real SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Revision argument to Git object database | Untrusted revision text must resolve once to one commit before any corpus read. |
| Commit-tree bytes to compiler | Paths and bodies at the SHA are untrusted; worktree/index/MEMORY/host state must not substitute them. |
| Overlay/manifest prose to authority scan | Phrase mentions are untrusted; only closed D-05 surfaces and self-claims may fail. |
| Recorded projection digests to selector recompute | Declared path#selector#digest tuples are untrusted until recomputed from git-show bytes. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Spoofing | high | revision resolution | mitigate | Resolve once with `^{commit}`, require full 40-hex SHA, spawn git with `--no-replace-objects`, and bind every blob read to that SHA. Replacement-ref fixture in Task 2/3. |
| T-07-02 | Tampering | high | dirty tree substitution | mitigate | Path-set equality with `buildDocumentationInventorySources`; dirty/staged/MEMORY isolation tests; no worktree `readFileSync` in the SHA-bound gate. |
| T-07-03 | Tampering / DoS | high | phrase false-fail | mitigate | Closed D-05 list, schema/role-primary scan, claim-vs-denial grammar, and unmodified-HEAD SAFE-02 pass. |
| T-07-04 | Tampering / Repudiation | high | digest mismatch | mitigate | Recompute with recorded selector and the same `digestText`/`select*Content`/handoff-redaction functions that wrote the field; never Git SHA-1 or inventory `digestBuffer`. |
| T-07-05 | Information Disclosure | high | projection privacy | mitigate | Case-exact Unix roots; T-06-22 patterns; D-11 allowlists so Worker UUID / CF account / sha256 identities are not hits; split hostile fixtures. |
| T-07-06 | Elevation of Privilege | high | write-on-hit | mitigate | Pure compiler has no write APIs; tests snapshot tree/index after throws; reject `write`/`fix`/`output` keys on the receipt. CLI write-rejection is 07-02. |
| T-07-07 | Spoofing | high | GSD closeout spoofing | mitigate | This plan only binds unchecked ISC-1290..1293 and records `phase_base_sha` in the normal 07-01 summary. ISA 4/4 and HANDOFF are 07-03. |
| T-07-SC | Tampering | low | package supply chain | accept | Node built-ins only; no package install or lockfile change. |
</threat_model>

<verification>
- Wave 0 RED is proven in Task 2, then GREEN in Task 3.
- Focused SAFE-01..03 tests, infinite-game-anchors, intent-graph, and temperance-flow suites exit 0 after Task 3.
- `git diff --check` exits 0 and no dependency or lockfile changes exist.
- Unmodified repository SHA compiles without SAFE-01..03 hits.
</verification>

<success_criteria>
- Phase 7 acceptance exists as an unchecked coherent 0/4 slice mapping SAFE-01..04 to ISC-1290..1293.
- One immutable commit tree produces a validated read_only safety receipt or a fail-closed throw naming gate and relative path.
- The committed contract defines the SHA-bound gate without adding a committed generated report or a new authority.
</success_criteria>

<output>
Create `.planning/phases/07-deterministic-safety-and-handoff/07-01-SUMMARY.md` through the normal execute-plan lifecycle; include the full `phase_base_sha` and do not suppress ordinary GSD state, roadmap, requirements, or metadata handling.
</output>
