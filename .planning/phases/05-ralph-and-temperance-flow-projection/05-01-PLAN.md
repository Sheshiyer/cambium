---
phase: 05-ralph-and-temperance-flow-projection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/infinite-game-anchors.test.mjs
  - scripts/temperance-flow.test.mjs
  - scripts/temperance-flow.mjs
  - docs/architecture/contracts/temperance-flow-v1.md
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - "D-01 / FLOW-01 / ISC-1282: authority precedence is approved ISA goal, live GSD transition, then one dependency-ready unit from the active plan."
    - "D-02 / FLOW-01 / ISC-1282: missing, stale, conflicting, or ambiguous authority produces one blocked result and no executable command."
    - "D-03 and D-04 / FLOW-02 / ISC-1283: the contract represents a fresh one-unit read-select-verify-persist-exit iteration."
    - "D-05 / FLOW-02 / ISC-1283: the projection has no queue, scheduler, mutable ledger, self-certification, or terminal-work revival."
    - "D-09 and D-11 / FLOW-04 / ISC-1285: a separate validated read-only schema exposes references, one action-or-blocked result, routes, gates, freshness, and stops without authority."
    - "D-10 / FLOW-04 / ISC-1285: Phase 5 references the Phase 4 Intent Graph and cannot extend its closed vocabulary or feed a projection back as authority."
  artifacts:
    - path: scripts/infinite-game-anchors.test.mjs
      provides: "Coherent Phase 5 ISA lifecycle sentinel"
      contains: "ISC-1285"
    - path: scripts/temperance-flow.test.mjs
      provides: "RED/GREEN semantic and adversarial contract for FLOW-01 through FLOW-04"
      contains: "node:test"
    - path: scripts/temperance-flow.mjs
      provides: "Pure flow compiler, validator, and Markdown renderer"
      exports:
        - TEMPERANCE_FLOW_SCHEMA
        - compileTemperanceFlow
        - validateTemperanceFlowProjection
        - renderTemperanceFlowMarkdown
    - path: docs/architecture/contracts/temperance-flow-v1.md
      provides: "Human authority and lifecycle contract"
      contains: "read_only"
  key_links:
    - from: scripts/temperance-flow.test.mjs
      to: scripts/temperance-flow.mjs
      via: "public compiler, validator, and renderer contract"
      pattern: "compileTemperanceFlow"
    - from: scripts/temperance-flow.mjs
      to: docs/architecture/intent-graph.v1.json
      via: "safe reference and digest only"
      pattern: "intentGraphRef"
---

<objective>
Define and prove the Phase 5 read-only authority-resolution contract before generating repository readbacks.

Purpose: make competing authority, dependency ambiguity, multiple next actions, false completion, stale route evidence, unsafe provenance, and projection foldback fail closed at one pure boundary.
Output: a coherent Phase 5 ISA lifecycle sentinel, committed RED contract, pure compiler/validator/renderer, and human contract.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@ISA.md
@.project/HANDOFF.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ralph-and-temperance-flow-projection/05-CONTEXT.md
@.planning/phases/05-ralph-and-temperance-flow-projection/05-PATTERNS.md
@.planning/phases/04-provenance-preserving-intent-graph/04-VERIFICATION.md
@scripts/infinite-game-anchors.test.mjs
@scripts/intent-graph.mjs
@scripts/intent-graph.test.mjs
@docs/architecture/contracts/intent-graph-v1.md

<interfaces>
- Schema: `cambium.temperance-flow-projection.v1`; `projectionAuthority: read_only`.
- Result is a closed union: `ready` with exactly one canonical GSD command and one selected plan task, or `blocked` with no command and at least one source-backed reason.
- Authority order is exact: ISA frontmatter task; live STATE exact next transition; active plan dependency graph. Verified summaries/evidence and reviewed handoff can prove readiness or blocking but cannot define goals or plans.
- Route intent contains skill cluster, OmniRoute combo, execution lane `native_orchestrator` or `paid_execution`, approval requirement, and receipt reference. Resolved provider/model fields are absent unless the source adapter supplies an authenticated, fresh, task-and-route-bound host receipt verification result.
- Lifecycle is declarative and finite: reread, select_one, execute_external, verify_declared, persist_existing_surfaces, exit_external_condition. The compiler performs none of these effects.
- Phase 4 linkage is one `{path,digest,schema}` reference to `cambium.intent-graph-projection.v1`; no Phase 4 enum is imported, copied, or extended.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit coherent Phase 5 lifecycle and RED flow contracts</name>
  <files>scripts/infinite-game-anchors.test.mjs, scripts/temperance-flow.test.mjs</files>
  <read_first>
    - ISA.md — bind the checked Phase 4 criteria, unchecked ISC-1282..1285, and exact `phase: plan`, `progress: 0/4` state.
    - scripts/infinite-game-anchors.test.mjs — extend the closed lifecycle matrix; never accept arbitrary phase/progress combinations.
    - scripts/intent-graph.test.mjs — reuse semantic RED, temporary-root, deterministic, path, contradiction, and foldback test conventions.
    - .planning/STATE.md and active 05 plans — establish exact next-transition and dependency-ready semantics without copying them into a ledger.
    - docs/architecture/intent-graph.v1.json — bind only safe path, schema, and digest reference fields.
  </read_first>
  <action>
Before any Phase 5 execution edit, record `PHASE_BASE_SHA=$(git rev-parse HEAD)` and require that exact 40-hex commit to remain the parent of the first 05-01 implementation commit. Preserve it for `05-01-SUMMARY.md` as the machine-readable line `phase_base_sha: <sha>`; do not derive it later from a moving remote.

First update the anchor lifecycle helper and its self-test so it admits exactly: verified Phase 4 with ISC-1277..1281 checked; Phase 5 plan or execute at progress 0/4 with ISC-1282..1285 unchecked; Phase 5 execute with progress matching a strict checked-prefix only if execution uses that prefix; and Phase 5 verify 4/4 only when all four criteria are checked. Preserve every prior valid Phase 3/4 state and reject cross-phase checkbox/progress mixtures. Prove the current ISA becomes GREEN without weakening goal or history assertions.

Create `scripts/temperance-flow.test.mjs` with Node built-ins only. Load the not-yet-present module through a bounded semantic harness. Cover FLOW-01..04 and D-01..D-11 by name. Test exact authority precedence; missing/stale/conflicting sources; no plan, zero ready tasks, and multiple ready tasks; blocked dependencies; terminal states; exactly one command; closed command grammar; no mutable ledger; deterministic ordering/digests; repository-relative containment and symlink escape; reference-only Intent Graph linkage; read-only projection/foldback rejection; route intent; missing or non-verified adapter receipt results; mismatched task/projection/route receipt results; redaction; machine/human parity; and zero source mutation. Cryptographic issuer/signature verification belongs to the bounded source adapter in Plan 05-02; this compiler accepts resolved attribution only from a closed `verified` adapter result. Commit the lifecycle sentinel separately, then commit the semantic RED test and prove RED fails on a named FLOW assertion rather than module resolution or syntax failure.
  </action>
  <acceptance_criteria>
    - Current `ISA.md` passes the lifecycle sentinel only as Phase 5 plan 0/4 with Phase 4 checked and Phase 5 unchecked.
    - `PHASE_BASE_SHA` is captured before edits, is the parent of the first Phase 5 implementation commit, and is reserved for the exact `phase_base_sha: <40 hex>` summary field.
    - Invalid Phase 5 phase/progress/checkbox combinations are covered and fail.
    - RED names FLOW-01..04, ISC-1282..1285, and D-01..D-11, exits non-zero on a semantic assertion, and contains no SyntaxError, ERR_MODULE_NOT_FOUND, ENOENT, or uncaught top-level error.
    - No package, lockfile, runtime, provider, deployment, D1, generated readback, or external-state change occurs.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; node --test scripts/infinite-game-anchors.test.mjs; out=$(mktemp); trap '\''rm -f "$out"'\'' EXIT; if node --test scripts/temperance-flow.test.mjs &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; rg -q "FLOW-01|FLOW-02|FLOW-03|FLOW-04|temperance flow" "$out"; ! rg -q "SyntaxError|ERR_MODULE_NOT_FOUND|ENOENT|no such file or directory" "$out"'</automated>
  </verify>
  <commit>Commit the sentinel as `test(05-01): admit coherent phase 5 ISA lifecycle`, then the RED boundary as `test(05-01): add failing Temperance flow contract`.</commit>
  <done>The current ISA state is coherently admitted and one immutable semantic RED boundary covers the complete Phase 5 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure flow compiler and authority contract</name>
  <files>scripts/temperance-flow.mjs, docs/architecture/contracts/temperance-flow-v1.md</files>
  <read_first>
    - scripts/temperance-flow.test.mjs — satisfy without weakening assertions.
    - scripts/intent-graph.mjs — reuse canonicalization, hashing, safe paths, validation, and Markdown-from-object patterns without changing its vocabulary.
    - workers/quests/src/goal-graph/projection-contract.ts — preserve the derived-projection non-authority boundary; shared integration lands in Plan 05-02.
    - .planning/phases/05-ralph-and-temperance-flow-projection/05-CONTEXT.md — preserve D-01..D-11 exactly.
  </read_first>
  <action>
Create the pure compiler module using only Node built-ins. Export the four public symbols in frontmatter plus closed constants for statuses, authority kinds, source kinds, route lanes, receipt freshness, gate kinds, stop kinds, and lifecycle steps. Canonicalize recursively, sort unordered semantic collections, preserve lifecycle order, normalize LF text, produce lowercase `sha256:` digests, and resolve every referenced path by realpath beneath the repository root.

Validate exactly one `ready` command or a blocked result with zero commands. Require unique selected task identity, satisfied dependencies, non-terminal state, exact GSD command grammar, source references and digests, explicit gates/stops, and no blocked-as-complete contradiction. Treat verified evidence and handoff only as evidence. Reject source bodies, absolute paths, prompts/responses, session identifiers, secret-shaped fields, provider stacks, quotas, credentials, failover policy, dispatch/write callbacks, projection-shaped authority input, and any Phase 4 vocabulary extension. Validate resolved provider/model only through a fresh, task/route-bound host receipt carrying status, observed time or age, evidence pointer, and redacted attribution; otherwise emit intent only. Render Markdown only from the validated object and preserve byte-equivalent semantic facts.

Document the schema, precedence, ready/blocked union, lifecycle, host receipt boundary, D1/ISA/GSD/root-anchor boundaries, Intent Graph reference-only rule, foldback ban, privacy contract, and explicit statement that the module cannot execute or persist work.
  </action>
  <acceptance_criteria>
    - Equal semantic inputs compile to byte-stable flow and source-set digests and identical readbacks.
    - Every adversarial Task 1 case fails before an executable command or resolved provider is exposed.
    - Only a fresh bound receipt can populate resolved attribution; route intent remains inspectable without one.
    - Compiler and renderer expose no mutation, dispatch, network, provider, clock, or D1 API.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/infinite-game-anchors.test.mjs scripts/intent-graph.test.mjs scripts/temperance-flow.test.mjs &amp;&amp; npm test &amp;&amp; node -e 'import("./scripts/temperance-flow.mjs").then(m=&gt;{for(const k of ["TEMPERANCE_FLOW_SCHEMA","compileTemperanceFlow","validateTemperanceFlowProjection","renderTemperanceFlowMarkdown"]) if(!(k in m)) process.exit(1)})' &amp;&amp; git diff --check -- scripts/temperance-flow.mjs docs/architecture/contracts/temperance-flow-v1.md</automated>
  </verify>
  <commit>Stage only the compiler and contract; commit `feat(05-01): define read-only Temperance flow contract`.</commit>
  <done>The pure contract deterministically derives one safe action-or-blocked result while preserving every authority and host boundary.</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Disposition | Mitigation |
|---|---|---|---|
| Forged ISA/GSD authority or precedence | Spoofing/Elevation | mitigate | Closed source authorities, exact selectors, fixed precedence, conflict-to-blocked. |
| Stale or tampered source/receipt | Tampering | mitigate | Canonical digests, freshness bounds, task/route binding, recomputation. |
| Multiple or dependency-blocked dispatch | Elevation/DoS | mitigate | Ready-set cardinality exactly one; terminal/dependency gates. |
| Path, prompt, identity, or credential leakage | Disclosure | mitigate | realpath containment, forbidden fields/patterns, no source bodies. |
| Projection self-certifies or folds back | Elevation | mitigate | read_only schema, no writer API, projection-family rejection. |
| Host policy copied into repository | Tampering | mitigate | Closed route-intent fields; receipt references only; provider stack/quota/failover rejection. |
| Dependency/package compromise | Supply chain | accept | Node built-ins only; no manifest or lockfile edit. |
</threat_model>

<verification>
1. Anchor lifecycle, Intent Graph, and new flow focused tests pass.
2. Full `npm test` passes after the Phase 5 sentinel update.
3. Exports, docs, diff hygiene, allowed paths, and forbidden-category scans pass.
4. No generated readback, runtime, D1, host, provider, deployment, or external state changes in Plan 05-01.
</verification>

<success_criteria>
- FLOW-01..04 and D-01..D-11 have named binary contract coverage.
- The compiler emits exactly one dependency-safe command or an explicit blocked result.
- Route resolution attribution is impossible without a fresh bound host receipt.
- Phase 4 semantics remain closed and every projection remains read-only.
</success_criteria>

<output>
Create `05-01-SUMMARY.md` with the exact machine-readable line `phase_base_sha: <40 lowercase hex>` captured before Task 1, prove it is the parent of the first implementation commit, then record commit SHAs, focused/full test counts, exact schema and exports, held boundaries, and handoff to Plan 05-02. Do not claim Phase 5 complete.
</output>
