---
phase: 05-ralph-and-temperance-flow-projection
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/generate-temperance-flow.test.mjs
  - scripts/temperance-flow-sources.mjs
  - scripts/generate-temperance-flow.mjs
  - docs/architecture/temperance-flow.v1.json
  - docs/architecture/temperance-flow.md
  - workers/quests/src/goal-graph/projection-contract.test.ts
  - workers/quests/src/goal-graph/projection-contract.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-03
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - "D-01 and D-02 / FLOW-01 / ISC-1282: the bounded source adapter resolves exact ISA/GSD authority or emits blocked; it never invents a transition."
    - "D-06 / FLOW-03 / ISC-1284: committed readbacks expose skill-cluster, combo, and native-orchestrator versus paid-execution intent."
    - "D-07 / FLOW-03 / ISC-1284: resolved attribution exists only when the fixed host-owned Manifest verifier returns a fresh verified receipt bound to task, route, projection, and evidence; raw files and custom verifiers are non-authoritative, and host stacks, credentials, quotas, and failover policy never enter the repository."
    - "D-08 / FLOW-03 / ISC-1284: next-wave is proposal-only and fresh approval is required before the `te-dispatch-paid` execution lane."
    - "D-09 through D-11 / FLOW-04 / ISC-1285: one compiled object produces matching deterministic JSON/Markdown with references, digests, action-or-blocked, routes, gates, freshness, and stops and cannot become authority."
  artifacts:
    - path: scripts/temperance-flow-sources.mjs
      provides: "Bounded repository-source and explicit host-receipt adapter"
      exports:
        - buildTemperanceFlowSources
        - parseHostManifestReceipt
    - path: scripts/generate-temperance-flow.mjs
      provides: "Read-only write/check/JSON generator"
    - path: docs/architecture/temperance-flow.v1.json
      provides: "Canonical machine readback"
    - path: docs/architecture/temperance-flow.md
      provides: "Matching human readback"
    - path: workers/quests/src/goal-graph/projection-contract.ts
      provides: "Shared early rejection of flow projections from authoritative intake"
  key_links:
    - from: scripts/temperance-flow-sources.mjs
      to: ISA.md
      via: "exact frontmatter task selector"
      pattern: "frontmatter.task"
    - from: scripts/temperance-flow-sources.mjs
      to: .planning/STATE.md
      via: "exact current transition selector"
      pattern: "Next action"
    - from: scripts/generate-temperance-flow.mjs
      to: scripts/temperance-flow.mjs
      via: "compile once, serialize JSON, render Markdown"
      pattern: "compileTemperanceFlow"
    - from: workers/quests/src/goal-graph/projection-contract.ts
      to: scripts/temperance-flow.mjs
      via: "schema-family marker only; no runtime import"
      pattern: "temperance-flow-projection"
---

<objective>
Generate canonical Phase 5 machine and human readbacks from bounded durable sources and enforce the shared non-authority boundary.

Purpose: let operators inspect the exact next action, route intent, fresh resolution evidence, gates, freshness, and stopping condition while stale or unsafe inputs fail closed.
Output: RED generator/integration tests, source adapter, contained generator, committed JSON/Markdown readbacks, and shared projection-family guard.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ralph-and-temperance-flow-projection/05-01-SUMMARY.md
@.planning/phases/05-ralph-and-temperance-flow-projection/05-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@ISA.md
@.project/HANDOFF.md
@scripts/temperance-flow.mjs
@scripts/intent-graph-sources.mjs
@scripts/generate-intent-graph.mjs
@scripts/generate-intent-graph.test.mjs
@docs/architecture/intent-graph.v1.json
@workers/quests/src/goal-graph/projection-contract.ts
@workers/quests/src/goal-graph/projection-contract.test.ts

<interfaces>
- Declared repository sources are closed selectors over root ISA, live STATE, active Phase 5 plan files, prior verified summaries/evidence, reviewed handoff status/next-command/boundaries, and the Phase 4 generated Intent Graph reference. Summary/handoff generated-digest receipt fields and both flow outputs are explicitly excluded to prevent self-reference; no glob-selected or generated self-source is allowed.
- Host receipt input is an optional opaque evidence reference, never a raw receipt file. Only the fixed host-owned Manifest verification boundary may resolve it to `verified`; production code rejects custom endpoints/verifiers and never accepts caller-selected trust material. The default committed projection shows unresolved attribution and a receipt-required gate.
- The verified result is bounded to schema, fixed issuer/audience, issued/expires times, nonce, status, evidence pointer, task/projection/route bindings, redacted provider/model, and payload digest. Any unverified result, wrong issuer/audience, credential, stack, quota, failover policy, prompt/response body, native session ID, or absolute path invalidates attribution.
- Generator modes are mutually exclusive `--write`, `--check`, and `--json`, with optional contained `--root`, outputs, and opaque host-receipt reference. Defaults resolve from the script URL. Only `--json` may perform the bounded read-only live verification; committed `--write` and `--check` remain repository-deterministic and unresolved unless supplied a previously verified bounded fixture in test mode.
- Outputs: `docs/architecture/temperance-flow.v1.json` and `docs/architecture/temperance-flow.md`, both produced from one validated object.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit RED source, generator, receipt, parity, and foldback contracts</name>
  <files>scripts/generate-temperance-flow.test.mjs, workers/quests/src/goal-graph/projection-contract.test.ts</files>
  <read_first>
    - scripts/generate-intent-graph.test.mjs — mirror real-root/temporary-root, check/write parity, drift, privacy, and source-preservation probes.
    - scripts/temperance-flow.test.mjs and scripts/temperance-flow.mjs — use only the committed public contract.
    - .planning/STATE.md, ISA.md, and active 05 plan files — establish exact source selectors and dependency truth.
    - .project/HANDOFF.md — select only the newest checkpoint's status, exact next command, gate state, and held-boundary fields; exclude any flow/source digest receipt field so output digests cannot feed themselves.
    - workers/quests/src/goal-graph/projection-contract.test.ts — add exact, malformed, and normalized flow projection fixtures to the shared family guard.
  </read_first>
  <action>
Create generator tests using Node built-ins and temporary directories. Use an injected test-only Manifest verifier adapter and prove production entry points reject caller-selected endpoints, raw receipt files, and custom verifier/trust-root arguments. Prove real-root and fixture compilation; identical writes; double check; stale/missing selected source; stale outputs; JSON/Markdown parity; one ready or blocked result; route intent without inferred provider; fixed-boundary verified fresh matching attribution; unverified, attacker alternate verifier/root, wrong issuer/audience, stale, future, mismatched-task, mismatched-projection, mismatched-route, unredacted, absolute-path, secret-shaped, stack/quota/failover, replay-conflict, and missing-evidence result rejection; contained output paths; symlink rejection; no source-body or machine-local data; no source mutation; and zero runtime writes.

Extend the shared projection-contract test with exact valid, malformed, and normalized future flow projection markers. Require the family discriminator and authoritative-input validator to reject them without changing Goal Graph or Intent Graph behavior. Commit both RED files atomically and prove semantic named failures.
  </action>
  <acceptance_criteria>
    - RED tests map FLOW-01, FLOW-03, FLOW-04 and D-01, D-02, D-06..D-11 by name.
    - Receipt tests distinguish route intent from resolved attribution and reject raw/caller-trusted, custom-verifier, unverified, wrong-issuer/audience, stale, unbound, replay-conflicting, or policy-bearing results.
    - Foldback RED proves the shared production guard does not yet recognize the new schema.
    - RED contains no missing-module, syntax, ENOENT, or unhandled-process failure.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -euo pipefail; out=$(mktemp); trap '\''rm -f "$out"'\'' EXIT; if node --test scripts/generate-temperance-flow.test.mjs &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; rg -q "receipt|parity|stale|FLOW" "$out"; ! rg -q "SyntaxError|ERR_MODULE_NOT_FOUND|ENOENT|no such file or directory" "$out"; node --experimental-strip-types --test workers/quests/src/goal-graph/projection-contract.test.ts &gt;"$out" 2&gt;&amp;1 &amp;&amp; exit 1 || true; rg -q "temperance|projection" "$out"'</automated>
  </verify>
  <commit>Stage only both RED tests; commit `test(05-02): expose flow generation and foldback gaps`.</commit>
  <done>Immutable tests prove the generator, receipt boundary, readback parity, and production foldback requirements before GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build bounded sources and deterministic flow readbacks</name>
  <files>scripts/temperance-flow-sources.mjs, scripts/generate-temperance-flow.mjs, docs/architecture/temperance-flow.v1.json, docs/architecture/temperance-flow.md</files>
  <read_first>
    - scripts/generate-temperance-flow.test.mjs — satisfy the committed contract unchanged.
    - scripts/intent-graph-sources.mjs and scripts/generate-intent-graph.mjs — reuse selector, root, contained-output, atomic-write, check, and parity patterns.
    - ISA.md — select only frontmatter task, active ISC-1282..1285 checkbox state, and the uniquely prefixed reviewed Phase 5 decision; do not select historical bodies or generated receipt text.
    - .planning/STATE.md — select the exact current phase/status/next action and reject multiple or missing transition claims.
    - active 05 plans — parse frontmatter dependencies and XML task identities; do not invent task readiness.
    - prior 05 summaries and verification — select task outcome/evidence identity and pass/block status only; explicitly exclude any recorded flowDigest/sourceSetDigest or generated-output byte digest.
    - docs/architecture/intent-graph.v1.json — reference schema/path/digest only.
  </read_first>
  <action>
Implement the bounded source adapter with exact declared paths and unique selectors. Normalize task dependencies and determine a single ready unit only when the live STATE transition and active plan agree. Treat absent/conflicting plan/state/ISA, multiple ready units, unsatisfied dependencies, terminal work, stale evidence, or an unreviewed handoff as blocked. Select summary/handoff semantic status and evidence references while rejecting or excluding generated flow/source digest receipt fields; prove that adding or changing only an excluded generated-digest receipt does not change the source-set digest, while changing selected status/next-command/gate content does. Accept only a bounded verification result returned by the fixed host-owned Manifest verifier for an opaque evidence reference; require the fixed issuer/audience, deterministic freshness window, evidence pointer, exact task/projection/route binding, and replay-key/content consistency. Expose no generic raw-receipt, public-key, custom endpoint, or caller verifier production API. Test-only dependency injection must be unreachable from the CLI. Never read a host directory, mutate the host, or infer a provider from route intent.

Implement the generator with contained `--root` and output paths plus an optional opaque receipt reference for JSON inspection; mutually exclusive modes; compile-once JSON/Markdown; atomic sibling writes; zero-write check; exact byte comparison; and JSON-only stdout. Generate committed readbacks without a live receipt so route intent remains visible and resolved attribution remains explicitly unavailable/receipt-gated. Live receipt verification is a bounded read-only call to the fixed host verifier in JSON inspection mode only; it cannot affect committed deterministic files. Run double check, parity, privacy, source-preservation, and deterministic probes before committing.
  </action>
  <acceptance_criteria>
    - Default outputs derive only from the declared repository paths and reference the Intent Graph without copying or extending it.
    - JSON and Markdown contain identical schema, flow/source digests, source references, action-or-blocked result, route intent, gates, freshness, and stops.
    - Only the fixed host verifier's bounded verified result adds redacted resolved attribution; raw, custom-trusted, invalid, or unverified inputs block attribution and cannot alter the selected action.
    - Generation writes only the two contained output files and leaves every source byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/generate-temperance-flow.mjs --check &amp;&amp; node scripts/generate-temperance-flow.mjs --check &amp;&amp; node --test scripts/temperance-flow.test.mjs scripts/generate-temperance-flow.test.mjs &amp;&amp; node -e 'const fs=require("node:fs"); const j=JSON.parse(fs.readFileSync("docs/architecture/temperance-flow.v1.json","utf8")); const m=fs.readFileSync("docs/architecture/temperance-flow.md","utf8"); if(j.schema!=="cambium.temperance-flow-projection.v1"||j.projectionAuthority!=="read_only") process.exit(1); for(const v of [j.flowDigest,j.sourceSetDigest]) if(!m.includes(v)) process.exit(1); const s=JSON.stringify(j); if(/\/Users\/|\/Volumes\/|credential|api[_-]?key|promptBody|responseBody|providerStack|quota|failoverPolicy/i.test(s)) process.exit(1)'</automated>
  </verify>
  <commit>Stage only the adapter, generator, and two readbacks; commit `feat(05-02): generate deterministic Temperance flow readbacks`.</commit>
  <done>Operators and machines receive the same current read-only flow truth without host-policy duplication or mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject flow projections at the shared authoritative boundary</name>
  <files>workers/quests/src/goal-graph/projection-contract.ts, workers/quests/src/goal-graph/projection-contract.test.ts</files>
  <read_first>
    - workers/quests/src/goal-graph/projection-contract.test.ts — make the committed RED fixtures GREEN without weakening prior families.
    - workers/quests/src/goal-graph/projection-contract.ts — extend only marker recognition and authoritative-input rejection.
    - .planning/phases/04-provenance-preserving-intent-graph/04-04-SUMMARY.md — preserve early-rejection and bounded-receipt evidence.
  </read_first>
  <action>
Add the exact flow schema and normalized `temperance-flow` plus `projection` family marker to the shared derived-projection discriminator. Reject exact, malformed, and future-shaped flow projections through `validateAuthoritativeInput` before any operational intake. Do not import the repository compiler, generator, receipt parser, host code, D1, Telegram, network, clock, or provider modules. Preserve all ordinary-command acceptance and Goal/Intent projection behavior. Run focused shared-contract tests plus existing pure intake and authenticated route suites proving zero D1 reads and zero task/idempotency writes for the new fixture family.
  </action>
  <acceptance_criteria>
    - Exact and normalized flow projection markers are rejected as non-authoritative.
    - Unrelated commands remain accepted; existing Goal and Intent projection cases remain unchanged.
    - Shared runtime code contains only schema-family recognition, not repository compilation or host policy.
    - Route evidence proves rejection before operational reads/writes and preserves bounded redaction.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test workers/quests/src/goal-graph/projection-contract.test.ts workers/quests/src/goal-graph-intake.test.ts &amp;&amp; node --experimental-strip-types --test --test-name-pattern="goal graph intake|goal graph approval" workers/quests/src/handler.test.ts &amp;&amp; node --test scripts/temperance-flow.test.mjs scripts/generate-temperance-flow.test.mjs</automated>
  </verify>
  <commit>Stage only the shared contract and test; commit `fix(05-02): reject Temperance flow projection foldback`.</commit>
  <done>The generated flow cannot re-enter D1 or another authority lane, while existing traffic remains unchanged.</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Disposition | Mitigation |
|---|---|---|---|
| Forged/stale host receipt | Spoofing/Tampering | mitigate | Fixed host-owned Manifest verifier, no caller-selected trust root/endpoint, issuer/audience, freshness, evidence, task/projection/route binding, digest/replay checks. |
| Provider policy or credentials copied | Disclosure/Elevation | mitigate | Reject stacks, quotas, failover, secrets, prompts, session IDs, absolute paths. |
| Stale or divergent readbacks | Tampering | mitigate | Compile once, canonical digests, atomic writes, double check, parity tests. |
| Output path escape | Tampering/DoS | mitigate | realpath containment, temporary sibling, explicit outputs. |
| Flow projection enters D1 | Elevation | mitigate | Shared family discriminator and pre-read/write rejection. |
| Generated output becomes a source | Elevation | mitigate | Declared-source allowlist and self-source rejection. |
| Dependency/package compromise | Supply chain | accept | Node built-ins only; no manifest or lockfile changes. |
</threat_model>

<verification>
1. Generator `--check` passes twice and JSON/Markdown parity is exact.
2. Focused compiler, generator, shared guard, pure intake, and route tests pass.
3. Full `npm test`, drift audit, rendered-doc check, privacy scan, and exact allowed-path checks pass.
4. No host call, provider configuration, deployment, external write, D1 mutation, or package change occurs.
</verification>

<success_criteria>
- FLOW-01, FLOW-03, FLOW-04 are visible in deterministic matching readbacks.
- Resolved provider attribution is fresh-receipt-backed or explicitly unavailable.
- The projection never acquires operational or planning authority.
- Shared production intake rejects the entire flow-projection family before state access.
</success_criteria>

<output>
Create `05-02-SUMMARY.md` with three commit SHAs, digests, output paths, route/receipt state, focused/full test counts, zero-write evidence, and handoff to Plan 05-03. Do not claim Phase 5 complete.
</output>
