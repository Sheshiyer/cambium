---
phase: 06-documentation-stewardship
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - docs/LIFECYCLE.md
  - PROJECT.md
  - README.md
  - docs/README.md
  - docs/doctrine/README.md
  - .planning/README.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]
user_setup: []
must_haves:
  truths:
    - "D-01 / DOCS-01 / ISC-1286: docs/LIFECYCLE.md is the single human map for the exact five lifecycle classes and unchanged authority owners."
    - "D-02 / DOCS-02 / ISC-1287: lifecycle and discovery surfaces expose copyable machine/human inventory commands for a caller-selected commit, not links to committed readbacks."
    - "D-03 / DOCS-03 / ISC-1288: PROJECT, README, docs, doctrine, and planning indexes remain additive spokes with direct owner links."
    - "Live .planning/STATE.md uniquely owns mutable planning status and next transition; no index freezes its values."
    - "D-04 / DOCS-04 / ISC-1289: historical/evidentiary recovery and indexed item exceptions stay visible without relocation, deletion, or bulk rewriting."
  artifacts:
    - path: docs/LIFECYCLE.md
      provides: "Single lifecycle legend, authority owners, defaults, exceptions, memory boundary, recovery, and on-demand commands"
    - path: PROJECT.md
      provides: "Reviewed repository entry with stewardship navigation"
    - path: README.md
      provides: "Product discovery route into documentation stewardship"
    - path: docs/README.md
      provides: "Documentation discovery index with explicit-revision inventory entry"
    - path: docs/doctrine/README.md
      provides: "Root doctrine catalog linking lifecycle and inventory contract"
    - path: .planning/README.md
      provides: "Planning index delegating mutable status and next transition to live STATE"
  key_links:
    - from: docs/LIFECYCLE.md
      to: docs/architecture/contracts/documentation-inventory-v1.md
      via: "contract link plus copyable explicit-revision JSON and Markdown commands"
      pattern: "docs:inventory:json"
    - from: .planning/README.md
      to: .planning/STATE.md
      via: "direct live status and transition delegation"
      pattern: "STATE.md"
    - from: README.md
      to: docs/README.md
      via: "one additive product-to-documentation route"
      pattern: "docs/README.md"
---

<objective>
Publish the single lifecycle map and additive navigation for on-demand documentation stewardship.

Purpose: make current authority, derived inspection, evidence, history, and local-only boundaries directly navigable without copied doctrine, committed readbacks, or frozen mutable status.
Output: expanded lifecycle map plus five focused index updates.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-documentation-stewardship/06-CONTEXT.md
@.planning/phases/06-documentation-stewardship/06-PATTERNS.md
@.planning/phases/06-documentation-stewardship/06-02-SUMMARY.md
@docs/architecture/contracts/documentation-inventory-v1.md
@docs/LIFECYCLE.md
@PROJECT.md
@README.md
@docs/README.md
@docs/doctrine/README.md
@.planning/README.md
@.planning/STATE.md
@docs/plans/README.md
@docs/plans/product-branches/index.md
@docs/evidence/README.md
@docs/archive/README.md
@docs/memory/README.md
@docs/memory/boundary.json

<interfaces>
- `docs/LIFECYCLE.md` maps lifecycle and authority but never replaces an authority it maps. The exact class labels are `canonical`, `derived`, `historical`, `evidentiary`, and `local-only`.
- Owner precedence remains: VISION/MISSION doctrine; ISA approved goals/acceptance; live `.planning/STATE.md` finite transition; contracts/runbooks bounded instructions; ephemeral inventory representations read-only inspection.
- Inventory discovery links to the committed contract and copyable commands requiring `--source-revision <REV>`. It never links to or implies the existence of committed `docs/documentation-inventory.v1.json` or `docs/documentation-inventory.md`.
- Index roles are closed by D-03: PROJECT is reviewed pickup, README product discovery, docs/README documentation discovery, docs/doctrine/README catalog, and .planning/README a direct route to live STATE.
- Indexes may state stable roles and paths; they may not copy doctrine, acceptance checkboxes, status, progress, next commands, timestamps, provider/runtime state, or operating procedure bodies.
- Historical/evidentiary material remains at its existing path and recoverable through Git/history indexes. Directory defaults yield to explicit item/index evidence; callable-looking historical content stays visibly non-operational.
- The tracked root `MEMORY/` fact belongs to a selected inventory revision. `docs/memory/` is a neutral committed contract and ignored/provider memory is local-only; neither is substituted for the other.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the single lifecycle, authority, and recovery map</name>
  <files>docs/LIFECYCLE.md</files>
  <read_first>
    - docs/LIFECYCLE.md — extend this existing owner map instead of creating another.
    - 06-CONTEXT.md — implement D-01, D-02, and D-04 exactly.
    - docs/plans/README.md, docs/plans/product-branches/index.md, docs/evidence/README.md, and docs/archive/README.md — preserve history/evidence recovery and item exceptions.
    - docs/memory/README.md and docs/memory/boundary.json — preserve neutral committed memory contract versus local-only runtime memory.
    - docs/architecture/contracts/documentation-inventory-v1.md and package.json — document exact on-demand interfaces.
  </read_first>
  <action>
Extend `docs/LIFECYCLE.md` with exactly the five lifecycle labels and concise current-instruction/recovery semantics. Preserve and expand one owner-per-truth table for VISION/MISSION, ISA, live STATE, contracts, and runbooks. Add directory defaults and explicit source-backed item-exception precedence, including only indexed product-branch packets; label unindexed lookalikes and callable historical material non-operational without rewriting their bodies.

Record the root `MEMORY/` fact as revision-scoped inventory evidence, distinguish it from `docs/memory/`, and refuse ignored/provider memory inspection. Link the committed inventory contract and provide copyable commands that first select an explicit revision and then run `npm run --silent docs:inventory:json -- --source-revision <REV>`, `npm run --silent docs:inventory:markdown -- --source-revision <REV>`, and `npm run --silent docs:inventory:check -- --source-revision <REV>`. Require `--silent` so package-manager banners never contaminate parseable JSON or human Markdown stdout. Explain that outputs are ephemeral stdout views and can be regenerated for any commit. Add no generated-file links, copied doctrine/status/next command, new authority, or destructive operation.
  </action>
  <verify><automated>bash -c 'set -euo pipefail; for value in canonical derived historical evidentiary local-only; do rg -q "(^|[^[:alnum:]-])${value}([^[:alnum:]-]|$)" docs/LIFECYCLE.md; done; rg -q "documentation-inventory-v1.md" docs/LIFECYCLE.md; rg -q "docs:inventory:json" docs/LIFECYCLE.md; rg -q "docs:inventory:markdown" docs/LIFECYCLE.md; rg -q "docs:inventory:check" docs/LIFECYCLE.md; rg -q "product-branches/index.md" docs/LIFECYCLE.md; ! rg -q "docs/documentation-inventory\\.(v1\\.json|md)" docs/LIFECYCLE.md'</automated></verify>
  <done>The single human map defines exact classes and owners, revision-scoped memory, recovery and exception rules, and explicit-revision commands without mutable or destructive authority.</done>
</task>

<task type="auto">
  <name>Task 2: Connect additive indexes and remove frozen planning status</name>
  <files>PROJECT.md, README.md, docs/README.md, docs/doctrine/README.md, .planning/README.md</files>
  <read_first>
    - PROJECT.md, README.md, docs/README.md, and docs/doctrine/README.md — preserve each existing role and add only direct navigation.
    - .planning/README.md — remove frozen status/action prose and delegate mutable truth to STATE.
    - docs/LIFECYCLE.md — use the stable vocabulary and commands established in Task 1.
    - .planning/STATE.md — link directly without copying any current value.
  </read_first>
  <action>
Update the five indexes according to D-03. PROJECT links lifecycle, inventory contract, and docs discovery. README adds one concise route through docs/README and lifecycle. docs/README provides an ordered route to root doctrine, architecture/contracts, runbooks, lifecycle/on-demand inventory, evidence/history, ISA, and live STATE; include copyable explicit-revision command discovery without duplicating the lifecycle explanation. docs/doctrine/README stays a catalog with direct root-anchor, lifecycle, and inventory-contract links. .planning/README removes the frozen `Status: Complete` claim and its derived action, then delegates current status and next transition solely to `.planning/STATE.md`.

Do not copy doctrine paragraphs, criteria/checklists, progress, current status/command, timestamps, provider information, historical commands, inventory entries, or generated output. Do not relocate, delete, archive, externalize, or bulk-edit any historical/evidentiary source.
  </action>
  <verify><automated>bash -c 'set -euo pipefail; rg -q "docs/README.md" README.md; rg -q "docs/LIFECYCLE.md" PROJECT.md README.md docs/README.md docs/doctrine/README.md; rg -q "documentation-inventory-v1.md" PROJECT.md docs/README.md docs/doctrine/README.md; rg -q "STATE.md" .planning/README.md; ! rg -q "Status: Complete|action=complete" .planning/README.md; ! rg -q "docs/documentation-inventory\\.(v1\\.json|md)" PROJECT.md README.md docs/README.md docs/doctrine/README.md .planning/README.md; git diff --check'</automated></verify>
  <done>All five indexes lead to direct owners and on-demand inventory discovery, while planning status and next transition remain solely in live STATE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lifecycle map to authority owners | A descriptive map must not become doctrine, approval, planning, or operating authority. |
| Additive indexes to mutable state | Navigation may link to STATE but must not cache status or next commands. |
| Historical/evidence navigation to maintainer | Callable-looking records must remain recoverable and visibly non-operational. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-13 | Elevation of Privilege | high | lifecycle map | mitigate | Name exact owners and prohibit copied authority or executable effects. |
| T-06-14 | Spoofing | high | planning index | mitigate | Delete frozen values and delegate mutable status/transition directly to STATE. |
| T-06-15 | Tampering | high | item exceptions | mitigate | Require explicit index evidence; directory naming alone never promotes an item. |
| T-06-16 | Information Disclosure | high | docs/index prose | mitigate | Link safe repository paths/commands only; no bodies, machine paths, or private state. |
| T-06-17 | Repudiation | medium | inventory discovery | mitigate | Require explicit revision and link the versioned contract; no moving default or committed readback. |
| T-06-18 | Tampering / DoS | high | history/evidence | mitigate | No relocation/deletion/bulk rewrite; preserve recovery routes and non-operational labels. |
| T-06-SC | Tampering | low | package supply chain | accept | No dependency or package changes in this plan. |
</threat_model>

<verification>
- Exact lifecycle labels, owners, recovery routes, root-memory boundary, item exceptions, contract link, and all three explicit-revision commands are present.
- Root/docs/doctrine/planning indexes resolve to direct owners and contain no frozen STATE values or generated-file links.
- `git diff --check`, `npm run render-docs:check`, and focused documentation tests pass without deletion/rename.
</verification>

<success_criteria>
- Maintainers have one lifecycle/authority map and additive navigation from product entry through doctrine, operations, evidence, history, and live planning state.
- Machine and human inventory discovery is on demand for a chosen commit, not a committed projection.
- Historical evidence stays recoverable, exceptions stay explicit, and no source is relocated or rewritten wholesale.
</success_criteria>

<output>
Create `.planning/phases/06-documentation-stewardship/06-03-SUMMARY.md` through the normal execute-plan lifecycle. Ordinary summary/state/roadmap handling does not affect the reproducibility of any explicit prior commit.
</output>
