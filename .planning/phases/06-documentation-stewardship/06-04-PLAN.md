---
phase: 06-documentation-stewardship
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - scripts/infinite-game-anchors.test.mjs
  - ISA.md
  - .project/HANDOFF.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]
user_setup: []
must_haves:
  truths:
    - "D-01 / DOCS-01 / ISC-1286: the exact lifecycle map and unchanged owner precedence pass phase-wide acceptance."
    - "D-02 / DOCS-02 / ISC-1287: both on-demand formats reproducibly inventory the complete selected commit and agree on all shared identities."
    - "D-03 / DOCS-03 / ISC-1288: additive navigation reaches direct owners and live STATE without copied mutable truth or generated-file links."
    - "D-04 / DOCS-04 / ISC-1289: evidence/history remains recoverable, item exceptions are source-backed, and no relocation, deletion, or bulk rewrite occurs."
    - "T-06-22: every Phase 6 changed byte and generated stdout representation is free of secrets, native sessions, prompt/response bodies, raw memory, and machine-local absolute paths."
    - "Normal execute-plan creates the summary and updates STATE, ROADMAP, and REQUIREMENTS after tasks; Phase 6 neither owns nor suppresses that closeout."
  artifacts:
    - path: scripts/infinite-game-anchors.test.mjs
      provides: "Phase-wide DOCS/D authority, corpus, navigation, recovery, privacy, and zero-write sentinels"
    - path: ISA.md
      provides: "Checked ISC-1286..1289 with bounded verification evidence"
    - path: .project/HANDOFF.md
      provides: "Reviewed-held repository-only checkpoint and independent verification continuation"
  key_links:
    - from: scripts/infinite-game-anchors.test.mjs
      to: scripts/check-documentation-inventory.mjs
      via: "explicit-revision double-generation and no-write acceptance"
      pattern: "docs:inventory:check"
    - from: ISA.md
      to: .planning/REQUIREMENTS.md
      via: "ISC-1286..1289 evidence maps DOCS-01..DOCS-04"
      pattern: "ISC-1286"
    - from: .project/HANDOFF.md
      to: .planning/STATE.md
      via: "bounded verification-next checkpoint; live planning remains STATE-owned"
      pattern: "verify-work 6"
---

<objective>
Prove Phase 6 end to end, close ISA acceptance, and leave a bounded repository-only handoff.

Purpose: demonstrate that lifecycle, exhaustive explicit-commit inventory, navigation, recovery, privacy, and non-destructive boundaries work together while the installed GSD lifecycle remains unmodified.
Output: phase-wide sentinels, verified ISC-1286..1289, and one reviewed-held handoff checkpoint.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PROJECT.md
@ISA.md
@.project/HANDOFF.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation-stewardship/06-CONTEXT.md
@.planning/phases/06-documentation-stewardship/06-PATTERNS.md
@.planning/phases/06-documentation-stewardship/06-01-SUMMARY.md
@.planning/phases/06-documentation-stewardship/06-02-SUMMARY.md
@.planning/phases/06-documentation-stewardship/06-03-SUMMARY.md
@scripts/infinite-game-anchors.test.mjs
@scripts/documentation-inventory.test.mjs
@scripts/generate-documentation-inventory.test.mjs
@scripts/check-documentation-inventory.mjs
@docs/LIFECYCLE.md
@docs/architecture/contracts/documentation-inventory-v1.md
@PROJECT.md
@README.md
@docs/README.md
@docs/doctrine/README.md
@.planning/README.md

<interfaces>
- Phase-wide acceptance invokes the inventory only with an explicit resolved commit SHA. It never expects a working-tree snapshot, staged candidate, committed generated pair, or globally “latest” readback.
- DOCS-02 equality means JSON and Markdown generated independently for the same full SHA carry the same schema, projection authority, source-set and inventory digests, root-memory fact, exact entry path set/count, lifecycle values, canonical anchors, and exception evidence.
- Zero-write proof snapshots the repository tree, file modes/mtimes, and Git index before and after JSON, Markdown, and check commands. Normal GSD summary/state/roadmap/requirements writes occur only after plan tasks and are outside inventory command execution.
- T-06-22 uses the unique full `phase_base_sha` from 06-01-SUMMARY.md. Its audited path union is committed phase-range paths plus staged paths plus unstaged tracked paths plus untracked paths; for every present path in that union it scans the current worktree bytes, and it keeps deletion/rename checks as separate fail-closed gates.
- Final ISA evidence records exact commands and observed exit status after gates pass; it does not claim independent Phase 6 verification or production deployment.
- Handoff appends one bounded reviewed-held checkpoint. It records changed surfaces, verification evidence, unresolved independent verification, repository-only scope, and `/gsd:verify-work 6`; it authorizes no relocation, deletion, host/provider/runtime mutation, deployment, connected-repository write, or externalization.
- Do not precreate `06-04-SUMMARY.md`, edit `.planning/STATE.md`, `.planning/ROADMAP.md`, or `.planning/REQUIREMENTS.md`, perform a terminal commit, require an in-place checkout, or suppress normal execute-plan/execute-phase closeout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase-wide authority, inventory, navigation, recovery, and privacy sentinels</name>
  <files>scripts/infinite-game-anchors.test.mjs</files>
  <read_first>
    - scripts/infinite-game-anchors.test.mjs — preserve every Phase 3/4/5 assertion and the Phase 6 lifecycle matrix from Plan 01.
    - .planning/REQUIREMENTS.md and 06-CONTEXT.md — cover DOCS-01..04 and D-01..04 exactly.
    - scripts/documentation-inventory.test.mjs and scripts/generate-documentation-inventory.test.mjs — reuse public contracts and contained fixtures.
    - docs/LIFECYCLE.md, the five indexes, docs/plans/README.md, product-branches/index.md, and memory boundary files — assert owners, commands, recovery, and exceptions.
    - 06-01-SUMMARY.md — read the unique full phase base for T-06-22.
  </read_first>
  <behavior>
    - Named `DOCS-01 / D-01` through `DOCS-04 / D-04` tests prove the four observable requirements together.
    - An explicit full commit produces two deterministic package-level JSON runs, two deterministic package-level Markdown runs, and a passing zero-write parity check, all through `npm run --silent`.
    - Every tracked root Markdown, docs/, and .planning/ path at the selected SHA appears exactly once; no body or excluded-category loophole exists.
    - Index links resolve, live planning values exist only in STATE, historical/evidentiary recovery is preserved, and indexed exceptions outrank defaults.
    - T-06-22 rejects sensitive values or machine-local paths in Phase 6 changed bytes and both stdout formats without rejecting safe repository-relative paths/digests.
  </behavior>
  <action>
Add exact named tests `DOCS-01 / D-01`, `DOCS-02 / D-02`, `DOCS-03 / D-03`, `DOCS-04 / D-04`, and `DOCS-PRIVACY / T-06-22`. For a selected full commit, invoke exactly `npm run --silent docs:inventory:json -- --source-revision <REV>` twice, `npm run --silent docs:inventory:markdown -- --source-revision <REV>` twice, and `npm run --silent docs:inventory:check -- --source-revision <REV>`; `JSON.parse` must consume each complete package-level JSON stdout and each complete package-level Markdown stdout must equal the pure renderer and its repeated invocation byte-for-byte. Compare all shared fields, enumerate the commit tree independently, and require exact inventory path equality. Snapshot repository bytes, modes, mtimes, Git status, and index before/after every command. Prove dirty worktree/index data neither leaks into nor changes an explicit-commit result.

Resolve every new link within the repository. Assert one exact lifecycle vocabulary and owner precedence, root-memory revision scoping, no generated inventory file links, no frozen STATE status/next command in indexes, explicit item evidence for product-branch exceptions, and historical/evidence recovery without deletion/rename/body rewrite. Negative fixtures must reject authority escalation, destructive disposition, copied live status, unindexed exception promotion, source-body output, and private memory reads.

Implement T-06-22 by parsing one full `phase_base_sha` from 06-01-SUMMARY.md and validating its ancestry. Build a normalized, deduplicated audited union from: committed phase-range paths reported between `phase_base_sha` and current `HEAD`; staged paths from the index diff; unstaged tracked paths from the working-tree diff; and untracked paths from Git's standard untracked listing. For every path in that union that is present in the working tree, read and scan its current worktree bytes so later unstaged edits cannot escape behind committed or staged versions. Separately fail on every deletion or rename reported in committed, staged, or unstaged ranges rather than dropping absent paths from the privacy audit. Scan added lines across committed, staged, and unstaged diffs with diff headers removed, and scan complete untracked files. Detect Unix/Windows user-root paths, file URIs, API/access/refresh tokens, authorization/bearer values, client secrets/passwords, native session identifiers/tokens, prompt/request/response/message body/content/payload assignments, `.claude/MEMORY`, `MEMORY/LEARNING|SIGNALS|STATE`, and serialized raw-memory records. Also scan the exact package-level JSON/Markdown stdout. Reject non-text Phase 6 additions unless explicitly tested as bounded fixture metadata; do not exempt summaries, ISA, handoff, indexes, plans, STATE, ROADMAP, REQUIREMENTS, history, evidence, or contract paths.
  </action>
  <verify><automated>node --test --test-name-pattern='DOCS-0|documentation stewardship|T-06-22' scripts/infinite-game-anchors.test.mjs</automated></verify>
  <done>Named sentinels prove all four decisions/requirements, exhaustive explicit-commit parity, zero writes, link/authority correctness, recovery, exceptions, and full-range privacy.</done>
</task>

<task type="auto">
  <name>Task 2: Run final gates, close ISA acceptance, and append the bounded handoff</name>
  <files>ISA.md, .project/HANDOFF.md</files>
  <read_first>
    - 06-01-SUMMARY.md, 06-02-SUMMARY.md, and 06-03-SUMMARY.md — bind exact prior outputs, commits, deviations, and phase base.
    - ISA.md — check ISC-1286..1289 only after all repository gates pass.
    - .project/HANDOFF.md — append one bounded checkpoint without rewriting earlier evidence.
    - AGENTS.md and PROJECT.md — preserve reviewed-held and repository-only boundaries.
  </read_first>
  <action>
Before acceptance edits, run focused compiler/CLI/sentinel tests, `npm test`, `npm run drift:audit`, `npm run render-docs:check`, `npm run standalone:audit`, `git diff --check`, separate no-deletion/rename checks across committed/staged/unstaged ranges, and T-06-22. Resolve the current committed HEAD to a full SHA and run `npm run --silent docs:inventory:json -- --source-revision <full-sha>` twice, `npm run --silent docs:inventory:markdown -- --source-revision <full-sha>` twice, and `npm run --silent docs:inventory:check -- --source-revision <full-sha>` while proving no repository or Git-index write. Parse the complete package-level JSON stdout and compare exact package-level Markdown bytes. Treat these as on-demand evidence for that commit, not files to save.

Only after every gate passes, check ISC-1286..1289 and set ISA to coherent Phase 6 `verify`, progress `4/4`. Record exact commands and observed outcomes without inventing test counts or claiming independent verification. Append one handoff checkpoint naming the repository implementation head available at that moment, changed surfaces, explicit-revision inventory behavior, gates, zero-write result, normal GSD closeout still pending, and independent `/gsd:verify-work 6` as next step. Keep relocation, deletion, archival/externalization, host/runtime/provider configuration, deployment, credentials, vault/native stores, and connected repositories explicitly held.

Rerun focused sentinels and diff/privacy gates after these edits. Leave creation of 06-04-SUMMARY.md and updates to STATE, ROADMAP, and REQUIREMENTS to the installed execute-plan lifecycle. Do not create special commits, change GSD configuration, require sequential in-place execution, or ask the handler to skip any normal closeout stage.
  </action>
  <verify><automated>bash -c 'set -euo pipefail; REV=$(git rev-parse --verify HEAD^{commit}); node --test scripts/documentation-inventory.test.mjs scripts/generate-documentation-inventory.test.mjs scripts/infinite-game-anchors.test.mjs; npm run --silent docs:inventory:check -- --source-revision "$REV"; npm run --silent docs:inventory:json -- --source-revision "$REV" | node -e "let s=\"\"; process.stdin.setEncoding(\"utf8\"); process.stdin.on(\"data\", function(c){s+=c}); process.stdin.on(\"end\", function(){JSON.parse(s)});"; M1=$(npm run --silent docs:inventory:markdown -- --source-revision "$REV" | shasum -a 256); M2=$(npm run --silent docs:inventory:markdown -- --source-revision "$REV" | shasum -a 256); test "$M1" = "$M2"; npm test; npm run drift:audit; npm run render-docs:check; npm run standalone:audit; git diff --check'</automated></verify>
  <done>All repository gates pass, ISA records bounded 4/4 implementation acceptance, handoff records held external boundaries and independent verification next, and normal GSD closeout remains untouched.</done>
</task>

</tasks>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | One authority/lifecycle map | 06-01 compiler vocabulary; 06-03 lifecycle; 06-04 acceptance |
| GOAL | Inventory provenance, purpose, overlap, disposition, anchors | 06-01 schema/compiler; 06-02 formats/check; 06-04 parity |
| GOAL | Root/docs indexes through current GSD next step | 06-03 additive indexes; 06-04 link/status sentinel |
| GOAL | Recoverable history; stale plans/memory cannot masquerade | 06-01 exception/memory rules; 06-03 recovery; 06-04 negative tests |
| REQ | DOCS-01 | 06-01, 06-03, 06-04 |
| REQ | DOCS-02 | 06-01, 06-02, 06-03, 06-04 |
| REQ | DOCS-03 | 06-03, 06-04 |
| REQ | DOCS-04 | 06-01, 06-02, 06-03, 06-04 |
| CONTEXT | D-01 | 06-01, 06-03, 06-04 |
| CONTEXT | D-02 | 06-01, 06-02, 06-03, 06-04 |
| CONTEXT | D-03 | 06-03, 06-04 |
| CONTEXT | D-04 | 06-01, 06-03, 06-04 |
| RESEARCH | No RESEARCH.md; repository analogs in PATTERNS.md | 06-01 through 06-04 follow the mapped roles and repository-only redesign |

Deferred relocation, archival, externalization, deletion, connected-repository inheritance, and host/provider/runtime mutation are excluded exactly as directed by 06-CONTEXT.md.
</source_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase implementation to acceptance tests | Every requirement/decision needs executable, named evidence rather than prose assertion. |
| Repository/history to privacy audit | Changed bytes and generated stdout may expose sensitive local or runtime material. |
| Accepted implementation to GSD closeout | ISA/handoff tasks must coexist with normal summary/state/roadmap/requirements handling. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-19 | Elevation of Privilege | high | lifecycle/index/readbacks | mitigate | Named negative authority tests and exact owner precedence. |
| T-06-20 | Tampering / Repudiation | high | explicit revision/parity | mitigate | Full-SHA binding, independent tree enumeration, double generation, and shared-field comparison. |
| T-06-21 | Tampering / DoS | high | corpus/history | mitigate | Exact path-set equality plus no deletion, rename, relocation, or bulk rewrite. |
| T-06-22 | Information Disclosure | high | Phase 6 range/stdout | mitigate | Union committed, staged, unstaged tracked, and untracked paths; scan present worktree bytes and exact package stdout; gate deletion/rename separately. |
| T-06-23 | Elevation of Privilege | high | GSD closeout | mitigate | Do not own/suppress summaries or ledgers; run through installed execute-plan unchanged. |
| T-06-24 | Spoofing | high | ISA/handoff claims | mitigate | Record observed commands/results, distinguish implementation acceptance from independent verification. |
| T-06-SC | Tampering | low | package supply chain | accept | No installs; dependencies and lockfiles remain unchanged. |
</threat_model>

<verification>
- Named DOCS-01..04 / D-01..04 and T-06-22 sentinels pass.
- Explicit-commit JSON/Markdown/check commands are deterministic, parity-complete, and zero-write.
- Full `npm test`, drift, rendered-doc, standalone, diff, privacy, link, no-deletion/rename, and acceptance coherence gates pass.
- Normal execute-plan creates the plan summary and updates GSD tracking after tasks without any Phase 6 override.
</verification>

<success_criteria>
- DOCS-01..04 and D-01..04 have named passing repository evidence.
- Maintainers can inspect complete machine and human inventories for any chosen commit without creating a stale committed projection.
- ISA records bounded Phase 6 implementation acceptance and handoff preserves independent verification plus held external/destructive boundaries.
- The installed GSD execution lifecycle remains unchanged and fully usable in its normal worktree mode.
</success_criteria>

<output>
Create `.planning/phases/06-documentation-stewardship/06-04-SUMMARY.md` through normal execute-plan handling, then allow the standard STATE, ROADMAP, REQUIREMENTS, and metadata updates. Do not precreate or suppress any closeout artifact. Continue with `/gsd:verify-work 6` only after execution completes.
</output>
