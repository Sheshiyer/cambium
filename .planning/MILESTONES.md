# Milestones

## v0.4 Cambium Infinite-Game Doctrine and Intent Graph (Shipped: 2026-08-29)

**Phases completed:** 5 phases, 16 plans, 35 tasks

**Key accomplishments:**

- One enduring Vision and one renewable Repository Mission now anchor Cambium without becoming a competing planner.
- Every doctrine discovery surface now points to the canonical anchors while operational Mission terminology remains explicitly bounded.
- A pure Node-built-in compiler now produces deterministic read-only intent projections with exact provenance, closed graph semantics, and fail-closed authority boundaries.
- A single source declaration now compiles 19 provenance-bound nodes and 25 labeled edges into byte-identical-current JSON and Markdown readbacks with fail-closed stale checks.
- Operators can now discover the deterministic intent graph without mistaking it for doctrine, planning, acceptance, or D1 operational authority, and Phase 4 acceptance is closed from committed evidence.
- A shared derived-projection guard now rejects Intent Graph foldback at Telegram intake before D1 reads or proposal persistence, with bounded receipt and immutable-range proof.
- Deterministic authority resolution now yields one dependency-safe GSD action or a source-backed blocked result, with receipt-gated route attribution and no operational writer.
- Exact repository authorities now compile into matching read-only flow readbacks with fixed-boundary receipt attribution and pre-D1 projection-family rejection.
- A stateless Ralph interpreter and fixed-boundary runner now execute, verify, CAS-persist, recover, and exit exactly one unit without owning a ledger or self-certifying Phase 5.
- Explicit-commit, body-free corpus inventory with five closed lifecycle classes, source-backed exceptions, and matching zero-write JSON/Markdown renderers
- Caller-selected commit inventories now emit deterministic machine JSON or human Markdown on stdout, with a no-write checker proving repeated-byte stability and complete shared-object parity.
- One closed lifecycle map and five additive indexes now route maintainers to direct authority owners, explicit-revision inventory commands, recoverable evidence, and live GSD state without committed readbacks or frozen status.
- Phase-wide sentinels now prove exhaustive explicit-commit inventory parity, direct-owner navigation, recoverable evidence, zero writes, and full-range privacy before ISA and the reviewed-held handoff accept the implementation.
- SHA-bound fail-closed SAFE-01..03 compiler over one committed revision, with unchecked ISC-1290..1293 ISA acceptance and exported digest/selector helpers
- Closed `--source-revision` `safety:check` CLI over `compileDeterministicSafety` with one SHA-bound receipt line and an npm dispatcher that never hardcodes HEAD
- Phase-wide SAFE-01..04 and T-07 sentinels now prove SHA-bound `safety:check`, fail-closed doctrine/authority/freshness/privacy fixtures, checked ISC-1290..1293, and a reviewed-held `/gsd:verify-work 7` checkpoint

---

## v0.3 Managerial Control Loop (Shipped: 2026-08-17)

**Phases completed:** 2 phases, 2 plans, 0 tasks

**Key accomplishments:**

- Proved a replay-safe, D1-leased synthetic service-agreement draft through immutable R2/D1 artifact readback.
- Exposed the same bounded flow through feature-gated Telegram intake and a redacted operator receipt.
- Preserved the human-approval boundary with no send, signature, publication, or real-client action.

**Known process debt:** 4 historical evidence-packaging warnings retained in the milestone audit; no broken integration edge or E2E blocker.

**What's next:** Cambium Infinite-Game Doctrine and Intent Graph.

---
