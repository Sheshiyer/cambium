# the why-handler's deviation ledger (the cortex-write STUB — runtime data; I3 → the real NIM Worker)
deviations.jsonl
node_modules/
dist/
apps/cambium-r3f/release/
*.tsbuildinfo
.DS_Store
.codegraph/
.readme-gen.json
.playwright-mcp/
.artifacts/
__pycache__/
*.pyc
apps/cambium-r3f/public/assets/meshy/image-to-3d/*/optimized/model-*.glb

# the unified cortex local store (variable-contract store; runtime - I3 follow-up = the CF Worker)
/cortex/

# the operator's event-sourced world-state (runtime; the replay log is deviations.jsonl)
.operator/
.wrangler/

# local/private reference exports; never commit account, payment, or third-party prompt dumps
motionsites-export/
.playwright-cli/

# temperance engine runtime state (host- and session-scoped; regenerated per run)
.temperance/manifest.json

# superset session-scoped setup/run scripts and hands-init snapshot (host-specific)
.superset/

# transient temperance orchestration state (undated; regenerated each planning wave —
# dated `.planning/YYYY-MM-DD-*.{md,json}` files are audit records and remain committed)
.planning/APPROVALS.json
.planning/NEXT-WAVE.json
.planning/ORCHESTRATION.json
.planning/PLAN-OPTIONS.json
.planning/mission-control-visual-swarm-tasks.json
